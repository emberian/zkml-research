# Resume the fixed utility comparison

[EXECUTED] Utility is complete; do not repeat it or tune on its outcomes.
`manifest.json` seals the full report, 64-history score/transition records,
compact first-two-history fixture and same-author independent implementation
audit. `freeze.json` pins 96 consumed/source files before the first evaluation.

[OPEN] Proposed encrypted successor: reuse the unchanged first-two-history
384 Learn/96 Infer schedule, all per-operation public byte replays, before/after
read-input hashes, two public routes and deferred private correctness drain.
Root schedules the launch. Keep public fixtures and the unrestricted client
key boundary explicit; no model call or new utility estimate is needed.
