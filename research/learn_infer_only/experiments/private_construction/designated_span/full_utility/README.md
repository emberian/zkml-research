# Complete designated fixed-span utility integration

[EXECUTED launched, incomplete] The current immutable packet is
`runtime/full_003/`, with preparation, launch and validation records in
`reports/full_003/`. Root authorized launch after the matching small run passed.
The full workload is running; no full-runtime success is claimed yet.
`full_001` and `full_002` are earlier unlaunched preparation snapshots,
superseded to add worker-startup cleanup and nonempty signature-library pins.
The installed cryptography package has no RECORD inventory; the final packet
pins its 79 actual Python/native implementation files instead. The first two
packets executed no cryptography and started no services.
This driver joins the already frozen
577-dimensional utility workload to the designated DDH backend, persistent
public host, durable authority and independently verifying ciphertext acceptor.
It preserves all 384 Learn, 96 Infer and 256 exact-original expiry events over
two independent histories. Each history receives a fresh genesis, public
context and private setup ceremony; each has two routes with capacity 32.
No model execution, training, new query selection or semantic-successor
promotion is part of this experiment.

[DERIVED launch condition] The prepared source is executable only after the
small 40-Learn/four-Infer/eight-expiry integration has completed successfully
and the root agent has authorized this longer run. `run --small-pass FILE`
checks that report's success/counts, private integer comparison, public
acceptance-before-decode condition, genesis digest and exact matching journal
and crypto source hashes before performing setup or starting any service.
The command-line condition is an artifact check; root authorization remains
a separate instruction, not something elapsed time supplies.

```sh
python3 driver.py prepare --runtime runtime/full_004 --reports reports/full_004
# Preparation only: source/fixture copying and hashes; no crypto or service execution.

# Run only after the matching small path passes and root signals launch:
python3 runtime/full_003/source/driver.py run \
  --runtime runtime/full_003 --reports reports/full_003 \
  --small-pass ../integration/evidence/normal_001/report.json
```

[DERIVED frozen preparation] Preparation copies the exact eight journal/service
sources, four crypto files, persistent host and its own source pins, and these
two driver files into an ignored mode-0700 runtime. It verifies and copies all
384 original issuer vectors, 16 original public query rows, the full event
index and independent integer oracle against the existing materialization
manifest. The original fixture is publicly known; its source hashes are
provenance, not hashes of newly secret observations. Issuer/oracle data files
are mode 0600 and are not arguments to host, authority or acceptor processes.
The interpreter executable, native OpenSSL library, cryptography signature
package's source/native files and all local source files are pinned. Fresh
setup and input encryption use the backend's OS-backed randomness.

## Public and recipient phases

[DERIVED] A `Run` subclass replaces only host `propose` calls with the frozen
persistent worker. Trusted issuer/command authorization, all public transition
validation, authority acceptance, independent acceptor recomputation and
canonical state/output semantics remain the frozen small-run code. The
acceptor holds no scalar path and durably acknowledges only the selected,
independently recomputed ciphertext. The complete accepted envelopes are
compared with its ordered journal and installed head. An independent public
replay recomputes the entire history and checks the final route admissions
(128 and 64) and queue lengths (32 and 32).

[DERIVED] Both histories finish all public replay/status/history checks and
public log/source/storage inventories before any private drain. Their host
workers, authorities and acceptors are then closed. Only after both public
histories end does a separate process invoke each recipient-private drain,
which owns the 16 dedicated scalars and stores its answers in a distinct
private database. No subsequent host RPC, public protocol operation or
private-decoder timing is emitted. A separate private oracle reconstructs
each exact integer state directly from the original input vectors, compares
all 96 decoded scores both with that reconstruction and the original frozen
oracle, and preserves every original correct/incorrect result. It imports no
crypto implementation.

[HYPOTHESIS acceptance criteria] All 96 integer comparisons must pass, including
16 known empty-state controls and 80 nonempty outputs. The original classifier
tie rule is score >= 0 → +1; the backend's auxiliary mathematical signum is not
used for classification. Expected utility totals remain 52/96 overall, 44/80
nonempty and 18/32 final-phase checks. All 44 original errors remain errors.
This is a conjunction test for exact encrypted computation, independent public
history verification and the already measured limited adaptation behavior;
it is not a new utility estimate or evidence of new model capability.

## Costs and failure checkpoints

[DERIVED] Public reports will retain actual setup phases and child-operation
counts, per-role crypto wall costs, replay time, total public-phase wall time,
persistent worker startup/lifetime/cache and full-read/hash counts, exact
context/state/output sizes, logical live queue bytes and all retained CAS
object counts/bytes. Cache JSON size is distinguished from process memory.
The whole public phase includes all roles and full replay; the separate
2.662× paired host result is not extrapolated into an end-to-end speedup.
Private decode/oracle costs remain in private files. No public total spans
the private decode phase.

[DERIVED] A public checkpoint records every completed revision and cumulative
counts. Public failures preserve that checkpoint, existing DB/CAS state,
logs and a failure note. Private-phase failure details are saved privately.
The initial driver deliberately does not resume or repeat a partially
completed run automatically; a failure must be inspected and a concrete
continuation chosen. Setup failures close any public processes already started.
Preparation and source syntax checks alone do not establish runtime success.

[INFERRED budget, pending small-run measurements] A full run can take two
hours or more even with the persistent host: issuer encryption, three active
transition roles, full independent replay and first-time ciphertext subgroup
checks remain. The six-Learn paired host measurement cannot price those other
roles. Root will set the launch time allowance using the completed small
integration's actual role counts and costs.

[DERIVED scope] Classical DDH and classical authorization; dedicated correlated
fixed-span recipient keys; trusted setup/private delivery/erasure and issuer;
independent command policy and persistent recipient state; same-account process
separation without OS isolation. Recipient scalar holders can still compute
their fixed-span projections outside the local finality gate. Public fixture
utility does not establish hidden-state ambiguity on the real encoder image,
private cognition, nonlinear protected learning, post-quantum security or
absence of all recipient-side information. No adversarial, malformed-input,
routing or extraction experiments are included.
