# Public ciphertext-window relation

[EXECUTED] Three new Lean modules kernel-check with 44 exact axiom pins. The patch is `../../../formal/durable_integration/bfv_window/minidregg-ciphertext-window.patch`, SHA256 `630d2fa6c0ec0c2b049fcc0affeca6c9b22a5a264cf638890780e9e0fe5eb4eb`. It depends on the original resident-release patch and first durable-integration patch, not the EMA patch. No companion tree was edited. Scry queries 0; web queries 0.

| New source under `formal/durable_integration/bfv_window/` | SHA256 | Pins |
|---|---|---:|
| `Theory/CiphertextWindow.lean` | `ad2e496b8f8d1fbf7125cf6be9306a97c9933a66c7882066d087a2b04c8605ee` | 17 |
| `Assurance/CiphertextWindowCell.lean` | `7b215707ee631a3d04854f6dd2fd044511dbcfd6d59b21c602380ff1d4c8a77d` | 7 |
| `Assurance/CiphertextWindowWitness.lean` | `8503df829d5f5bcd9aa13bb5febd578b533dd3f8e8a075a687fa4fd30ffe9d92` | 20 |

[DERIVED] The public state contains a queue of `(admissionId, ciphertext)`, a ciphertext accumulator, and a monotone next admission counter. `Ct` is any additive commutative group. A lawful codec canonically encodes ciphertexts. The separate admission predicate sees the id and those canonical public ciphertext bytes. No plaintext or decryption credential appears in the transition state.

[DERIVED] `CiphertextWindow.check:66` enforces positive W, source length ≤ W, fresh id equal to the current counter and admission acceptance. A growing queue requires no expiry; a full queue requires the current head's id and its exact canonical ciphertext bytes. `sameSerialized_eq:57` derives ciphertext equality from the codec round trip. The actual `advance:77` subtracts the **proposed** expiry, so this check is load-bearing; equality is not silently assumed inside the executor.

[DERIVED] `reachable_queue_sum:294` says every reachable state has accumulator equal to the sum of exactly its current ciphertext queue and queue length ≤ W. `window_invariant:285` also preserves admission provenance, ids less than the current counter, and uniqueness of current ids. `all_finite_horizons:339` strengthens the conditional reachability result: for any positive W and a stream accepted at each id, the actual checked continuation exists at every finite horizon and preserves the same invariants. Natural-number counters and arbitrary finite histories are mathematical objects; this is not a machine-counter overflow or availability theorem.

[DERIVED] `CiphertextWindowCell.coeffStream:24`, `vectorStream:28`, and `ciphertextStream:32` compose existing streaming codecs into lawful logical modular coefficient arrays. The whole state codec includes queue order, each id, accumulator and next counter. The resource is total; there is no absent-state ambiguity. Arbitrary digest functions are permitted. Exact canonical bytes, not root equality or hash injectivity, determine the logical state (`canonical_state_opens:83`). The logical ciphertext group may also be a product of RNS limbs; the supplied flat `ZMod q` vector codec is not asserted to be the Rust representation.

[DERIVED] `materialized_window_binding:147` connects a checked window transition with the existing validated resource write, independently opened actual current cell, full DataIntent preflight and fresh journal transaction. It concludes exact installed bytes, the logical post-state and its sum/provenance, and the installed post-root matching `context.next`. `ExecutedHistory:169` ties an entire sequence to actual materialized snapshots and installs. `executed_history_invariant:188` proves installed canonical bytes encode the same logical state, its counter equals the actual journal length, and its current queue satisfies the logical invariant. `executed_is_durable_reachable:214` projects this into the first tranche's existing durable reachability. This uses that store executor; it does not add another one or reuse Stage0/EMA arithmetic as a BFV certificate. **The history is exclusive to window admissions:** its edges are all window transitions, so counter=journal length does not cover separately journaled inference/no-op or unrelated cells. A mixed learn/infer history needs an admission projection or a scheduling restriction; the generic authority revision is not automatically the window counter.

[EXECUTED] The nonzero `ZMod 17` two-component witness admits `(3,5)`, then `(7,1)`, then `(2,6)` into a W=2 queue. The third step expires exactly the first ciphertext; the surviving ids are `[1,2]`, the accumulator is the sum of the latter two, and the counter is 3. `unbounded_public_fixture:192` inhabits the always-admitted stream premise, continuing with a specified nonzero ciphertext for all later ids. Three genuine DataIntent commits also inhabit `ExecutedHistory` (`executed3:169`, `executed_history_subject:178`). `exact_durable_retry:186` returns the existing recorded intent without a second subtraction. The witness deliberately uses a constant digest, establishing that the canonical-byte argument does not hide an injective-root premise.

[REFUTED: omitted exact current-entry checks] `wrong_serialized_expiry_refused:66` rejects a different ciphertext with the same toy public plaintext projection. `stale_id_and_denied_input_refused:70` rejects a stale expiry id, reused fresh id and denied fresh ciphertext. `bypassed_byte_check_breaks_sum:77` demonstrates nonzero ciphertext debt even though that toy plaintext projection agrees. Generic `replacement_debt:229` identifies the debt as old minus proposed; `bypassed_wrong_ciphertext_breaks_sum:245` proves the queue invariant fails when the replacement is unequal. This projection is an algebraic toy, not a BFV decryption implementation or cryptographic attack.

[REFUTED: detached logical/durable histories] The one-step materialization witness is deliberately also tested with logical counter 2 but an empty initial durable journal. `detached_history_is_not_reachable:127` refuses to classify that pair as an executed history. The separate three-commit subject inhabits the stronger linked relation. Independent logical reachability and successful one-step preflight alone do not supply the missing durable history.

## Actual Rust source and serialization control

[SOURCE: implementation inspected] `experiments/he_closure_costs/sliding_window_83_probe/src/main.rs:42–56` encrypts a new input with the public key, adds that ciphertext, appends it, then pops and subtracts the same old ciphertext when the queue exceeds W. Every 64 steps it recomputes the current queue sum and compares exact `to_bytes()` output. Its plaintext queue entries, secret test reader and reference sums are testing instrumentation, not fields of our public transition. That lane records the actual W128, width577, 1,024-update runs separately.

[SOURCE: implementation inspected] In `/Users/ember/dev/breadstuffs/vendor/fhe-dregg/src/bfv/ops/mod.rs:54–69`, addition checks parameter identity, level and polynomial count and clears the seed metadata after ordinary nonempty addition. Subtraction has matching checks at `:149–164`. In `src/bfv/ciphertext.rs:135`, serialization converts to the protobuf form. `:156` constructs an empty-polynomial zero placeholder; `:167–179` serializes polynomials and optional seed metadata and indexes the final polynomial. Those are runtime representation conditions outside the abstract group law.

[EXECUTED] `serde_probe/src/main.rs` uses the actual vendored Rust library at the actual Q83 parameters. `serde_check_02.json` records a successful offline release run with source/dependency hashes and unchanged inputs. Its output is:

```text
EMPTY_INITIAL_ZERO serialize_panics=true polynomial_count=0
NONEMPTY_GROUP_ZERO serialize_ok=true polynomial_count=2 serialized_bytes=85022 decrypts_zero=true
SEEDED_ALIAS exact_polynomials_equal=true serialized_equal=false seeded_bytes=42545 expanded_bytes=85022 same_plaintext=true
PUBLIC_ZERO_NORMALIZATION empty_serializes=true seeded_alias_equal=true PK_bytes_preserved=true restored_polynomials_equal=true zero_from_public_ciphertext_self_subtraction=true
PASS serializer domain/normalization controls; no attack on PK window and no no-reader claim
```

[REFUTED: raw Rust serializer is a total canonical codec on all ciphertext representations] Serializing the empty zero panics (caught deliberately; expected panic text is retained in stderr). A seeded ciphertext and its expanded form have identical polynomial objects but different protobuf bytes. Neither finding attacks the actual public-key window: its fresh public-key ciphertexts are unseeded, and the recorded window checks occur after admission. They prevent identifying the universal logical codec with raw `to_bytes()` without a domain/refinement argument.

[EXECUTED] A concrete public normalization control computes a nonempty exact group zero as a public ciphertext minus itself, then serializes `ct + full_zero`. It handles the empty initial placeholder, makes the tested seeded alias byte-identical to its expanded form, preserves the tested public-key ciphertext bytes and restores exact polynomial equality after decoding. It requires a compatible nonempty zero with matching parameters, level and component count. The fixture retains a full secret key only to test decryption; generating the normalization zero and running the normalization use public ciphertext operations. This is a checked fixture, not a proved general Rust canonicalization refinement or absence-of-reader result. Old pre-normalization output/source is preserved in `serde_snapshots/` and the numbered logs.

[OPEN] Remaining refinement obligations: establish a lawful public serializer domain/canonicalizer for every admitted runtime state (including genesis zero, seed metadata, parameter/context/level/component count), relate logical group operations to actual RNS/NTT arithmetic and exact Rust add/sub, prove admission's encryption/source/range contract, and connect a modular phase map to implementation readout. The HE lane owns the separate phase/noise/signed-output theorem and relies on `reachable_queue_sum`; no additive map from a finite ciphertext group to the integers is assumed here. The public queue proof contains no noise or decryption-correctness theorem of its own. Fixed-cell finality authorization, an actual BFV descriptor/FS adapter, succinct/hiding evidence, physical persistence/output mediation and credential-absence security remain separate.

## Reproduction and provenance

[EXECUTED] Run from `/Users/ember/dev/zkml-research`:

```sh
python3 research/learn_infer_only/experiments/durable_integration/bfv_window/check.py Theory/CiphertextWindow.lean Assurance/CiphertextWindowCell.lean Assurance/CiphertextWindowWitness.lean
python3 research/learn_infer_only/experiments/durable_integration/bfv_window/review.py
python3 research/learn_infer_only/experiments/durable_integration/bfv_window/serde_check.py
```

[EXECUTED] Final Lean records are `lean_CiphertextWindow_10.json`, `lean_CiphertextWindowCell_09.json`, and `lean_CiphertextWindowWitness_06.json`, all exit 0 and source-matching with unchanged inputs. All 44 pins depend on subsets of `propext`, `Classical.choice`, and `Quot.sound`; none introduces a custom axiom. `review_01.json`/`review.json` records the red-tested forbidden-token scan over precisely the three source files, direct source/olean imports, successful staged Theory and Assurance umbrellas, read-only apply check, actual apply/content comparison in a full copied source corpus, and the real import-boundary script over that copied Theory/Selvage corpus plus this patch. Companion dependencies are prebuilt oleans, not a clean whole-tree rebuild. The first durable and EMA source/patch hashes remain unchanged. Earlier failed elaborations and stopped exploratory execution attempts remain recorded; only final source-matching successes support the claims above.

[EXECUTED] `InspectWindow.lean` runs the actual public Lean checker/step on logical two-component Q83 vectors for16 transitions at W2: `Except.ok (16,14,16,2,16,true,true)` records14 expiries,16 codec round trips, final length2/counter16, exact sum and final ids. `inspect_08.json` pins the source and all three unchanged proof dependencies. This is public finite-vector execution, not encryption or a performance claim. Larger exploratory10000/256-step runs were stopped/timed out; no completed count or speed claim is drawn from them. The all-finite-horizons statement is the separately kernel-checked theorem, not an inference from this finite run.

[EXECUTED, independent review] The adversarial lane independently checked all44 pins together with the HE62 dependent pins. Its `lean_window_journal_frame_02.json` executes a valid unrelated no-op cell transaction after the three admissions: window bytes remain fixed, but full journal length4 differs from admission counter3, so the exclusive `ExecutedHistory` relation excludes it. Its coefficient-decoder alias tooth also shows that decoding natural18 through `coeffStream17` returns coefficient1. The codec round trip supplies injective canonical **encoding**, not uniqueness of every accepted decoder input; exact-byte openings and `sameSerialized` use canonical encoding and remain scoped correctly. A raw input wire adapter must reject noncanonical bytes or normalize them before assigning canonical identity. These are explicit interface/refinement obligations, not contradictions of the frozen44.

[DERIVED, independent cost review] The live queue bound W does not bound durable replay storage. Existing DataIntent erasure retains complete canonical post-state writes in every ReplayEnvelope; without physical sharing, compression or a proved checkpoint/compaction rule, T admissions can retain order T·W·ciphertext-size serialized post-state payload, plus ids/metadata. The current queue is O(W), while the admission counter and journal continue growing. This is semantic payload accounting, not an observed storage benchmark or a lower bound on all implementations. See `ADVERSARIAL_REVIEW.md` §§15–16 and `experiments/adversarial_review/window_semantic_review.json` for final independent44/HE62, strict-component serde02, codec-alias and unrelated-journal checks.
