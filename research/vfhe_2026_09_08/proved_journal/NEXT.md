[OPEN integration] Feed the root-owned `update_pipeline/` output through
`service.py request --case ... --proof ...` with the exact approved template.
For a new accepted update, its accumulator must be the installed output and its
learner event must advance. Keep genuine source-event authorization explicit.
The completed demonstration is not pending another independent review gate.

[SOURCE preservation] Preserve service source bytes and `results/demo001/`.
The demonstrated live state remains under ignored `runtime/demo001/service/`;
both service processes are closed. Existing stopped routing paths and frozen
predecessor artifacts were not used or modified. Root owns shared ledgers and
publication.
