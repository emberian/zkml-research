[EXECUTED] Complete: one native deployed Poseidon/PaddingFreeSponge collision between canonical 20- and 24-word payloads. Source pins unchanged; exact result and command retained. No full proof, key setup or private data.

[EXECUTED pure source/data] Existing fixed canonical profile admits the retained public log and refuses one salt4→salt8 FRI shape control. The two colliding payload lengths fit different canonical root roles, not the same one.

[DERIVED] This refutes only a blanket untyped variable-length collision-resistance premise. It does not establish a forged accepted proof or require a fixed-profile encoding repair by itself. Owner is using shape-filtered extraction. Lane is held after this result.
