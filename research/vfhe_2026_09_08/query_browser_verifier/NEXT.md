# Next

Root can integrate `web/shared-query-worker.js`, `query-consumer.js` and `query-case.js` beside the site's existing frozen verifier assets, with the six public fixtures under a separate bundle base URL. The action should report success only when `result.verified === true` and scope it to both exact class-query arithmetic computations. README gives the interface and sizes.

Actual site/browser execution belongs to root. This lane is complete; preserve the package, recorded proofs, source pins and single Node execution. No additional proving, private-state access or backend changes are needed.
