# Concrete adapter sources and preparation evidence

[SOURCE: inspected unchanged source] The source map in SOURCE_PINS.json
records the direct public-coin adapter setup.py and its four crypto files,
copied without edits into source/public_setup/. The wrapper uses its
registration, matrix/completion and context routines. The normal workload
uses its already reviewed issuer, linear update, exact original expiry,
registered transform and recipient decode routines. Legacy dealer branches
remain present but are outside the driver's explicit backend allowlist.

[SOURCE: full written mathematics] The parent PROPOSAL.md hash remains
`ec9af914cb349e0bc6881bd8a5b99fd493e43b106a869a44eb58edf758083ab3`;
its independent mathematical REPORT.md hash is
`387c53f581c6adda25dc0a5da5ab91257000e59e1d016bd5e78dce3459e7b90d`.
The accepted classical-ROM game is a conditional theorem; this concrete
single-seed execution is not a validation of its ideal-oracle premise.

[SOURCE: official API and standard landing pages read]
[Python hashlib documentation](https://docs.python.org/3/library/hashlib.html#shake-variable-length-digests)
specifies SHAKE digest lengths in bytes and gives the example used by
helper_check.py. The [FIPS202 page](https://csrc.nist.gov/pubs/fips/202/final)
identifies the standard for SHA-3 and extendable-output functions. This lane
read those HTML pages, not the FIPS PDF or a hash security reduction.

[EXECUTED query accounting] This new implementation tranche used zero web
search queries and two primary HTML opens, zero Scry SQL/schema, zero Kagi,
zero eprint requests and zero new PDF reads. The preceding mathematical
tranche's separate counts are unchanged in ../SOURCES.md.

[EXECUTED final preparation command, after source corrections]

```sh
python3 -B research/learn_infer_only/experiments/private_construction/designated_span/public_coin_setup/public_seed/adapter/helper_check.py > research/learn_infer_only/experiments/private_construction/designated_span/public_coin_setup/public_seed/adapter/helper_check.log
```

[EXECUTED] Exit zero. The helper uses public hashlib calls, arithmetic,
AST parsing and the frozen group constants only. It imports no crypto backend
or NativePow and creates no real credential, ciphertext or registry. Earlier
preparation executions also passed; the final log is pinned to the frozen
source package. The change from an unsourced hardcoded empty-message vector
to the API's actually inspected example was made before source freeze.

[DERIVED availability provenance] CONTRACT.md's fixed group lengths are
executed in helper_check.log. Its cap bound `577/2^128` follows from16 tau
and 561 U coordinates and each ideal-word rejection probability below1/2.
This is a finite symbolic bound, not an observed failure rate or concrete
hash-security estimate. No deliberately failing or grinding fixture was run.

## Completed normal execution and public collection

[EXECUTED] The copied review gate was validated before the one run. Launch
used driver.py with the sole reports/normal_001 root, with stdout/stderr
redirected to reports/normal_001.stdout.log and reports/normal_001.stderr.log.
Exec session57136 began at 06:43:53 UTC and finished exit zero. REPORT.md
records the 33/four/one result; sources/contract stayed frozen after launch.

[EXECUTED postrun collection command]

```sh
python3 -B research/learn_infer_only/experiments/private_construction/designated_span/public_coin_setup/public_seed/adapter/collect_public.py > research/learn_infer_only/experiments/private_construction/designated_span/public_coin_setup/public_seed/adapter/collection.json
```

[EXECUTED] Exit zero. This postrun collector is outside the prelaunch source
manifest and performs public-file integrity checks only. It traverses no
private path and invokes no crypto backend. Native counters are separate
from logical public-reference counts. Integer agreement is attributed to
the saved aggregate; private scores were not opened. No additional web or
Scry access occurred. No retry of the cryptographic experiment was performed.
