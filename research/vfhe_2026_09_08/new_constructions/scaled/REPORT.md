# Larger native proof comparison and complete public replay

[EXECUTED] One matched pair at ring degree 4,096 and integer matrix shape 64 × 8,192 reduced whole proving time from **711.650 ms to 459.557 ms (1.5486×)**. Both paths used the same matrix and encrypted input objects, with all three WHIR openings enabled. Their complete serialized proof files are **byte-identical**. Both passed the unchanged verifier in the producer and in fresh consumer processes before 64/64 plaintext comparisons. Source, commands and results are in `source_pins.json`, `command.json`, `paired_001/` and `RESULTS.json`.

| [EXECUTED] Phase | Original | Fusion + shared contraction |
|---|---:|---:|
| Public preprocessing | 37.926 ms | 49.703 ms |
| Whole proof generation | 711.650 ms | 459.557 ms |
| Preprocessing + proof | 749.576 ms | 509.260 ms |
| Producer verification, including fresh preprocessing | 54.094 ms | 46.326 ms |
| Fresh saved-proof verification, including preprocessing | 48.032 ms | 48.191 ms |

[DERIVED] Including the extra preprocessing cost gives 1.4719× for preprocessing plus proving. The cached matrix spectra contain 1,048,576 u64 values: 8 MiB of payload, excluding allocation and transform-plan overhead. This is one baseline-first pair on a shared Apple M2 Max, not a repeated or isolated benchmark. `RESULTS.json` retains exact nanoseconds, arithmetic and the post-run hardware observation. The comparison combines the two arithmetic changes; it does not attribute the whole-prover improvement to either alone.

[SOURCE implementation] The baseline is the byte-exact upstream prover from commit `00379074cad457367a86dde2ecee9d0f318a7e12`, copied into `src/protocol/prover_unfused.rs`. The optimized path contains the previously executed fused matrix transform/product/remainder implementation plus polynomial_gluing's exact shared `Y/Q` contraction. Its patch is `../implementation/fused-matvec.patch` followed by the arithmetic in `../../prover_performance/ring_consumer/ring-contraction.patch` (SHA256 `419193cdc4c273fa0763d8ececd4374179041e3ba6a0df0b4ae49896a2c28af1`). This package's complete `combined-matvec.patch` applies directly to the upstream commit; it also adds the public wire codec, baseline module and two example drivers. Patch replay reproduced every changed source exactly.

[DERIVED] For `S = Σ_j A_ij C_jk = L + X^D H`, the single zero-padded length-2D transform product gives `Y=L−H`, `Q=H` and `S=Y+(X^D+1)Q`. The public matrix transforms are cached, input-component transforms are shared across rows, and spectral products are accumulated before each inverse. After existing transcript commitments and challenges, the shared contraction computes `y_eval,q_eval` using the existing coefficient, component and row weights. The two honest prover totals become `z1=y_eval+(α^D+1)q_eval`, `z3=q_eval`. All round tables, commitment logic, verifier equations and PCS protocol remain the existing ones. See `../RELATION.md` for the degree-bound distinction: the honest highest quotient coefficient check does not create a new verifier-enforced degree bound.

[EXECUTED validation] Three focused public tests passed: fused `Y/Q` against the original product and long division; complete nonconstant base/extension-field wire messages round-trip exactly; shared claims against literal original sumcheck tables at four challenges, with matching challenge/terminal checks and shape/order controls. The single native command started `2026-09-08T16:31:35.523227Z`, PID 94771, finished exit 0 after 1.5535 seconds under a 600-second cap. No retry occurred. All 524,288 output ciphertext coefficients matched across paths. The source hashes were rechecked after execution. The original verifier, PCS and transcript sources are unchanged; sumcheck receives only public export/import accessors, with its proving/verifying logic unchanged.

[EXECUTED public replay] `paired_001/statement.bin` contains the complete caller-selected matrix, encrypted input and selected expected output ciphertexts. It is 8,519,793 bytes. Each complete proof is 4,433,968 bytes, including all output coefficients, both sumcheck claims and all ordered polynomial coefficients, both quotient commitments, and all three PCS opening claims and byte strings. The proof SHA256 is `b3a17a7a1660c5f2ab1befc7b8f6a08e4e68ec013414e134869bb3fd45ff578c`; the statement SHA256 is `1a9d2f8aad28cc2429d293f3c1b6ba6c53c689d2d8ec259c4a7a9fffe912e929`. `public_artifacts.json` pins every retained run artifact. Fresh `verify_saved` processes decoded canonical bounded fields/messages, checked the caller-selected output, reconstructed preprocessing and transcript from the matrix, and invoked the unchanged verifier. No key, prover state or transcript state was imported. Both child successes preceded the source-recorded public-close checkpoint at 1,514,237,625 ns; only then did the parent perform the private decrypt/compare step. The exact 64 matches are the producer's aggregate result; no private files were retained.

[SOURCE configuration] Base field is Goldilocks, `p=18446744069414584321`; proof field is its quadratic extension with nonresidue 7. Ring inner dimension is 2, RLWE rank is 1 (two components), plaintext modulus is 16, rows are 64. WHIR uses the existing security-level parameter 100, `ConjectureList`, starting log inverse rate 1, folding factors 4/4, batch size 1, dimension-derived default PoW, and compressed/deduplicated Blake3 Merkle proofs. Cargo.lock is unchanged (`d73ad84a9fdf4b34cda8a453fc287e7e33da6afa0517ee255ea4b405d8081b50`); build used Rust `1.98.0-nightly (91fe22da8 2026-06-21)`, release mode, the owned dependency cache and `--offline --locked`. This execution establishes correctness and a measured arithmetic improvement in the upstream research implementation; it is not a new protocol-security proof, an RLWE security estimate, or a timing/privacy claim.

[EXECUTED scope and sources] This successor used no additional web or Scry query, no paper download and no external source modification. The earlier primary-source search and its counts remain in `../SOURCES.json`. No eprint PDF was downloaded. No seed grinding, extraction experiment or stopped task was used. The earlier implementation and run remain frozen.

[OPEN] Native complete-proof replay is now demonstrated. Browser/WASM portability is handed to independent_review; it is not claimed here. A deployment would still need to choose and authenticate its own statement/expected output, review the inherited verifier/security assumptions, and select a suitable performance measurement policy.
