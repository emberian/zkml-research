[DERIVED, before proof] For the actual four 50-bit moduli, canonical source RNS residues satisfy the native optimized lazy-lift precondition and the native reduction leaves them unchanged. Canonical reduction before target-prime NTT is equivalent modulo that target. The actual Galois map is the exact 13-bit-reversed index substitution; its emitted permutation will be the verifier input, not a second Rust-derived schedule.

[DERIVED, composition target] For every sequence of BabyBear assignments accepted by the existing emitted paired-MAC relations, whose public inputs bind to the same prior-state lifts, public evaluation key, query, and linear addends, all eleven output tensors equal the deterministic initial ct×pt then ten rotation/add stages. The conclusion follows from the existing source theorem at each row, not a premise assuming the output arithmetic. The forward/backward NTT callbacks and public key/parser bindings remain explicit; this is not an NTT, Rust, BFV decryption, or backend-protocol refinement.

[PREDECLARED] Nonzero small modular stage witness; wrong-output falsifier; canonical-lift and bit-reversal boundary facts; arbitrary-length stage induction; exact actual stage and emitted row instantiation. Reuse the frozen MAC circuit and generic proof pipeline; no new AIR or cryptographic execution campaign.
