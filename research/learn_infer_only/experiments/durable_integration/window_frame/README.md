[OPEN closeout] Usage interruption stopped packaging: both current modules have matching successful elaborations, but axiom guards, patch, umbrella/import-boundary and independent review are unfinished. Excluded from the final combined proposal. Preserve the frozen44-pin window patch.

# Mixed window/journal history (active)

[OPEN] This tranche preserves the frozen window44 sources and adds a separate relation for interleaved window admissions and valid framed durable transactions. The admission counter will equal a projection of actual journal entries, rather than the total authority revision. Frame conditions inspect actual canonical post-write bytes and preserve selected genesis/window cells; source admission, authorization and noise remain separate premises.

[OPEN] Target witness: existing nonzero window admissions interleaved with a genuine same-cell no-op/infer-style DataIntent and unrelated-cell transaction, followed by expiry and exact retry. A valid preflight transaction that changes canonical window bytes must fail the frame check even with a constant digest. No companions, commits, queries or modifications to finished patches.
