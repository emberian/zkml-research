[OPEN] Attach emitted_runtime’s generated-relation proof for the frozen public event65 to proof_join/, preserving the existing descriptor/ciphertext bytes. Distinguish a proved ciphertext relation from encoder/FIFO/full-wrapper verification. Root owns integration and publication.

[EXECUTED ready] Continue teaching/querying via learner.py or session.py; the largest encrypted model has78 classes at models/full77_bfv. Existing learned prototype accuracy can regress with further FIFO updates; report future behavior rather than tuning against the same test. No old fixtures need rerunning.
