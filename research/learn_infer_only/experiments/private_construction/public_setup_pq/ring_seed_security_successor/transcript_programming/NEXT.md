# Resume

[DERIVED] Use `KERNEL.md` pointwise density formula and full joint-law
statement, not an independence claim between the programmed table and target.
Preserve aborts and prior-query bad events explicitly in the parent game.

[EXECUTED] The one exact finite control is complete; read `CHECKS.json` and
`RUN.log` rather than repeat a grid. Source and proof pins are in `MANIFEST.json`.

[OPEN] Parent should supply the full joint target regularity theorem and
fresh-seed/registry chronology. Any QROM extension needs its own argument;
the classical freshness bound cannot be relabeled quantum security.
