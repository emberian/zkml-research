# Next

Root: after the active quadratic workload and worker stop, apply with
`python3 research/vfhe_2026_09_08/continuing_system/integration/apply.py --apply`.
Wire the existing HTTP init fields to `score`/`proof_backend` and read
`core.metadata(root)` for display. Run the alternate whole learner on fresh
instances with signed-dot references; do not migrate the quadratic genesis or
reuse its proof phases as fresh linear work. The final matched caller/profile is
available at `research/vfhe_2026_09_08/matched_field/`.

If source hashes changed before application, rebase the two-file proposal and
review that concrete diff; do not bypass the base-pin check.
