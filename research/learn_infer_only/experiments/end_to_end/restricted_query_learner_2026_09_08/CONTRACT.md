# Fixed semantic queries with restricted ring recipient keys

[DERIVED] This join uses the frozen ideal-uniform ring transport's full profile: N16384,w64,d577,r16,p28439893,q=4294967767·2^256+1,sigmaK2^25,sigmae2^10,F2^247,W32,D8204908842 and Delta=floor(q/D). The sampler, ring multiplication and designated scalar decoder are byte-identical owned copies. The new adapter supplies a general invertible public basis and binds its full registry digest into every A/registration/key/public/ciphertext header and context check. It never reuses previous setup keys.

[EXECUTED, public preparation] The first eight saved test queries from each fixed class `card_arrival` and `cash_withdrawal_charge` form16 rows of actual cached E5/projection int8 vectors, extended by the existing final zero. Their rank over F_p is16. Canonical leftmost modular pivot columns determine an invertible16×16 minor. Appending identity rows for the561 nonpivot columns completes B in GL577(F_p): given the transformed coordinates, recover all nonpivot input coordinates directly, then solve that minor for the16 pivot coordinates. The generic mapping retains all577 field coordinates; the known semantic fixture itself has more structure and is not a state-ambiguity witness.

[DERIVED/EXECUTED] The public query maximum L1 norm is9,925. For any allowed int8 observation x, |q_i·x|≤127||q_i||_1. For a two-example class memory this gives |score|≤2,520,950 <floor(p/2)=14,219,946. Thus every intended whole-window score has an unambiguous signed representative, using the unchanged p/Delta/noise. The original deterministic cutoff correctness condition Delta>2W(F+wN(8sigmaK)(8sigmae)) remains in the unchanged Parameters constructor. This does not estimate Ring-LWE hardness.

[PREDECLARED] One normal full-profile run creates public A,16 recipient-local keys and registrations, and561 directly uniform missing public rows. No missing-row secret or universal reader is constructed. It performs six new ring encryptions of the first three cached training observations per fixed class, interleaved by class, and six public Learn updates. Each class has a window of two; its third teaching subtracts its actual first ciphertext, giving two exact expiries. The final state of each class is compared byte-for-byte with the direct sum of its two remaining ciphertexts. No encoder/model rerun, tuning, seed search, extraction attempt or retry is included.

[PREDECLARED] At revisions2,4,6 a public class map records the two actual state ciphertext hashes and active counts. After all public arithmetic, exact rebuild, source/hash/storage checks and public actor shutdown, each of16 recipient processes loads only its own key and reads its registered query's two class scores at these three checkpoints. It ranks means with exact rational comparison and deliberately publishes these known-public benchmark scores for the report. This is96 scalar comparisons and48 decisions. Predetermined plaintext references are saved now, before any ring sampling. The known reused slice happens to be16/16 at all three checkpoints; it is not a fresh utility estimate and selection will not be changed.

[SCOPE] A recipient's complete row gives its fixed query projection on every retained issued input and any permitted linear combination; a coalition gets its entire per-input registered-query span. Restriction to final class scores is not cryptographic. Class routing, query texts/vectors, active counts, lineage, benchmark observations, outputs and timings are public. Full semantic state ambiguity, natural-language input privacy against a public dictionary, hidden routing, adaptive new query issuance and full resident privacy are not claimed. Recipient sampling and setup are honest; exact scheme/hardness hypotheses, expected-time samplers, Python/FLINT/serialization and host administration remain assumptions. OS profiles deny named other-recipient paths and networks, without a sandbox-escape or host-admin claim.

[PREDECLARED] The run is sequential with a15-minute overall cap and180-second per-actor subprocess cap. No actor command spawns background workers; the copied native sampler library already exists. A failure ends the sole run, preserves partial evidence, and permits no automatic retry/resume. Sources and this contract are pinned before launch. No external acceptance gate or unrelated fault/check campaign is added.
