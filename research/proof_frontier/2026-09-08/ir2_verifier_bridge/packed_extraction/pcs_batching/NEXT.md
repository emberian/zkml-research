The parent owns the additive join to arbitrary packed acceptance. Import `Selvage.Ir2PcsBatching`; use `Pcs.reduction points claims alpha` for `Packed.Reduction`, with the same fixed extracted input rows across alpha. Keep off-domain points, source degree≤16384, canonical shape admission and fresh challenge timing explicit. The frozen sources need no further expansion or runtime replay.
