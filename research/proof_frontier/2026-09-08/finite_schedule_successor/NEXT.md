[OPEN] Connect the finite supplied scalar-path theorem to packed runtime MMCS openings, actual checkpoint/log production and the transparent terminal-polynomial check. Keep the observed Failure union unpriced until an explicit oracle/hash reduction is proved.

[OPEN] The finite schedule here uses fixed external arity eight. Variable external arities can reuse the prefix probability engine and propagation induction, but need a proved per-arity transition and corresponding coherent index map. A degree-seven zero-injection refinement could reduce the conservative degree-eight challenge term where the input is certified zero.

[EXECUTED] Main, frozen one/two-round, curve, kernel, carrier, tower and extraction sources remain unchanged. Root owns integration and any separately authorized broader validation. No ErrorBudget or runtime-equivalence claim is added.
