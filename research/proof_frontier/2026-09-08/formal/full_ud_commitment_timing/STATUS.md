[EXECUTED] FROZEN / GREEN: 5 additive modules, 728 lines, 36 theorem declarations and 36 exact guarded axiom pins. All dependency-ordered final source checks returned exit0 with empty diagnostic output. The root's actual qualified declaration census passed with no forbidden constructs. Patch clean apply, source byte equality and candidate-independent import boundary passed. Patch SHA256 `449608f151061051d3c2567d86d6b505c86ecf03906f990ffb7a5238995e4756`. `manifest.json`, `integration_entry.json`, `integration_census.json`, `source-evidence.json` and final logs retain the evidence.

[EXECUTED] Arbitrary prefix-selected malicious roots are resolved from accepted openings without an honest-image premise. Raw acceptance reduces to the existing ideal adaptive sampled verifier or a selected-root two-opening event. Transparent terminal word roots need no final Merkle commitment. A concrete F5 one-round witness has an off-image initial root and a genuinely challenge-dependent final word; raw acceptance, no bad event and ideal acceptance are all proved. Image, binding-removal and future-timing falsifiers are checked.

[EXECUTED] The existing full-UD/BabyBear coherent budget is consumed directly: raw acceptance at 3603 queries is at most `2^-55 + Pr[BadRoots]`, conditional on the actual tower and initial resolved-word farness. Exact position binding removes the added event. No new radius arithmetic or security bits are introduced. All 16 frozen predecessor dependency hashes are unchanged.

[OPEN] The retained event is semantic existence of conflicting accepted paths, not a priced efficient collision-adversary event. Canonical resolution uses Classical.choose; no PPT extraction/search/rewinding reduction is claimed. Runtime layout/domain/MMCS, initial-farness and Fiat–Shamir/PoW composition remain separate. Root owns independent review and the whole umbrella integration gate.

[EXECUTED] Companion trees and frozen predecessors were read-only. Zero web queries, zero Scry SQL and no runtime benchmark in this follow-on. No shared ledger, security label, runtime setting or commit changed.
