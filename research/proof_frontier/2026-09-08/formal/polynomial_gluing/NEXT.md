[EXECUTED] Assigned full polynomial gluing theorem completed. Resume from the checked three-file patch and exact source hashes; no mathematical sublemma remains open in this lane.

[OPEN] Root integration: apply `polynomial-gluing.patch` to the intended minidregg checkout, add umbrella imports in root's integration patch, and run the coordinated whole-tree build. This lane deliberately did not edit `Theory.lean` or `Minidregg.lean` during the swarm.

[OPEN] proof_frontier integration: import `Theory.PolynomialGluing`, which transitively supplies `Theory.PolynomialGluingStatement` and `Theory.PolynomialBivariate`. The global source names `polishchuk_spielman`, `ps_degree_x_swap`, `ps_nat_degree_y_swap`, `ps_eval_y_eq_eval_x_swap` and `ps_eval_x_eq_map` remain available. Direct helper names are under `Polynomial.Bivariate`.

[DERIVED] The all-one witness establishes consistency of all generic premises; it is not a claim about deployed decoding parameters. Concrete full-UD premise inhabitation and its adversarial audit belong to the consuming theorem.
