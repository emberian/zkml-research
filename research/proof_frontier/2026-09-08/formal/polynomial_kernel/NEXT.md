# Next

[EXECUTED] The assigned polynomial-kernel theorem, witnesses, teeth, axiom pins, and checked patch are complete. Resume from the final patch and `provenance.json`; do not re-extract ArkLib.

[OPEN integration] Apply `polynomial-kernel.patch` to the integration checkout. Add `import Theory.PolynomialMatrixKernel` to the actual consuming module. The public namespace is `PolynomialMatrixKernel`; use `correctness` with `Premises`, or the direct source-compatible theorem in `.Source`. Its field and row types are universe-polymorphic.

[OPEN integration] Root owns umbrella registration/full build and the concrete full unique-decoding caller. The parallel `full_ud` lane was sent the green module and exact signature. That continuation must reuse existing RS semantics; this module introduces none.

[OPEN integration] Preserve `LICENSES/ArkLib-Apache-2.0.txt`, the module header attribution, and the recorded source pin. No permission or additional source acquisition is needed for the remaining normal integration work.
