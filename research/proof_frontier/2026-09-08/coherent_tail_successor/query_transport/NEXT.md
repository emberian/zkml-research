# Resume

1. [OPEN] Consume the three frozen modules listed in `MANIFEST.json` through the parent `coherent_tail_successor` combined additive patch.
2. [DERIVED] The head is used as `BabyBear.consistency_query_exact s q r hterminal`; it expands to the exact `Accepts` probability and the terminal mass fraction to power `q`.
3. [OPEN] Root owns the whole-tree integration gate and soundness-head assembly. No further query-transport theorem is needed for the assigned bounded target.
