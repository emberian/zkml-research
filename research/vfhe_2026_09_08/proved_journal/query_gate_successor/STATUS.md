[EXECUTED] Completed gated001: two fresh genuine proved teaches and two new
joined class-query proofs, complete active-class gate/current-model binding,
relevant omitted-class refusal with no reader call, public acceptance before
both private receives, matching later integer scores and exact head reopen.
The runnable query command is complete. All source/runtime pins match; frozen
predecessors and historical decrypted queries remain unchanged.

