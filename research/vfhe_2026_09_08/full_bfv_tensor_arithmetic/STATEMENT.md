[DERIVED, statement before proof work] The source relation accepts five canonical11-limb radix64 words a0,a1,p0,p1,p2 for a fixed modulus q. Acceptance must force p0=a0*a0 modq, p1=2*a0*a1 modq and p2=a1*a1 modq. One pair of public canonical input words is shared across all three products. All input/output bounds are constraints in the emitted source, not external semantic premises. The same source is instantiated for each of the nine actual extended moduli; the largest is below2^62, fitting64^11. Public tuple is rowID followed by these five11-digit words, width56.

[DERIVED, implementation plan] Generalize the existing exact schoolbook multiplication source to11 limbs and a fixed public factor1 or2, with22 signed-carry columns. Native witness plan computes quotient/carry values from the two supplied inputs and copies every output digit unchanged. Generic BigInt witness execution is untrusted; the emitted source relation forces correctness. Compiler emission folds this source, with no handwritten AIR.

[PREDECLARED keystone obligations] Universal source soundness for all assignments and q with0<q<64^11; numeric BabyBear column-capacity bounds; concrete nonzero accepting witness/premise inhabitation; output-change falsifier that leaves canonical bounds admissible; exact deployed prime capacities. Axiom census/pins, import boundary and additive patch/application checks accompany final source. Whole-ciphertext capture/proving is the runtime lane's responsibility.

[SCOPE] This component proves pointwise tensor multiplication in the extended NTT basis. Public capture binding, forward/inverse NTT, basis extension and rescale are separate joins. It does not itself prove the complete BFV multiplication or decryption law. Companion trees and frozen prior packages stay read-only.
