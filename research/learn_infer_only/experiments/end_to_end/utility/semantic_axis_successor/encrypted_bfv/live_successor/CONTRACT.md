# One frozen fresh text through deferred verified BFV release

[DERIVED: fixed before tokenization, model or cryptographic execution] Execute
exactly Infer(q01), one positive-label Learn on public route 0, Infer(q01).
The source text is newly written and fixed in ignored `runtime/.private/input.json`.
q01 is the inherited public vector [127,0,...,0], fixed independently of model
output. There is one text, two semantic-axis examples in one model forward,
two separately registered query tickets, three events, and no expiry. No
answer-change or classification requirement permits retry or reselection.

[DERIVED] Preserve the unfinished sibling `live/` draft and completed parent
manifests. Check exact full-text disjointness against the five named earlier
synthetic corpora and retain their paths/counts/hashes. No training-corpus or
new-task absence claim follows. Keep text, prompts, token IDs, raw predictions,
selected-feature bindings, scalar answer fields and their hashes out of public artifacts.
All possible one-hot rows and their file hashes already occur in the public
query dictionary; their presence does not newly identify the chosen row.
Freeze the private input/rendered prompts and public source/model/query pins
before the first model or cryptographic call.

[DERIVED] Reuse the frozen SmolLM3-3B revision and cached files, framing A,
public plant-axis definitions, alternative token IDs 15/16, MPS float16 eager
attention, explicit left-padding positions, CPU float32 likelihood arithmetic,
and tie-to-bit-0. Use the unchanged scorer tensor loop with one partial batch
of two (maximum batch eight). No download, weight update, free generation,
backend fallback, prompt alternatives, tuning, or repeat after a scored result.
The scorer sees text and definitions, with no label or oracle parsed. The
trusted plaintext issuer applies the fixed supplied +1 label after scoring,
forms the unchanged 127-valued one-hot vector padded to 577, and invokes the
frozen signed BFV issuer. A computed vector cannot substitute for the model.

[DERIVED] Reuse the pinned BFV binary, journal arithmetic, authority, reader,
verified-reader service, persistent host, and byte-identical LiveRun class.
The bounded driver wiring changes the frontend, query index, and release
schedule. The verifier uses a separate socket from the authority's delivery
destination. Thus both authority publications remain transport-pending;
no receive operation is called in the public phase. `VerifiedReader.sync`
checks and stores ciphertext transitions without decrypting. Full reader key
loading by that process remains explicit and is not a public-only capability.

[DERIVED] Complete all three accepted events, all three independent verifier
syncs, full public arithmetic replay, actual durable journal/head equality,
and checks of zero received answers and zero decrypt commands. Then stop the
host, authority, baseline reader and verifying service. A separate process
recomputes all finalized transitions in an independent CAS from public inputs,
checks the actual stopped authority and verifier SQLite public tables and all
relevant stored bytes, and records a passing artifact. It reads no private
config, key, input, vector, prediction or answer. Before this check the ignored,
mode0600 verifier DB contains no answers.

[DERIVED] Only a passing public verification artifact with exact phase/source
hashes permits a separate offline private process to call the unchanged
`VerifiedReader.receive` on the two already registered and verified Infer
envelopes. Both decryptions therefore occur after public service shutdown and
replay. Authority services stay closed and their outbox remains unacknowledged;
this demonstration establishes durable verified offline receipt, not completed
authority delivery acknowledgement. Read the two durable answers only then,
and independently compare against zero and the direct integer vector/query
dot product. Baseline decryptions must remain zero, verified decryptions two.

[DERIVED] Publish consistency, counts, costs and the output-change boolean.
Because the label is +1, the contribution is 127-valued one-hot and q01 selects
its first coordinate, the after-score is either 0 or 16129. The disclosed
change boolean therefore reveals the exact selected after-score. Omitting raw
scalar fields is only an output-format boundary; no selected-score secrecy is
claimed. Retain the attempt even if the query answer does not change.

[DERIVED] This is one illustration, not an accuracy estimate or utility gate.
The semantic issuer sees plaintext and the receiver retains the full BFV key.
There are four effective bins per public route despite padding. Four spanning
exact queries expose a route aggregate. Roles share one OS account; there is
no OS isolation, no-master-read authority, encrypted 3B cognition, exactly-once
physical delivery, or end-to-end post-quantum claim.

[DERIVED] Retain source/model/query pre/post pins, public envelopes/replay,
compressed logs, costs and stopped-service records. A source review accepting
the exact freeze is required before launch, followed by coordination with the
priority TFHE worker. At most one model/BFV attempt; ten-minute public-driver
timeout and five-minute source-model subprocess timeout. On a public failure,
preserve partial artifacts and do not drain, retry or resume cryptography.
No stopped task is read or executed. Root owns ledgers and commits.
