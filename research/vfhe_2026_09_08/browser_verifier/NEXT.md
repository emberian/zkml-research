# Next

[EXECUTED] Complete: both operation and actual learner-expiry proofs are
packaged, each accepted through the frozen JS/WASM API and rejected after a
public-output mutation. Native CLI shares the same portable adapter and builds.
No extra verification cycle or cryptographic rerun is needed.

[DERIVED handoff] Root can copy `web/verifier.js`, `web/worker.js` and `web/pkg/`
preserving their relative paths, plus either/both fixture directories. Pin the
expected template SHA256 from approved application configuration; render only
the actual returned verifier result. `BROWSER_PACKAGE.json` supplies exact
paths/sizes/hashes. Root owns the microsite and browser-engine measurement.

[OPEN] Future expansion may add ciphertext-to-public-row decoding at this
consumer boundary, or a new approved relation/fixture. Neither is implied by
the present coefficient-relation check. The pre-adapter failing proof remains
debug history and must never replace a genuine fixture.
