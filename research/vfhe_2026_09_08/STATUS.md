# Wider constructive swarm — 8 September 2026

[REPORTED] A fresh autonomous goal is active at the user's explicit request. The current cycle builds larger components and joins working ones; it does not create an independent review queue. Main minidregg and breadstuffs stay read-only, with proof-system changes delivered as patches.

## Actual learner update, generated proof, independent verifier

[EXECUTED] A real encrypted text-learner expiry (`acc + fresh − old`, event 65) now has a proof of its complete stored-NTT coefficient relation: 8,192 rows and 16,384 residue equations. The Lean-generated template and Lean-produced witness reach the existing IR2 prover. The proof is 508,777 bytes, takes 1.767 seconds to generate, and passes fresh native verification. The same proof passes the real WebAssembly verifier; changing an output digit is rejected. This establishes the selected coefficient equation, with ciphertext parsing and protocol execution still implementation assumptions; it does not prove the text encoder, expiry authorization, or absence of a full reader. [Runtime](proved_operation/STATUS.md), [arithmetic theorem and patch](arithmetic_coverage/README.md), [standalone consumer](browser_verifier/README.md).

[EXECUTED] The complete `3A + 5B` BFV operation also passes native and WebAssembly verification: 24,576 residue equations and a 557,834-byte proof, generated in 3.141 seconds. The original failed proof is retained as a failure. The working shared backend fixes portable deterministic salts for public preprocessing only; witness/other PCS operations retain the existing hiding implementation. It is an explicitly adjusted configuration, not a cryptographic security upgrade. Commands and outputs are in the runtime package.

## Useful continuing learning

[EXECUTED] The reusable teach/query learner now includes a persistent 78-class encrypted model. Actual encrypted execution totals 1,364 teaches, 680 expiries and 12,501 matching integer scores. The full 77-class plaintext benchmark reaches 2,253/3,080 correct (73.15%); the separate full-class encrypted integration uses one fixed query per class. New text creates new classes and changes predictions. The encoder is plaintext, routing/queries are public, and a full BFV reader survives. [Runnable learner and report](../learn_infer_only/experiments/end_to_end/useful_learner_2026_09_08/README.md).

## Real ring constructions and proof performance

[EXECUTED] The repaired fixed-coordinate ring scheme has actual setup, encoding, signed combination, exact expiry and designated decoding. Its faster successor completes the full repaired dimensions in 117.13 seconds versus 265.74 seconds for the baseline (2.27×), with all 112 reads matching. The exact finite Gaussian law and decoder are unchanged. These are single matched runs; variable timing, fixed-span recipient authority and computational assumptions remain explicit. [Implementation](../learn_infer_only/experiments/private_construction/public_setup_pq/ring_implementation_fast/README.md).

[EXECUTED] The public-seed durable journal join completes 33 Learn, four Infer, one expiry, revision-18 reopen, exact retry and full public replay. All public processes close before four private comparisons; all match. Recipient authority remains usable outside the journal. [Report](../learn_infer_only/experiments/private_construction/designated_span/public_coin_setup/public_seed/journal/joined_service_001/REPORT.md).

[EXECUTED] Fused polynomial remainder/quotient computation now runs inside an actual ring-matrix prover with PCS enabled, accepted by its existing verifier. A shared contraction removes another repeated claim scan. The first native configuration is small and uses the upstream WHIR `ConjectureList` parameter selection; it establishes execution, not certified security or a measured whole-proof speedup. The larger paired run is a separate successor. [Construction and native report](new_constructions/REPORT.md), [shared contraction](prover_performance/ring_consumer/README.md).

[EXECUTED] The minidregg algebraic sumcheck prover takes 1.970 ms versus 7.817 ms on the saved `[2,1024]×[1024,128]` workload (3.97×), preserving the complete transcript. This is an algebraic-prover measurement, not a complete PCS/STARK speedup. [Patch and measurements](prover_performance/README.md).

## Proof and restricted-release constructions

[DERIVED/EXECUTED] Seven additive Lean modules connect actual arity-eight folding, source farness, supplied openings and the coherent BabyBear sampler for one transition. The concrete bound is `2^20/p^4 + (9/10)^q`, plus the explicit observed-log-failure residual. All 39 exact guarded axiom reports and changed-module checks pass. Multi-round composition is the next construction. [Theorems, witnesses and patch](../proof_frontier/2026-09-08/constructive_successor/README.md).

[DERIVED] The closed bounded MIFE transcript/replay construction erases anchor issuance, retains public observation issuance and one exposed Replay key, and supplies a conditional adaptive IND reduction including fresh hidden learning. Its executable is a symbolic FE model. Practical cryptography, indefinite continuation, selected-history release and PQ remain open. [Construction](../learn_infer_only/experiments/private_construction/release_successor_2026_09_08/CONSTRUCTION.md).

[EXECUTED publication] The visual essay and actual on-device learner-update verifier are live at https://emberian.github.io/dregg-microsites/keep-learning-keep-a-secret/ . Microsite commit225b1db33b5c312bd7805577b7343708414a66e4, successful Pages workflow34252316719 and HTTP200. The recorded teaching interaction and78-class model are also included. JS syntax passed; the underlying exact WASM consumer passed in Node. No browser UI execution was available in this run. The existing `tools/shoot.js` edit is preserved. [Active construction queue](NEXT.md).

## Further completed construction joins

[EXECUTED] The production update pipeline generates a fresh proof for a supplied four-ciphertext case and verifies it in a new process. Actual learner event 66 completes in 41.372 seconds versus 218.802 seconds with the original checked exporter. The template and complete 33,325,056-byte witness are identical; the 5.29× one-pair difference comes from omitting duplicate interpreter constraint checks before actual proving. [Runnable pipeline](update_pipeline/README.md).

[EXECUTED] Exact integer rescaling of all 24 positions and 72 output residues in an actual small N8 ciphertext multiplication now reaches a generated proof: 8,592,353 bytes, 1.377 seconds proving. The theorem uses the deployed 6→3 directed fixed-point correction, including nearest-plus-one behavior. This is a complete small rescale step, not secure N8 parameters or a proof of convolution/extension. [Arithmetic and patch](arithmetic_rescale/README.md), [actual proof](proved_rescale/REPORT.md).

[EXECUTED] The ring scheme now has canonical packed transport and separate setup, registration, issuer, evaluator and recipient processes. The full workflow takes 160.27 seconds across 45 fresh actor PIDs, with 20 designated outputs matching. Public processes are denied private-key paths; each recipient receives only its own key path. Actual public setup is 379,390,500 bytes and a fresh ciphertext 37,901,312 bytes. Same-host administrator access and the earlier cryptographic assumptions remain outside this process demonstration. [CLI and measured path](../learn_infer_only/experiments/private_construction/public_setup_pq/ring_transport/README.md).

[EXECUTED] The larger ring-matvec comparison proves the same degree-4096, matrix-64×8192 input in 459.557 ms versus 711.650 ms, with byte-identical 4,433,968-byte proof files and fresh native replay. Including preprocessing, the one-pair difference is 1.47×. A thin WASM consumer runs the unchanged verifier in 108 ms under Node. This is the upstream ConjectureList configuration with no certified-bit claim. [Matched native result](new_constructions/scaled/REPORT.md), [portable consumer](ring_browser_verifier/README.md).

[EXECUTED] The next ring-matvec join computes actual prototype-learner scores: all 64 signed scores and eight class decisions match cached references. One proof takes 637.339 ms and its saved proof passes fresh native verification before decryption. Two CRT residues preserve exact integer scores with the same upstream noise parameter. This uses newly encrypted inputs under a separate backend, not the existing BFV ciphertexts, and retains a full recipient reader. [Real learner-data join](new_constructions/learner_join/REPORT.md).

[EXECUTED] A durable proof-gated service reconstructs exact public rows from ciphertext payloads and admits an update only against the current parent. Its model-map successor commits actual events 65→66 under one global head, changes exactly one class each time, rejects a stale parent, and preserves its head and original receipt across reopen and retry. The initial two-class checkpoint is imported history. Genesis, parent, recipient and declared randomness policy are application bindings; no new formal refinement or restricted decryption mechanism follows. [Single-cell service](proved_journal/README.md), [multiclass successor](proved_journal/multiclass_successor/README.md).

[DERIVED/EXECUTED] Five further Lean modules prove actual BabyBear two-round supplied terminal acceptance at levels 0→3→6, with prefix-dependent second input and a single shared coherent-query tail. The bound is `(2^20 + 2^17)/p^4 + (9/10)^q + Pr[Failure]`. All 43 exact guarded reports and changed-module checks pass. This derives the bad-transition bound and does not assume a far middle word. Packed-MMCS/runtime integration and the observed-log failure price remain open. [Two-round theorem and patch](../proof_frontier/2026-09-08/multi_round_successor/README.md).

[HYPOTHESIS/DERIVED] The MHFE successor records an explicit proposed online continuation capability and a scoped issuing-oracle obstruction. A full source construction/game was unavailable in its named bounded primary-source/local-mirror search; the new design has unproved joint-security and compactness obligations. [Frontier note](../learn_infer_only/experiments/private_construction/multi_hop_successor_2026_09_08/FRONTIER.md).

[DERIVED/EXECUTED] A generic Lean assertion-sharing pass removes 5,880 duplicate assertions from the small rescale relation (45,209→39,329), with proved acceptance equivalence and unchanged witness columns. Real native proofs pass; one clean pair takes 588 ms versus 565 ms, with an earlier overlapped pair retained separately. The 13% constraint reduction is the result; no broad speedup is inferred. [Reusable compiler pass](compiler_cost_successor/README.md).
