# Independent verified-project-cache review

[DERIVED verdict, 2026-09-08] **The pinned helper passes this bounded source and synthetic review for its declared observed-cohort cache scope.** The export-path hardening correction described below is included in the accepted final version; no check was weakened. Actual consumption additionally requires a completed sealed export, admission and destination checks, and successful wrapper compilation. An unfinished export directory is not usable evidence.

[SOURCE/EXECUTED] Helper `experiments/integration/verified_project_cache.py` SHA-256 `91f31c3b80f3897d394832a79e43959e2c60c1b1206ae894a3656b25de02ac07`; synthetic tests `4b5bc97c537fabde276ccf494fd29b1737d646e94f18a61591b0b78098e0d52d`; policy `491b866c9c9184c20d544208ec904ced42a58e774bc11530d4ca9c3b6f7a258e`. Paths here are relative to `research/learn_infer_only/`. `source_pins.json` and `export_observation.json` record the final available evidence; `manifest.json` seals this review.

[EXECUTED] Independently reran all 31 final author synthetic tests, redirecting their object scratch into this review's own temporary directory. All pass. Fixtures explicitly contain non-Lean object bytes and a fake compiler. This review performed no Lean build, actual project export, cache installation, crypto, or private-file inspection. No author, companion, integration harness or shared ledger was edited. No new web search occurred.

## Source and transitive objects

[SOURCE/DERIVED] The export requires explicit expected hashes for a passed clean producer report and archived harness. It checks saved successful source/command/object records, empty inherited-project-artifact list, complete topological project compile coverage, source hashes before/after each compile, and exact object hashes. Its reports and command logs are evidence under the trusted producer/harness assumption, not self-authenticating proof of execution. Synthetic fixtures intentionally demonstrate that distinction.

[SOURCE/DERIVED] Each input key binds source bytes, ordered imports, implicit Init, resolutions, cohort/policy and each project dependency's recursive key and object hash. The parent export rechecks every copied source/object/evidence byte, recomputes the graph and input keys, and rejects object tampering. Admission compares current source/import/dependency/resolution records and propagates rebuilds through reverse dependencies. Any external/toolchain/policy difference rebuilds all nodes. Four umbrellas always rebuild. A changed dependency output cannot be accepted merely because its source remained unchanged.

[EXECUTED] An independent header-only audit of run018's 567 closure modules found every actual header consists of the plain single-name import syntax understood by the helper. The helper inventories 596 source files overall. This supports the current producer's lexical dependency extraction; it is not a general equivalence proof between this parser and every Lean syntax extension. Unsupported source syntax must remain an error or require a reviewed parser successor.

## Cohort and namespace resolution

[SOURCE/DERIVED] The entire explicit external/toolchain cohort is bound by file bytes and directory membership, including external artifact sidecars and resolved file-link targets. An absent external root is represented with `present=false`; later appearance, even empty, changes the cohort and invalidates reuse. Directory symlinks and paths overlapping the explicit forbidden project cache are rejected. File symlinks may only reach explicit public cohort roots and cannot resolve into the forbidden cache.

[SOURCE] Independently inspected Lean 4.30's local `Lean/Util/Path.lean:60`: `findWithExt` selects the first search root containing the namespace directory or root `.olean`, then forms the requested module path. It does not continue to later roots when that first namespace lacks a submodule. `initSearchPath` adds the builtin toolchain path after the supplied environment search path. The explicit current policy already includes that same toolchain library root last.

[DERIVED] The helper models that first-namespace-root rule, including implicit Init, and rejects partial namespace shadowing rather than falling through. Planned project namespaces cannot silently borrow missing modules from external roots. Its output `LEAN_PATH` consists only of the isolated overlay and explicit external roots; the policy rejects overlap with `/Users/ember/dev/minidregg/.lake/build/lib/lean`. No main-project cache fallback or ambient search-path import is performed by the helper.

[SOURCE/DERIVED] Project exports and staged overlays require the exact set of expected main `.olean` files and directories. Unknown `.ir`, `.olean.server`, `.olean.private`, other files, and even unexpected empty namespace directories fail checks. Final output verification also rejects changed copied bytes. The external cohort's complete artifact inventory handles external sidecars separately. These are conservative v1 boundaries; they must not be relaxed to accommodate an unexpected file during integration.

## Plan integrity and wrapper duties

[SOURCE/DERIVED] Plans are read-only admission results. Their digest is checked, and rechecking recomputes decisions, copy/rebuild records, order and search path from the verified parent/current inputs. Rehashing an edited plan cannot authorize an altered copy source. The staged check verifies exact destination membership/bytes; final checking rechecks inputs and copied objects. Newly built hashes are observations and do not themselves establish successful Lean execution. Cached runs cannot become clean seeds.

[DERIVED required use] The wrapper must independently pin the helper, parent manifest, policy and plan; enforce the actual compiler argv, source cwd, environment and returned `LEAN_PATH`; copy only planned objects; run `verify_staged_copies`; compile ordered rebuilds with normal source, axiom, patch and boundary gates; and finish with `verify_final_outputs` and retained successful command logs. The helper does not launch a process or enforce the consuming process's environment. A helper source hash recorded inside a manifest is not a substitute for the wrapper pinning the helper it actually imports.

[SOURCE/DERIVED] The export deliberately declares a **post-success observed trusted external cohort**, with no historical external pre/post or ambient-environment attestation. This review preserves that limit. Shared-OS trust, compiler/metaprogram behavior and sources' possible nonhermetic behavior remain outside byte-cache equivalence. Pre/post hashes are not protection against an adversarial host changing and restoring inputs during execution. The proposed use is a verified-cache integration, not a second clean full project-source build or a demonstrated speedup.

[EXECUTED] Reproduction: `python3 -B research/learn_infer_only/experiments/adversarial_review/verified_project_cache_review/run_checks.py`, with retained stdout/stderr and `results.json`. Thirty-one tests pass, covering unchanged reuse, reverse-dependency invalidation, new modules, cohort changes, absent-root appearance, source/object/log/manifest tampering, namespace shadowing, symlinks, sidecars, unexpected directories, plan edits and final copied-byte changes. These are meaningful file-protocol controls; they do not replace wrapper review or a real consuming run.


## Final correction and observed export

[DERIVED/EXECUTED] Before the final verdict, the author identified that `_fresh` rejected symlinks but did not require canonical spelling; a lexical `..` could bypass the export storage-prefix check before later validation. The sole helper change adds `require(path.resolve() == path, ...)` before any export mutation. The new traversal test and all 30 existing tests pass independently. The actual export used canonical paths, so this bug did not affect its output. The original helper/test/result bytes are preserved under the author’s `pre_hardening_001/`; the final accepted helper is the successor pinned above.

[EXECUTED] The actual observed export completed with exit zero and manifest SHA `3c8943f716651ed9db18176f41d7197a6d919c364dda9b8c30520f42a015439f`. Independent read-only `_verify_export` succeeds, rechecking its 588 evidence files, 1,134 project source/object files, source graph, resolution bindings and recursive keys for 567 modules. Its recorded cohort SHA is `f0118789533ab124a0366b078f903a4e944cc682a502c816d18786c5b1058e8a`. The exporter honestly records its earlier helper `86c3c330...` before and after execution; the later one-line hardening does not alter `_verify_export` or admission semantics. This review did not rescan the large current external cohort; real admission and the wrapper’s rechecks must do so.

[REPORTED/OPEN] Root elected not to run a cached integration before the deadline. Its prepared wrapper remains unexecuted and pending full wrapper review and first-run validation. This helper/export verdict does not accept that wrapper or any cache consumption. No further cohort scan is claimed.
