# Audit continuation

[DERIVED] Root can collect this bounded report without changing VERDICTS. The
old four-pair result stays scoped to those four cases. The later run001 remains
a failed full-byte replay workload, with no completed private decode or utility
result. Do not resume it or retrospectively substitute a fixed-plan build.

[OPEN] A new authorized successor can record/check its chosen cached plan,
actual polynomial size, compiled features and execution environment. The
existing fixed-Dif4 feature removes timing selection as one variable. It does
not itself supply a universal byte-determinism or plaintext-correctness theorem.
The emitted-runtime lane has the exact source pointers and observation route.

[EXECUTED reproducibility] `audit.py` has fixed owner-approved inputs and reads
public bytes/library code only. Its final saved command and result are recorded
in `COMMANDS.md`, `audit_004.stdout`, `result.json` and `source_manifest.json`.
Rerunning it requires the original public ciphertexts and local pinned crate
archives, but no private material and no cryptographic execution.
