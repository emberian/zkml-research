# Active construction cycle

[REPORTED] The user cleared the previous goal and explicitly authorized a replacement on 8 September 2026. The new autonomous goal is active from 22:29 UTC, with no new deadline or token budget. Keep broad builders moving toward larger constructions and converge in occasional batches. Completed demonstrations need no repeat run merely for integration.

- **Complete actual BFV multiplication:** `basis_extension_successor/`, `full_bfv_tensor_arithmetic/` and `full_bfv_multiply_successor/` join the deployed four-to-nine extension, all nine-prime pointwise tensor products, public transforms, and the already-proved entire rescale. Preserve deployed shift127 Garner rounding. Reuse and freshly verify the six existing rescale proofs against the identical captured operation; do not reprove them. Linear transforms and serialization remain explicit verifier implementation assumptions.
- **Exact TFHE external product and full PBS:** `bootstrap_external_product_successor/` joins all 2,048 output coefficients to the actual signed digit input and raw GGSW row. Its selected sparse digit uses an anchored recurrence over ZMod(2^24), followed by multiplication by256 for the torus output. Extend native execution with existing exact integer PBS routines. The sparse first-step proof does not establish later dense products or FFT equivalence.
- **Stronger usable proof soundness:** the proof-frontier builder develops weighted consistency through the actual five-round tower, keeping the same farness radius. Its independent query transport builder connects the existing coherent sampler to terminal weighted mass. The target tail is (4/5)^q at radius1/5, with existing challenge and commitment terms explicit; the complete joined theorem is not yet claimed.
- **Seeded restricted-key continuation:** `proved_journal/seeded_ring_successor/` and `../learn_infer_only/experiments/private_construction/public_setup_pq/ring_seed_semantic_successor/` are finishing the single completed dual384-bit-seed service run and fixed-basis conditional QROM application. Do not create another setup to package these results. Keys still read their fixed projections outside the journal.
- **Proof-bearing restricted-ring updates:** `restricted_ring_arithmetic/` builds a complete 289-bit modular window-update relation and native witness plan for actual public ciphertexts from that seeded run. Eleven radix-2^27 limbs bind whole residues within a 45-field public row. All 1,049,153 residues must be covered by explicitly indexed bounded chunks; a subset is not a complete update.
- **Useful nonlinear restricted features:** `../learn_infer_only/experiments/end_to_end/restricted_tensorsketch_successor/` builds a source-grounded degree-two feature map from original semantic vectors. Freeze its map before a paired cached utility evaluation; retain the earlier compact-kernel failure. Approximation, quantization and fixed-span exposure remain explicit.
- **Root convergence:** publish completed native emitters, whole rescale, commitment accounting, conditional QROM theorem and real query verifier in one batch. The microsite has a prepared consumer for both actual fast001 class proofs. Its Node WASM execution is recorded; no browser UI execution is claimed. Integrate new minidregg patches when a substantive group is complete, checking new glue without rerunning every predecessor.

[SOURCE scope] Main minidregg/breadstuffs remain read-only; deliver isolated patches. Frozen predecessors and stopped signer_route/verified_route paths remain untouched. Current authority remains docs/VERDICTS.md, which this run does not edit. Named unsigned checkpoint commits use a per-command signing override because the configured 1Password signer failed; Git configuration is unchanged.
