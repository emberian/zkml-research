[SOURCE] No web or Scry searches were used. The public inputs are frozen `../restricted_query_learner_2026_09_08/{registry.json,fixture.json}` and `../useful_learner_2026_09_08/{features.npz,data/selected.json}`. SELECTION.json records their exact hashes and the fixed withheld row. No private paths are inputs.

[SOURCE] The admitted domain is the existing restricted learner's `transport.py` encode check: canonical577 F_p coordinates, centered absolute value at most127, final coordinatezero. This is broader than the actual E5 encoder image, a distinction retained in every claim here.

[EXECUTED] FLINT integer LLL produced a candidate from [I64 | 2^32 Y_first64^T]. Its correctness is not trusted by the result: verify.py recomputes exact integer Ydelta and all histories from public values. CANDIDATES.json preserves the64 candidate summaries; CONSTRUCTION.json and construct.log record the one construction and its timing.

[DERIVED] Preservation is ordinary distributivity/induction for common additive schedules. The existing BFV arithmetic/scaler patches do not formalize this feature-history statement and are not cited as its proof.
