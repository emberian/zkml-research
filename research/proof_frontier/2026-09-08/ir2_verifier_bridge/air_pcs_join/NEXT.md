# Next

1. Instantiate the selected-column head with the actual native main and lookup equations and eight-coset quotient expression. Derive source rows from selected polynomial evaluations on H; do not claim every committed LDE position equals that polynomial. Prove field descent where the source requires BabyBear-valued rows.
2. Consume the independent native range/LogUp bridge for the repaired compact learner; combine polynomial constraint and lookup extraction with the already derived integer MAC/Infer result.
3. Price or preserve the on-domain opening exception, shaped hash extraction and Fiat–Shamir/PoW/native correspondence honestly. Do not relabel the fresh-challenge probability as deployed security bits.

The recovered whole-domain delivery, first five-module polynomial/OOD bridge and three-module selected-column extension are frozen. Do not repeat their native terminal control, old demo proofs or broad builds to resume this work.
