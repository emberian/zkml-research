# Resume

[DERIVED] Parent theorem should reference `SIMULATION.md`: one-query phase
wrapper, k=2Q+c finite simulation, and separate information-theoretic family
helpers. Count any new honest fallback reads in c and keep prefix lengths
within the declared L. Initial advice must be independent of fresh function
coins; earlier oracle preprocessing belongs inside Q.

[EXECUTED] Source extracts, symbolic checks and arithmetic are complete.
No additional searches or repeated simulation runs are needed for this
bounded subtask. Read `MANIFEST.json` for artifact and source hashes.

[OPEN] Keep the public field-modulus advice and its deterministic validation
inside the permitted hardness/advice model. Charge polynomial gate overhead;
do not transfer the adversary's original concrete resource budget unchanged.
