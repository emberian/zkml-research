# Construction and exact runtime boundary

[SOURCE: construction, not abstract] Amit Deo and Louis Tremblay Thibault, *Verifiable Bootstrapping from Lattice-based Folding*, local eprint2026/1127. The PDF is `/Users/ember/dev/gh/forks/IACR-eprint-mirror/2026/1127.pdf`; its SHA256 and the `pdftotext -layout` extract are pinned. No PDF was downloaded. This lane used zero web, Kagi or Scry searches. Local instruments were `rg`, `sed`, `pdftotext`, source reads and hashes.

[SOURCE] §2.4.2, printed p7, separates modulus switching, blind rotation, sample extraction and key switching. Modulus switching rounds every LWE mask/body coefficient to modulus `2d`. §5.2, printed p21 (local extract lines1403–1451), writes a blind-rotation step as `cout = BR*cDec + cin`, with `cDec` a bounded decomposition of `(X^a-1)*cin`; the realistic rounded decomposition includes a bounded error polynomial. These digit and error bounds are essential arithmetic obligations.

[SOURCE] §5.1, printed pp20–21, chooses the prime modulus of the LatticeFold Frog ring near `2^64`, dimension16 for that ring, and TFHE ring dimension1024/module rank2, decomposition base`2^32`, one level and LWE dimension694. §5.2, printed pp22–23, explains why naive bit constraints exceed`2^27` after its ring switching and instead embeds range checks into the folding relation. This package does not implement that modified range-check protocol or inherit its claimed parameter security.

[DERIVED] The useful construction lesson here is to preserve the actual rounded integer operation and its digit ranges before claiming a blind-rotation proof. The runtime below has modulus`2^32`, not that paper's prime modulus. Its 2-adic wrap and its floating FFT execution require their own exact connections. The emitted modulus-switch relation is a concrete first component at this boundary, not a runtime instantiation of the paper's prime-ring folding construction.

[SOURCE: implementation] Read-only TFHE sources are under `/Users/ember/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/tfhe-1.6.3/src/`:

- `boolean/parameters/params.rs:10–26`: the actual default preset, N512/u32, dimension805, GLWE dimension3, base-log10/levels2, Small encryption key.
- `boolean/engine/mod.rs:558–594`: encrypted `AND` forms the affine LWE precursor using `lwe_ciphertext_add` then `lwe_ciphertext_plaintext_add_assign(PLAINTEXT_FALSE)`, then applies the configured bootstrapping pattern.
- `boolean/engine/bootstrapping.rs:39–75`: the accumulator mask is zero and its entire body is `PLAINTEXT_TRUE`; `:488–528` performs PBS then key switch for `BootstrapKeyswitch`. The native capture asserts this actual order; parameter prose elsewhere is not used to infer it.
- `core_crypto/algorithms/modulus_switch.rs:17–27`: existing lazy native modulus-switch wrapper. `core_crypto/fft_impl/common.rs:10–23`: exact scalar rounding offset, wrapping addition, and shift.
- `core_crypto/fft_impl/fft64/crypto/bootstrap.rs:294–357`: actual blind-rotation initialization divides every LUT polynomial by `X^body`; each nonzero mask exponent then computes `(X^a-1)*ct0` and calls the external product. `:512–514` constructs the modulus-switched input used by that function.
- `core_crypto/fft_impl/fft64/crypto/ggsw.rs:483–599`: existing external product constructs `SignedDecomposer`, feeds its states into `TensorSignedDecompositionLendingIter`, converts signed digit polynomials with `forward_as_integer`, performs complex products, and converts the accumulated inverse result back to the torus.
- `core_crypto/fft_impl/fft64/math/fft/mod.rs:201–329,386–495`: integer/torus conversion and forward/backward APIs on that actual FFT path. Its fixed algorithm feature is retained in the new native crate.

[SOURCE: project runtime] The completed `research/learn_infer_only/experiments/end_to_end/private_ema/emitted_fixed_fft/Cargo.toml` pins TFHE1.6.3 with `boolean` and `experimental-force_fft_algo_dif4`. Its `src/ciphertext.rs` implements the generic backend through `ServerKey::xor/and`; `src/main.rs` validates the emitted schedule before reading public keys/ciphertexts. This successor uses the same AND API and feature, with a fresh minimal fixture. It does not replay the long workload or read its private key. The successor's own Cargo lockfile is recorded separately; matching the TFHE version is not claimed to mean byte-identical old binaries.

[OPEN: precise next implementation task] Bind the native initial rotation to the proved body exponent, then generate a relation for the actual signed base1024/two-level decomposition of a captured `(X^a-1)*ct0`. For a complete existing-backend blind-rotation proof, the Fourier bootstrapping key, actual FFT arithmetic/conversion path and inter-step ciphertexts must also be linked. This package contains neither such a generated FFT relation nor a trace exporter for those internals. There is no claim that the paper or all implementations are obstructed; this is the named missing component in this exact runtime path.

[OPEN: formal transport] The kernel proves the source relation and its existing simplification equivalence. JSON serialization, Rust parsing, public-table implementation, source-to-binary correspondence, cryptographic protocol security, and TFHE noise correctness remain external boundaries. The operational proof result exercises these implementations; it is not a kernel theorem about them.
