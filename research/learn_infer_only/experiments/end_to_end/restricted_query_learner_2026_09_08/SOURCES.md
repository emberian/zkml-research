[SOURCE] No web or Scry query was needed for this integration. Existing notes and source are reused read-only.

- `../../private_construction/public_setup_pq/ring_candidate/CANDIDATE.md`, §2: the conditional construction already permits any fixed full-row-rank query matrix Y completed to an invertible B; it defines independent recipient rows, directly uniform missing public rows, scalar flooding, designated decoding and whole per-input coalition projection scope. Its old parameter illustrations do not replace this run's exact full-profile parameters.
- `../../private_construction/public_setup_pq/ring_implementation_fast/{ring.py,run.py}`: exact deployed ring arithmetic, sampler interfaces and repaired `candidate_full` parameters. The owned byte-identical copies are `source/fast/`; source/origin/library hashes are in SOURCE_PINS.json.
- `../../private_construction/public_setup_pq/ring_transport/{transport.py,workflow.py,codec/packed.py}`: canonical packed transport and actor role separation. `source/transport.original.py` preserves the predecessor; `transport.patch` shows the owned general-basis and registered-query changes.
- `../useful_learner_2026_09_08/{config.json,encoder.py,features.npz,data/selected.json}`: fixed encoder/projection and known public vectors/texts. Registry source hashes bind the exact reused bytes. No old reader keys, ciphertexts or private outputs are read.
- `basis.py`: public pivot completion, exact inverse minor, projection and application range checks. `prepare.py` selects predetermined rows and computes cached integer references with no model/crypto execution.

[DERIVED] The inverse construction is explicit: identity completion recovers nonpivot coordinates; subtract their contributions from the first16 outputs and invert the pivot minor. This is why the general B is bijective over F_p. The fixed query L1 bound supplies signed application correctness independently of natural-language properties of future int8 observations.
