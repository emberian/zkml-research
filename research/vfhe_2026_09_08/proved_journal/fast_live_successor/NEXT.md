# Next

[SOURCE] The local reusable CLI is ready for a new explicitly selected instance;
no additional validation or baseline run is needed to retain this result.
Preserve completed `fast001` and predecessor configurations.

[OPEN] Future interaction can reuse a persistent `QueryGate` object to retain
the encoder between authorized teaches and queries. Wider learners, expiry
workloads or concurrent deployment require their own concrete task and must
retain proof acceptance before private receive. This two-class result does not
establish their performance or semantics.

[SOURCE] Native witness generation does not narrow the surviving BFV reader.
Do not merge its scope with the separate restricted-ring key construction.
