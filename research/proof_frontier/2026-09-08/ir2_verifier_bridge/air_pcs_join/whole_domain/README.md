# Whole-domain compiler emission and actual query rows

[DERIVED] The generic `Ir2WholeRow.gateJson` folds the existing `AirSig` source into the native `window_gate` grammar with `on_transition=false`. This replaces the query emitter's transition-only assertion wrapper; it does not author a second arithmetic relation. `scope_contract`, `selected_iff`, and `all_rows` state exactly where source assertions apply. The serializer/native parser correspondence remains a source and conformance boundary.

[DERIVED] `BfvQueryRow.WholeRows.all_rows_sound` applies the existing emitted query theorem to **every covered row**, including the physical last row. `public_sound_of_perm` then carries its two-prime modular multiply/subtract result to an exact-public tuple list through multiset coverage, without assuming physical row order. Public multiset coverage is an explicit premise here: this module does not prove native LogUp soundness.

[DERIVED] The actual-layout terminal witness has zero inputs and output digit one at column 43. The existing source theorem refuses it, the old terminal selector vacuously admits it, and the whole-domain wrapper refuses it. The positive scope witness includes a nonzero value on a last row. The emitter's four actual arithmetic examples, including nonzero and modular-boundary cases, execute separately.

[EXECUTED] Existing completed checks were recovered unchanged from their pinned isolates, rather than rerun. The two source modules have 15 exact guarded axiom prints, containing only `propext`, `Classical.choice`, and `Quot.sound`; the completed application check retained the dependency source/object hashes and passed the import boundary. The emitter ran successfully and its generated template is byte-identical to the separately strict migration of the old template: all 2,744 arithmetic bodies and the public lookup are preserved. See [MANIFEST.json](MANIFEST.json), [theorem/CHECKS.json](theorem/CHECKS.json), and [checks/template-equality.json](checks/template-equality.json).

[EXECUTED] The separate [native terminal-row control](../native_terminal_control/README.md) already produced and verified an old-backend proof of one malformed last row, and checked that the generated repair fires on that row. Its completed evidence is sufficient; packaging this deliverable performed no native proof run.

[EXECUTED] [whole-domain-query.patch](whole-domain-query.patch) replaces `EmitBfvQuery.lean` and adds the two compiler modules. Apply it after the cumulative `minidregg-complete-infer.patch` on the pinned companion base. Its exact predecessor emitter was used for `git apply --check`, exit zero. Public source copies are under `proposal/`. The companion tree itself was not edited. There is no umbrella edit or broad rebuild in this additive research proposal.

[OPEN] No claim here says arbitrary native acceptance implies all rows are correct. The polynomial batching/OOD, PCS, lookup, transcript and native-code refinement obligations belong to the subsequent application bridge. Earlier query proofs retain their demonstrated terminal-row gap; replacing a template does not repair those existing proofs retroactively.
