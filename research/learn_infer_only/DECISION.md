# Decision: advance the finite private construction and join the encrypted learner to integrity

[DERIVED decision, 2026-09-06 closeout] The strongest new confidentiality candidate is a reverse-setup, independent-key randomized-FE ladder. The strongest executed numerical path is a BFV rolling-window learner on frozen useful model features. The strongest integrity path binds public exact arithmetic, context and materialized state to a journal. Continue trying to join them. Their conjunction is not yet a private resident: authenticated unknown inputs, restricted release, complete mediation and PQ composition remain open.

## A protected continuation with an explicit lifetime

[SOURCE/DERIVED] The fixed H2 ladder uses the computational-output IND_pre game of 2013/729. Three independent instances are set up backwards; each issued transition function embeds only the next public key, which exists before its own setup. The deployed package includes all three public keys and all seven restricted function keys. The compatibility proof retains the entire future package and uses joint ciphertext hybrids under the same future key. Current-layer MPK/MSK are independently sampled after the functions/auxiliary state, as required by Appendix C footnote 8. Honest erasure covers both raw PKE secrets, PRF originals, unobfuscated key programs and initializer state/coins. Exposed obfuscated programs still contain sensitive constants internally; only the assumed FE theorem justifies their restricted exposed capability. See [FINITE_LADDER.md](experiments/private_construction/FINITE_LADDER.md).

[DERIVED scope] This is a classical, selective-state, fixed-horizon IND-style construction direction, not a practical encryption implementation or an ideal black-box compiler. It accepts public commands, permits copying and every authorized branch, and has no finality gate. Infer consumes a layer except for terminal read. It does not combine an unknown new observation ciphertext with current state. Growing horizon and QPT composition have separate saved audits and explicit unfinished review items in NEXT.md.

[EXECUTED] The symbolic witness checks 256 states and 2,304 complete paths, with 14 behavioral classes and noncommuting updates. Parent independently enumerated 15 adaptive two-step policies,3,840 transcripts and 32,640 distinct-state pairs; their observation equivalence exactly matches the stated relation. States 0 and 1 remain equivalent at H2 but first separate at H7 in this toy. The budget therefore matters to the functionality, not merely to a proof's runtime. These are finite interface facts; opaque handles are not cryptography. See [parent review](experiments/recovery_policy/finite_ladder_review.json).

[SOURCE/DERIVED] The related routes retain narrower results. DDH inner-product FE and modular-LWE public additions support restricted continuations after erasure, but their projection key also reads fresh inputs directly. The static fully attribute-hiding predicate source is a conditional one-shot positive; the inspected update interface does not preserve its coupled ciphertext construction. The same-key randomized-FE loop fails the newer source's quantitative compatibility test through a tiny but too-large fixed-coin event; that is an inadmissible theorem application, not an attack on the cryptosystem. See [PRIVATE_CONSTRUCTION.md](PRIVATE_CONSTRUCTION.md).

## Useful encrypted arithmetic now has a shared fixture

[EXECUTED] The preregistered representation study improves the routed LMS learner from last-token 53.74% to mean-after-block 10's64.53%, a paired gain of 10.79±4.18 percentage points. Selected routed window accuracy is 63.16%; selected retrieval is 60.52%, with the LMS-versus-retrieval difference uncertain. Simple cue rules improve; XOR/XNOR remain near chance. Routing contributes architectural separation between skills. These are local synthetic held-out results, not broad semantic adaptation. The earlier disjoint-text failure and privileged structured 100% control remain recorded. See [ADAPTATION_UTILITY.md](ADAPTATION_UTILITY.md).

[EXECUTED] The actual Q83/N4096 BFV replay reuses that frozen window learner for two preregistered histories, with 32 retained observations per skill. It executes 384 Learn additions,256 expiries of the original ciphertext objects and 80 encrypted public-query readouts;16 known empty-route zeros are counted separately. All 96 scores match the original integer reference. Ten nonempty checkpoints match every decrypted coefficient and the exact retained ciphertext sum. Peak stable ciphertext serialization is 5,611,452 bytes. On the shared M2 Max, one run's median host Learn is 0.013458 ms and public query encode-plus-host work 0.208917 ms. These timings exclude proving and restricted release. The harness retains the full key and reads whole polynomials. See [encrypted-window report](experiments/adaptation_utility/encrypted_window/README.md).

[EXECUTED/DERIVED] Separate HE results include four real TFHE signed-byte EMA steps and two 1,024-step Q83/W128 BFV histories. Subtracting the identical expired ciphertext cancels its original error; re-encrypting its plaintext does not. The 62-pin conditional noise/phase proof instantiates Q83/W128/r577 with nonzero witnesses and falsifiers. It composes through an explicit additive phase map; Rust/RNS/packing refinement remains open. The pinned estimator supplies model-specific generic-LWE attack estimates, including quantum core-SVP cost models, with exact source/call census. Those estimates are not lower bounds or a PQ security proof. See [HE_CLOSURE_COSTS.md](HE_CLOSURE_COSTS.md).

## Integrity closes several real seams and exposes the next ones

[EXECUTED] The materialized journal (45 pins) eliminates the first witness's empty-write gap and derives packets from installed data, fixing an independently found recipient-relabeling API error. Canonical EMA (48 pins) forces exact signed-byte floor semantics at installation from a satisfying 153-wire descriptor and opened pre/post bytes, even with a constant root. The generic contextual gate/EMA (42 pins) gives the actual adaptive, query-complete classical-ROM price (t+9)*161/p^6 for p=2013265921 and exact false-Step event at delta<1/153. Its fixed acknowledgement does not hide the public full word. Stage0 retains its separate (t+14)*4160/p^6 bound. The later29-pin two-phase proof supplies ((t0+14)*4160+(t1+9)*161)/p^6 from one shared tagged cache; it allows EMA to depend on the complete preceding Stage0 transcript. Arbitrary interleaving remains separate.

[EXECUTED] Ciphertext-window (44 pins) proves bounded current queue sums, exact-original eviction, installed canonical bytes, journal admission provenance and retry for every admitted finite history. Its present ExecutedHistory is an exclusive window-admission history: another journaled transaction requires frame closure or a projected admission counter. The actual serializer fails on its empty sentinel and distinguishes seeded/expanded representations of identical polynomials. A public exact-zero normalization passes captured totality/equality/round-trip controls; a universal lawful Rust codec is still unproved. The repaired separate-role persistent journal run005 and independent review pass actual process crashes, retries and races; repeated publication attempts are logically deduplicated. See [DURABLE_INTEGRATION.md](DURABLE_INTEGRATION.md).

[DERIVED cost boundary] A bounded current queue does not imply bounded retained history. The modeled durable journal stores a full post-state ReplayEnvelope for each admission and has no compaction theorem. Its serialized history can grow as O(T*W*ciphertext_size), while the current ciphertext queue and noise depend on W. The standalone BFV timings and memory counts exclude that durable journal.

[EXECUTED] Exact BFV source arithmetic now reaches an emitted checker. The 23-pin source certificate forces canonical six-limb inputs, deterministic Garner correction and output, rather than accepting a loose nearest-or-nearest+1 envelope. A22-pin target sidecar binds the same output integer to all three canonical target residues. Captured Rust output 172481 passes;172480, disconnected shared-Y claims and coherent noncanonical target limbs are refused. Source plus target costs 161,027 gates per scalar. Generic zero/constant simplification has 27 checked theorems and reduces the earlier integer descriptor to 15,208 gates. Source operand/NTT provenance, literal Rust arithmetic, protocol public-input binding and universal emitted-layout completeness remain explicit. See [BFV source certificate](formal/bfv_lift_refinement/source_certificate/REPORT.md) and [target projection](formal/bfv_lift_refinement/target_projection/REPORT.md).

[EXECUTED] Policy migration has an axiom-free adaptive trace theorem. The 14-pin distributional extension supplies a nonvacuous one-read privacy example even though individual states are distinguishable: a hidden fair seed makes the one-read output laws equal through public affine updates. A second read recovers the secret; public or biased seed breaks the premise. Pointwise public-policy equality does not cover coins correlated with the hidden seed. See [RECOVERY_POLICY.md](RECOVERY_POLICY.md).

## What still decides the resident

[DERIVED] The release mechanism must expose only the intended computational capability. A normal service holding the BFV/TFHE secret remains a full reader. A public receipt can force a designated transition while disclosing its entire witness. Independent continuity can regulate one published history without possessing a decryption key, but cannot by itself remove locally usable read capabilities or copied branches. Unknown inputs and private entropy need actual issuance and composition rules.

[SOURCE/DERIVED] The PQ audit found a conditional standard-model iO theorem under PQ subexponential LWE **and** average-case iO; it also found a correctness-interface mismatch and unproved downstream quantum games. The newer weak-extraction route repeatedly uses classical auxiliary state. The older rFE proof offers a more promising straight-line audit target, but its QPT lift is not yet established here. Current receipt theorems use uniform-field classical ROM, with concrete hash sampling and QROM separate. See [PQ_COMPOSITION.md](PQ_COMPOSITION.md).

[DERIVED proposed maintainer updates; VERDICTS unchanged] Mark the public materialized/EMA/window integrity successors and actual useful-feature BFV replay as completed evidence with these boundaries. Add the fixed independent-key ladder as a conditional bounded privacy construction. Retain private release, fresh ingress, physical continuity and PQ composition as independent open obligations. Retain the prior corrections: a fixed Step can accept observations; lack of function hiding does not prove total function recovery; general-circuit PCE/iO implications do not classify every restricted fixed Step.

[EXECUTED verification] The combined49-module/ 651-pin proposal passes all four umbrellas, patch application/content comparison and import-boundary instruments against existing dependency oleans; no clean whole-tree rebuild is claimed. The final proposal also includes mixed ROM29, modular words 38, horizon arithmetic 40 and source phase 45. Unfinished successors are named in NEXT.md. Lane reports preserve exact hashes, source access, witnesses, refusal controls and failed attempts. STATUS.md and NEXT.md record the deadline/usage interruption and exact resume points.

[DERIVED reviewed lifetime extension] The classical uniform reduction now uses a fixed-time power-of-two padded selector: exact gap Delta/M with 2N<=M<4N. For a uniform PPT host and uniform pre-setup sampler of states/Step, polynomial N suffices; fixed branching B>1 gives H=O(log lambda), and B=1 allows polynomial H. Independent source/finite-channel review supports this scoped result. Finite arithmetic is additionally kernel-checked, while FE/PPT reduction code itself is not formalized. The final rank-size wording includes log(H+1) for B=1; that is a parent collection correction to the reviewed prose.

[EXECUTED source arithmetic completion] Modular-word38 connects the accepted target checker to the complete handwritten Shoup/Barrett/correction/accumulation model, adding no constraints. Captured live API and full-row controls pass. This closes that model equation, while preserving the Rust language/compiler/array and operand/NTT provenance boundaries.

[EXECUTED closure] The usage limit interrupted the swarm before 10am; replenishment arrived after the cutoff. Final collection checked saved proofs and preserved unfinished drafts without launching new research lanes. No complete private resident or PQ construction is claimed.
