# Actual semantic-fixture BFV and independently verifying receiver join

[HYPOTHESIS] The frozen semantic fixture can execute through the existing BFV
add/expire core, persistent keyless host, durable authority and independently
verifying full-key receiver without changing its96 exact scalar results.
This is an authorized normal positive-flow integration run, not a new utility
sample, security attack or cryptographic construction. Budget at most90 minutes.

## Exact fixture and immutable core

[DERIVED: protocol] Reuse `../materialized/fixture.json`, SHA-256
`bcdda47d227853e546427e9fb42de489b7c227ace65b10a9099cec81f4481b11`,
with histories67000/67001 and the frozen16 query record IDs. All duplicate
model-predicted rows remain in the query policy and event chronology. Counts:
384 fresh signed Learn contributions,96 Infer results,256 same-contribution
expiries,480 ordered events,16 public-empty-route and80 nonempty outputs.
No source-model forward, teacher re-encoding, prompt or feature change occurs.

[DERIVED] Snapshot and hash the existing BFV binary, expected SHA-256
`9c79c02e7d919ecdc24851c6f77e50ed6990397bd669f571a886f8e2b85058c2`,
the pinned `host_runtime/frozen_core` journal modules, unchanged host worker
and unchanged `verified_reader/service.py`. Record source/binary hashes before
and after execution. Freeze all adapter code and copied fixture bytes before
the first crypto command. Existing core validators, arithmetic, signing,
receiver verification and durable transactions are unchanged.

[DERIVED: bounded adapter] Copy the normal utility worker and persistent-host
Run wrapper. Change only fixture staging, assertions for this fixture's known
label-correctness totals, result naming/scope, and the outer cost summary.
Remove the old baseline-workload speedup comparison: this fixture has different
queries and duplicates. The persistent host and public validation bodies are
unchanged. Query-policy list membership permits duplicate rows; keep16 query
files and16 policy entries, while recording8 distinct rows.

## Required actual path

[DERIVED] For each history, use fresh setup/key material and issuer encryption
with the existing OS-randomized BFV executable. Host proposals are processed by
the frozen persistent worker. The authority independently validates/recomputes
and commits its finalized journal. A separate receiver DB/CAS verifies the
signed authorization, parent, revision, nonce/input uniqueness and exact BFV
transition before permitting release. An Infer is initially pending, then
released only after that receiver installs the independently verified event.
All96 outputs, including16 empty-route zeros, traverse this same verified path.

[DERIVED] Compare every actual received scalar to the frozen oracle in the
trusted coordinator. After services stop, independently recompute each of96
scores directly from the retained plaintext contribution queue and query row,
without reusing a BFV/accumulator calculation. Verify256 expiry identities,
the complete480 verified journal rows and96 durable answer rows. The baseline
reader must perform zero decryptions; the verifying reader must perform96.

## Evidence and limitations

[DERIVED] Retain public source snapshots, genesis/accepted envelopes, compressed
logs, event costs, persistent-worker meters, source/fixture pins and the final
integer/evidence audit. Runtime keys, receiver answer DBs and role configs stay
under ignored `runtime/`. They are retained locally, not erased. Audit that
public-role argv has neither full keys nor plaintext Learn vectors, and that
reader scalar stdout/key bytes are absent from retained reports. These are
same-account process/file boundaries, not OS isolation or an information-flow
proof. All clear fixture values are public synthetic research data.

[DERIVED] The reader retains the full BFV secret. Classical Ed25519 authorizes
commands and finalizations. This is trusted-reader benchmarkR, not restricted
decryption, no-master-read authority, private source cognition or end-to-end
post-quantum security. The semantic image has rank4 per route /8 total despite
577-coordinate padding. A coalition obtaining four spanning exact scores at
one checkpoint determines the whole route aggregate. Queue order is separate;
this run makes no recipient-coalition aggregate-state privacy claim.

[DERIVED] The source semantic study remains7,462/8,192 final labels versus
4,178/8,192 original mean10, with15 fresh wet-soil factual errors. The chosen
two histories/16 query records are a deterministic integration subset, not a
fresh accuracy denominator. Preserve the full study and selected error labels;
do not replace them with integration subset success.

[DERIVED: timing] Record whole staging/worker/validation elapsed time, every
crypto role call, receiver RPC, transition, persistent-worker startup/cache
meter and retained/live byte counts. Intervals overlap and shared machine load
applies. Do not infer source-model cost from the small learned state, or claim
a speedup against a different fixture. No adversarial or previously stopped
experiments are imported or executed. Parent and materialization files remain
byte-identical; root owns shared ledgers and commits.
