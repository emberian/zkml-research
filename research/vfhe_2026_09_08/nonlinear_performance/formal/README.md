# Range-native paired MAC for complete nonlinear inference

[DERIVED] `RangeCompletedMac.source_completion` fills the omitted Boolean witnesses from canonical BabyBear bounds and proves acceptance of the existing `BfvKeyswitchCore.system`. `sound` then applies the frozen exact integer MAC theorem. Arithmetic assertions come from the existing addition, canonical-word, and MAC constructors. They are not a second AIR implementation.

[DERIVED] `RangeKeyswitchRow.actualRowSound` applies this completion to all four actual nonlinear BFV primes and the compact layout. `RangeKeyswitchLookup.lookupRowSound` obtains its range premises from the existing IR2 `Lookup.holdsAt` semantics through `IR2RangeLookupBridge`; faithful range-table contents are explicit. `RangeKeyswitchInfer.inferSound` reuses the existing public operand bindings, prefix induction and square composition to force the unchanged `BfvInferContract.result`.

[EXECUTED] Five owned modules passed individual Lean checks with **23 exact axiom guards**, including four generated comparison instances. Nonzero q37 acceptance, premise inhabitation and rejection of a canonical output missing one product are in `RangeMacExhibits`. These exercise the generic source at inhabited parameters, without reducing an entire ciphertext trace in the kernel. Commands, exact source hashes and logs are in `results/` and `READY.json`. The first two generic proof attempts retained elaboration timeouts; explicit arguments and targeted simplification removed them. No arithmetic statement was weakened.

[EXECUTED] `EmitRangeKeyswitch.lean` emitted four fixed profiles in `artifacts/prime{0..3}/`. Each has **349 declared columns, 274 arithmetic assertions, 210 range9 lookups and 26 range16 lookups**, plus the unchanged public97 exact-row lookup. Arithmetic uses the shared `Ir2WholeRow.gateJson` fold: `window_gate`, `on_transition:false`, local `loc/c` leaves. Lookup tuples retain `var/v`. This includes the last row; the former `gate` wrapper did not.

[DERIVED] Public columns 0–96 are unchanged. The retained data columns contain 16 canonical complement words and Boolean addition carries, one shared constant word, two quotient words and two sets of encoded MAC carries. Bit-only columns are omitted. The untrusted native plan appends 349 copies to the previous generated plan; the source theorem does not trust that plan or its executor. `publicPrefix` proves that the projection preserves the caller's public prefix. Each plan has 9132 instructions and 8783 registers and uses the frozen generic BigInt executor.

[SOURCE/EXECUTED] The sibling backend's actual symbolic layout has main width 1293, 132 Ext4 permutation columns after grouping 524 nibble sends into 131 same-bus contexts, and degree 6 / eight quotient chunks. See `../backend/checks/010-compact-shape.json`. Bare ungrouped range lookups would add too many permutation columns; no cost improvement is claimed from declared width alone. The runtime lane owns the single corrected whole-class run and its observed costs.

[OPEN] Lean does not prove Rust JSON/Vec execution, native nibble decomposition and LogUp extraction, backend proof soundness, or public ciphertext parsing/NTT bindings. In particular, the old IR2 semantic range theorem is **not** a proof that native `eval_decomp` implements it. The native bits16 full-top branch sends all four nibbles; the old general emitted helper is not silently instantiated as its proof. The grouped backend has its own algebraic and native conformance evidence. The end-to-end source theorem requires all source rows, including square phases, and the explicit input-specific transform roundtrip. These boundaries stay separate from compiler source correctness.

[EXECUTED] `READY.json` is the runtime handoff and pins the immutable owned sources, templates, plans, executor and successful checks. `range-keyswitch.patch` adds only the five Compiler modules and exporter; its prerequisites are the frozen paired-MAC/witness and Infer packages, the sibling `IR2RangeLookupBridge`, and the shared `Ir2WholeRow` module. No companion tree, prior artifact, private input, or source ciphertext was modified or accessed by this lane. No crypto or prover run was launched here.
