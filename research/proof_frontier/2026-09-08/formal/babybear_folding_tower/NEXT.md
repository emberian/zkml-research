[OPEN] Root owns independent review and combined umbrella integration. The single proposed umbrella import is `Selvage.BabyBearFoldingWitnesses`; its closure includes all four new modules. Preserve this package and the frozen full-UD/carrier/sampling/timing predecessors.

[EXECUTED] The actual 19-round BabyBearExt4 FoldingTower and a concrete 2/5-far initial word are now proved. `MultiplicativeTower.coherent_sound` supplies the actual tower to the frozen consumer. The witness module instantiates the ideal binding transcript, proves positive accepting probability and a rejected index, and applies the existing sampled bound. No more root-generator or tower-inhabitation hypothesis is needed for this mathematical instance.

[OPEN] The independently assigned p3_folding_transport lane should consume the exact natural-power domain and modulo/squaring lemmas. The runtime bridge still needs bit-reversed array transport, right-shift/modulo query-index relation, source coset/polynomial-variable interpretation, fixed/binary arity selection, and the multi-height beta^arity input-injection branch. Its implementation source hashes are already pinned in source-evidence.json; do not claim equality of the Ext6 kernel helper and this Ext4 theorem.

[OPEN] For arbitrary deployed proofs, initial PCS/IOP farness, concrete row/MMCS binding, Fiat–Shamir/challenger and proof-of-work composition remain. The frozen arbitrary-root timing theorem can be instantiated with this tower, but its semantic conflicting-path existence event is still not an efficient collision-extraction bound.

[EXECUTED] Reuse the frozen FullUDSamplingBudget arithmetic; do not credit new security bits from proving the tower or repeat its query/radius optimization. No runtime knob, shared ledger or security label changed.
