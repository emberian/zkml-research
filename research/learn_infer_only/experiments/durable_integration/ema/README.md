# EMA semantic bridge evidence

[EXECUTED] This second tranche is settled. The high-level claim, exact premise/witness/falsifier chain and residuals are in `../../../DURABLE_INTEGRATION.md` under “Second tranche — public signed-byte EMA semantic bridge”. No duplicate lane verdict is maintained here.

[EXECUTED] `check.py` compiles the four new modules against a private research overlay and retained companion oleans. Numbered `lean_*.json` files preserve every result and source hash. `review.py` packages the new patch, checks both proposed roots and the import boundary, and preserves the first tranche hashes. `review.json` records its source/olean dependency map.

[EXECUTED] `audit.py` imports the existing HE Python bit-circuit and exhausts the full signed-byte domain; `audit.json` records65,536 pairs and imported source hash. `InspectEma.lean` calls the existing compiler checker on honest, wrong-post and minimum-byte candidates; `inspect.py` records exact commands/source hashes, with `inspect.json` the current result and numbered failures retained.

[OPEN] Independent source/compile review is being folded by the root. This evidence proves public algebra, canonical openings and model-level install/packet binding. It does not implement encryption, hide the full-word certificate, prove TFHE-rs refinement or realize durable disk atomicity.

Queries: Scry0, web0 (local source work).
