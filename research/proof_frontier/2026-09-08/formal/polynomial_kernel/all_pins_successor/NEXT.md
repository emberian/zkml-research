# Next

[EXECUTED] All-pins successor is complete and checked. Replace the original polynomial entry in the integration manifest with `integration_entry.json`; preserve the original evidence package. The same destination module must appear only once in a combined integration.

[OPEN integration] Include `support_file_entry.json` in the root manifest's `support_files` list so `LICENSES/ArkLib-Apache-2.0.txt` accompanies the module. The standalone patch already includes both files. The full-UD importing theorem has no signature or proof-body change to absorb.

[OPEN integration] Root runs the combined/full umbrella gate. Original mathematical scope and integration limitations remain those in the frozen parent package. No commits or companion-source writes were made.
