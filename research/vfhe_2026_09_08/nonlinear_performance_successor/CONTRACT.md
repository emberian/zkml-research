[DERIVED / intended outcome] A smaller complete nonlinear class proof bundle for the same approved saved `live001/query01/class000` computation. Preserve the predecessor's exact model/query/evaluation-key/kernel ciphertext bindings and all-row arithmetic. Retain source-proved signed-matrix semantics for extension and rescale; explicit bits become reconstructed witnesses under canonical range bounds.

[SOURCE] The unchanged runtime consumer, native binaries, four-bit range backend, fixed-four LogUp grouping and 4096-row coverage come from `../nonlinear_performance/`. Existing MAC and tensor templates/proofs are reused exactly. The two newly emitted templates and native witness projections are the only arithmetic-profile changes. All retained and new artifacts are pinned by the approved profile.

[EXECUTION CONTRACT] Produce four extension and six rescale proofs over the existing public case rows. Consume all 116 native proofs in a fresh pass, including the 106 unchanged proofs. No key, text encoder, ciphertext production, private reader, baseline reproof, parameter grid or sampled-position proof is authorized by this package. Preserve a failed attempt and continue from its actual terminal state; never restart a live process because observation times out.

[OPEN] This changes the two source/lookup profiles and their PCS dimensions. Source completion is not a new numerical proof of Rust parsing, native range lowering, LogUp extraction, PCS/FRI or Fiat–Shamir. Report the exact profile and boundaries. A composed total using recorded unchanged-stage costs is not a newly measured whole-class production latency.
