# Next

[OPEN] This task is sealed as an uncompiled draft under the coordinator's reset/deadline instruction. Do not automatically resume or consume the old caps after the overnight deadline.

[HYPOTHESIS] If separately resumed, first freeze exact public tensors, independent schoolbook outputs and adapter source, then type-check the existing u32 composition against the pinned dependency in a new target directory. Keep the original small finite scope; successful compilation and public arithmetic agreement are the first missing evidence.

[OPEN] No existing keys, ciphertexts or failed-run outputs are needed for that step. Real-key setup, Boolean backend integration, parameter/noise correctness, full EMA execution and portable numerical refinement are separate later premises, not implied by this draft.
