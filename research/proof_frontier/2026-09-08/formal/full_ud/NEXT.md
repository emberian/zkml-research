# Integration handoff

[OPEN] Root applies, in order, the polynomial matrix `all_pins_successor`
patch, polynomial gluing `universe_successor` patch, and `full-ud.patch`.
Deduplicate the identical Apache license file between sibling patches.
The owned patch expects minidregg base
`6937394e1dc2c2aaff986c7d4b3a258aca5d16fd` and adds only new files.

[OPEN] Root wires `Selvage.FullUDFriConsumer` and `Selvage.FullUDWitnesses`
into the umbrella, runs its declaration census and full integration build,
and reviews the exact theorem premises. Positive degree is explicit at the
probability head and every positive tower tail. The old bare-divisibility
`PolishchukSpielman` Prop has not been silently identified with the proved
bounded-quotient theorem.

[OPEN] Useful separate follow-up: instantiate the generic full-UD theorem on
the existing `BabyBearExt4.Ext4` carrier and prove its finite cardinality from
its existing coefficient equivalence. Keep that deployment instance separate
from the explicitly toy witness types. The parameter lane handles sampled
query schedules; the frozen consumer covers whole-opening resolution.

[EXECUTED] No proof work remains inside this frozen bounded tranche.
