# Native sampling status

[EXECUTED] Runtime sources frozen; parent notified before its integrated run.
Eight baseline differential cases and five forced cap/suffix controls passed.
Contemporaneous one-million-coefficient timings show 10.28x error-width and
7.23x key-width sampling speedups. `MEASUREMENTS.json` and `RUN.log` retain
the command/output; `MANIFEST.json` pins this package.

[DERIVED] The same finite output law is retained under independent unbiased
input bytes by sequential native screening plus the unchanged exact fallback.
Read `DESIGN.md` for the argument and limits. The frozen baseline was preserved.

[OPEN] Parent integration results, OS randomness, side-channel engineering,
native-code audit and whole-construction security remain separate.
