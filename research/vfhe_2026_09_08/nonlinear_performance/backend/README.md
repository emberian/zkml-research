# Compact MAC backend support

[EXECUTED] The isolated `p3-lookup-grouped/` crate builds and its two focused tests pass. Its one production change groups at most four **consecutive global interactions on the same bus** into the existing multi-element `Lookup`; local lookups are unchanged. Flattening preserves every tuple, signed multiplicity, bus and order. `p3-lookup-group4.patch` is against Plonky3 `82cfad73cd734d37a0d51953094f970c531817ec`; the other ten upstream lookup source files are byte-identical. Both original licenses remain. Runtime owner has built four existing public readers against this additive dependency selection. This directory runs no proofs.

[EXECUTED] `shape-probe` uses copied actual `descriptor_ir2.rs` with one diagnostic added after the original source. It invokes the actual private layout/instance constructors, symbolic `Lookups::from_air`, maximum-degree and quotient-chunk functions. It performs no preprocessing commitment or proof. The diagnostic asserts equality of all flattened symbolic field expressions and signed counts against the raw AIR interactions. It refuses challenge gates. The probe copy is **not** the selected runtime circuit crate; actual readers retain the unchanged companion circuit crate and select only the lookup patch.

| Actual prime0 artifact | Main columns | Raw bus terms | Ext4 permutation columns | Main + permutation base equivalents | Max degree | ZK quotient chunks |
|---|---:|---:|---:|---:|---:|---:|
| Frozen original MAC | 2655 | 1 | 1 | 2659 | 3 | 4 |
| Compact ranges, groups of four | 1293 | 525 | 132 | 1821 | 6 | 8 |

[EXECUTED] Exact results and template hashes are in `checks/010-compact-shape.json`, `checks/013-old-shape.json`, and `SOURCE.json`. The compact source has 349 producer columns, 210 declared 9-bit ranges and 26 declared 16-bit ranges. Each range adds four auxiliary columns: `349 + (210+26)*4 = 1293`. The byte bus has `210*2 + 26*4 = 524` sends, grouped into 131 contexts; the public97 send is a separate context. New table instances: byte width2/one Ext4 permutation/degree3/four quotient chunks, and exact-public width1/one Ext4/degree3/four chunks. The byte table's hiding degree is5 (16 data rows doubled by ZK), while the other two instance heights follow the actual case. The original template has the separately identified transition-only gate issue; it is a width/cost predecessor, not a newly certified full-row baseline.

[DERIVED] Main plus permutation width falls by 31.5%, but unbatched range replacement would instead have `1293 + 525*4 = 3393` base equivalents. Grouping therefore matters to the proposed optimization. The new degree6 includes the final-row selector; the initially estimated lookup-only degree5 would understate the actual AIR. Existing log blowup3 permits the derived quotient degree. This is a new three-instance PCS profile; no earlier security price is inherited. Runtime owner measures the net whole-class result, including the doubled quotient count and lookup work, with fixed original knobs.

[SOURCE] Relevant unchanged runtime code is `/Users/ember/dev/breadstuffs/circuit/src/descriptor_ir2.rs`: declared ranges take precedence over the fallback whitelist at2120–2180; four-bit limbs are fixed at468; `eval_decomp`3965–4000 handles partial/full top limbs; `fill_main_layout_row`5357–5390 fills honest auxiliaries; verifier byte-height pin is7753. Do not change limb width: the canonical hi4 bound shares the nibble table. Range9 uses IDs78, range16 ID85, exact-public ID11.

[SOURCE] Upstream `batch-stark/src/transcript.rs:74–103` already shares α/β by global bus. Upstream `lookup/src/logup.rs:228–260` clears the sum of all element fractions into one transition/final equation; `:404–554` already fills denominators and accumulates signed multiplicities per multi-element group; `:314–320` checks the total cumulative sum. These algorithms are unchanged. Consecutive grouping preserves the order of first bus appearance. The opt-in profile excludes `ChalGate`: its raw-context challenge-index supply contract is not preserved by a general grouping change. Runtime preparation enforces only whole-row window gates/lookups and zero declared challenges.

[EXECUTED] Lean modules each passed an individual final check with every theorem's exact guarded axiom output: `IR2RangeLookupBridge`7 declarations, `LogUpGrouping`6 declarations, only standard axioms. First bridge applies the **existing** `EffectVmEmitV2.lookup_replaces_rangeW` and `DescriptorIR2.range_row_mem_iff` to canonical compiler BabyBear values; its faithful-table premise is explicit. Nonzero511 witness and512 overflow refusal exercise width9. The second proves the four-term common-denominator equation, transition equivalence when denominators are nonzero, and invariance of sums under regrouping; it has a signed nonzero witness, changed-sign refusal and zero-denominator falsifier. The generic field algebra applies to actual Ext4 but does not prove Rust program refinement or a cryptographic error bound.

[OPEN] Native nibble decomposition and accepted LogUp extraction into semantic `LookupHolds` remain explicit runtime/IR2 lowering assumptions. Existing `ByteTableEmit.value_is_the_row_index` and `served_key_is_canonical` prove coherent accepted table rows; they do not close this native refinement. `TableAirIR.decompGates` is used by its partial-top27/30 consumers and must not be blindly treated as the full-top16 lowering. The formal sibling's completion theorem starts at semantic range acceptance and does not erase this boundary.

Reproduce the bounded checks with `cargo test --manifest-path p3-lookup-grouped/Cargo.toml --release --offline -j2 grouped_`, `python3 check_one.py <new-log-prefix>`, and `python3 check_one.py <new-log-prefix> LogUpGrouping`. Native shape command is recorded by its source and JSON input path; execute `shape-probe/target/release/ir2-grouped-shape <template>`. Use new result filenames. Runtime source and shape bytes are frozen.
