Current service demonstration keeps its frozen baseline cohort. New requests may opt in through emit.py after the runtime owner adds an additive version/config. No more changes to this package. A native emitter or serialization successor may address remaining interpreted cost; that is outside this single matched comparison. Existing query encoding/decryption/protocol proof limitations are unchanged.
