# Deterministic semantic integration fixture, not a new utility estimate

[DERIVED: fixed selection] Select the first two frozen history IDs in ascending
order: 67000 and 67001. Within each public task and each gold attribute pair,
select the lowest two test record IDs, then order the resulting 16 IDs globally.
This identifier rule is frozen before fixture materialization. Gold metadata
balances this public fixture selection and supplies the integer test oracle;
it never supplies the primary feature bits. No outcomes select an example.

[DERIVED] Reuse the parent's exact scored teacher/test predictions and one-hot
map Q=127*e_(4*route+2*a+b), padded577. Reuse all teaching IDs, orders and true
category-label rules from the selected histories; W=32 per route. Preserve
each contribution's identity through expiry. Materialize all three checkpoints
with the same 16 queries, including duplicate model-predicted query vectors.
There is no query deduplication or success-only subset.

[DERIVED: expected schedule to verify] Two histories each have 192 Learn events
and 48 Infer events: 384 signed Learn vectors and 96 query evaluations in 480
chronological events. Expected same-contribution expiries are 32+32+64 per
history, or 256 total. Expected empty-route queries are 8 after the first
checkpoint per history, or 16 total. Count the actual schedule; if it differs,
report the discrepancy without adapting the frozen chronology.

[DERIVED: artifacts/roles] Retain a canonical combined PUBLIC research fixture,
the 16 public query records, source hashes, integer oracle, full-survey outcome
references and error metadata. Generated 384 issuer vector files and 16 query
files are regeneratable role-layout inputs under ignored `runtime/`; retain
their complete hash census and generator. Mode0600 issuer files are a local
role-layout convention, not a confidentiality claim: every vector is public
synthetic research data. Host-facing event metadata excludes the issuer text,
labels and clear contributions for future separation tests. No crypto is run.

[DERIVED] Compare every selected score and state against the already frozen
full utility outputs and independently recompute integer states from retained
queues. Preserve source-model errors and the full survey's denominators; this
two-history integration subset has no new utility accuracy claim. Compute
exact query row rank and the effective aggregate image. A recipient coalition
holding a spanning set of exact scores at one checkpoint can determine all
four aggregate coordinates per route. Padding to577 does not hide them. Queue
order is separate, and sign-only answers do not determine exact magnitudes.
There is no recipient-coalition aggregate-state privacy claim in this fixture.

[DERIVED: preservation] Only this child directory is owned. Verify every frozen
parent manifest entry before and after generation. No encoder execution,
model download, prompt/feature tuning, cryptographic process, parent edit,
shared-ledger edit or commit is authorized in this tranche.
