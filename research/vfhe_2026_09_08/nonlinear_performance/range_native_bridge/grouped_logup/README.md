# Grouped LogUp extracts the byte histogram

[DERIVED] `NativeByteExtraction.sound` starts from the actual fixed-four grouped LogUp first-row, transition, final-row and global cumulative equations. It concludes **exact field histogram conservation or a named bad-challenge event**. Global rational balance and individual lookup membership are conclusions, not assumptions. The theorem allows arbitrary permutation traces and cumulative values chosen after both lookup challenges.

[DERIVED] The main-trace keys and byte-table multiplicities are fixed before fresh independent Ext4 coins `(beta, alpha)`. For a fixed sender list of length `L`, the probability that a wrong histogram admits such permutation witnesses is at most

```
(2 * L + 31) / 2013265921^4.
```

`ByteLogUpDefect` obtains this bound from the existing `Selvage.LogupStar` nonzero defect polynomial and root-counting theorem on the actual BabyBear `X^4 - 11` extension. The sharper support-dependent expression is `(|S| + |S| - 1) / p^4`, where `S` contains the distinct sent values and the sixteen table values. Denominator poles are included. Actual byte tuples contain one field element, so their native Horner compression is independent of beta; `no_tuple_collision` proves the tuple-compression collision event empty for every beta. These are fresh-coin lookup bounds, not Fiat–Shamir or whole-proof security claims.

[SOURCE] The pinned native implementation commits main traces before sampling the shared global-bus lookup challenges, and commits permutation traces and cumulative values afterward. The two challenges named `alpha` in the source occur at different stages: the later AIR constraint-folding challenge is not this lookup denominator coin. `TRACE_SOURCE.md` records the inspected native lines, exact hashes and timing. The byte-table source still requires its coherent, accepted sixteen-row interpretation and the existing verifier height pin.

[DERIVED] The fixed-four group count is a parameter. The same source-shaped theorem serves the compact MAC's 131 groups and the extension/rescale successor's 921/985 groups; its probability depends on the complete fixed sender list. The helper proves the finite-list-to-native-fraction identities and derives rational balance by telescoping exclusive prefixes, including the last contribution exactly once. The signed receiver uses all sixteen table rows.

[DERIVED] `allRowsRanges_or_bad` and `allRowsSound_or_bad` join admission to the frozen compact MAC source, preserving every row including the terminal row. The explicit binding is a permutation between the native grouped sender list and the compiler-declared list; it accounts for group-first versus row-first traversal and does not assume receiver equality. The existing proved bound `524 * n < p` for `n <= 131072` discharges field-count wrap. `checked_for_infer` produces the exact `NativeRangeKeyswitch.Checked` premise consumed by the frozen Infer theorem, with the existing public operand binding and square/transform premises unchanged.

[DERIVED] Nonvacuity includes a nonzero complete 131-column byte-bus witness on the actual Ext4 carrier. A separate committed wrong instance accepts at a bad coin: four invalid key16 sends and eight key15 receives balance at alpha17. Its histogram is proved wrong. The source gate witnesses also show that dropping the terminal equation or the final global check loses the claimed balance. The prior range packet retains the characteristic-wrap falsifier.

[EXECUTED] Four additive Compiler modules pass individual Lean checks with **45 exact guarded theorem footprints**. `CHECKS.json` identifies the final source-matching logs; `SOURCE.json` pins the patch, sources and prerequisites. The additive patch passes application checking against the unchanged minidregg base, and its import boundary passes. No native proof runs, companion edits, parameter searches or changes to the frozen parent range packet were made.

[OPEN] Accepted STARK proofs must still supply these exact source-shaped local equations and the fixed pre-challenge data through commitment/PCS/AIR extraction. Native Rust parsing, vector layout, byte-table coherence/height and group-to-source tuple binding remain explicit refinement boundaries. The lookup challenge model is fresh and independent; there is no Fiat–Shamir, ROM/QROM or complete proof-system security reduction here. This package closes the algebraic and fresh-coin LogUp-to-histogram seam once those source facts are supplied.

Reproduce in dependency order with `python3 check_one.py GroupedLogUpTrace`, `ByteLogUpDefect`, `GroupedByteFractions`, and `NativeByteExtraction`. The isolated runner uses the frozen parent range/backend overlays and the existing `Selvage.BabyBearFullUD` carrier package. Land the additive patch after those prerequisites. The lane is frozen for integration into the complete continuing learner; no successor theorem task is queued.
