[OPEN successor] The running workload used the frozen baseline query emitter.
A faster byte-equal emitter can be selected through an explicitly new pipeline
configuration/genesis in a future instance; it must not relabel these measured
bytes. Expiry, more classes and concurrency are further executions, not gaps in
the bounded two-class demonstration. No independent-review gate is pending.

[SOURCE preservation] Preserve gate.py, run_demo.py and results/gated001 bytes.
The new reader/state and large traces remain under ignored runtime/gated001/.
Root owns publication/shared ledgers. No stopped routing paths or companion
sources were used as write targets.
