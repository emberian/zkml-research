# One fresh semantic text through the normal verified BFV path

[DERIVED: fixed before encoding] Use one newly written synthetic plant text,
one supplied positive teaching label, public route 0, and the already frozen
public query record **q01** for both the before and after Infer. q01 is fixed
by this contract before tokenization or model scoring. It is not selected from
the model's inferred bin. Its public one-hot row is visible by design; the
query choice communicates that fixed requested bin, not a post-encoding choice.

[DERIVED] The actual source text and exact input commitment are stored only
under ignored `runtime/.private/`. Check full-text disjointness against the
original text-transfer and representation records, attribute-calibration,
E5 and semantic-successor record corpora before any forward. Record the corpus
paths/hashes/counts publicly, but keep the input hash, rendered prompts and
token IDs private. Full-text freshness within that named corpus does not imply
a new semantic task or absence from all model training data.

[DERIVED] Reuse the fixed teacher-selected framing A and its public semantic
axis definitions, SmolLM3-3B model revision and cached files, alternative token
IDs 15/16, MPS float16 eager attention, explicit left-padding position IDs,
float32 CPU likelihood arithmetic and tie-to-bit-0 rule. Score the two axes
in one batch of size two using the same maximum-batch-eight loop; no free
generation, prompt alternatives, fallback backend, semantic changes or retry
after a scored result. This is a single partial batch, not a claim that its
numerical result must match another batching arrangement.

[DERIVED] The source encoder sees only the text, task and public requested
axis definitions. It sees neither teaching label nor oracle attributes. After
scoring, the trusted issuer maps its two predicted bits to the unchanged
127-valued one-hot representation, padded to 577 coordinates, and multiplies
by the supplied label. Then it invokes the frozen BFV issuer and signs the
fresh ciphertext for one Learn. No precomputed vector substitutes for this
model forward. No download or weight update is permitted.

[DERIVED] Execute fresh setup and Infer(q01) -> live text encoding plus signed
Learn -> Infer(q01), through the existing persistent host, durable authority
and independently verifying receiver. The BFV, host, verifier and journal core
remain frozen. Adapt only the normal live driver's fixture/frontend wiring,
fixed query index and result description. Both actual outputs must equal
separately recomputed integer references in the private test-oracle role.
All three commands must have the same authority/receiver journal and state;
the verifier decrypts twice and the baseline reader never decrypts.

[DERIVED] Whether the public fixed query's answer changes is an outcome, not
an acceptance requirement. A zero after-score remains a valid result when the
inferred contribution lies in a different bin. Retain the single attempt and
all raw predictions privately. Publish only consistency/counts/costs and the
explicitly disclosed output-change boolean, not text, prompts, token IDs,
predicted feature bits, vector, scalar answers or hashes of those private
inputs/results. The output-change boolean itself is a deliberate observable;
this is not a zero-leakage demonstration.

[DERIVED] This is one illustration, not an accuracy estimate or an additional
utility gate. The source model runs as a trusted plaintext issuer. The receiver
retains a full BFV key; four spanning exact scores expose each route aggregate
on this semantic image. Process/file separation uses the same OS account and
does not supply OS isolation. No no-master-read, encrypted 3B cognition,
exactly-once physical delivery or end-to-end post-quantum claim is made.

[DERIVED] Freeze public source/model/query pins and a private input/prompt
commitment before the first model/crypto call. Retain actual command, elapsed
time, CPU/RSS/accelerator costs, source pre/post checks, normal envelopes and
compressed logs with private content omitted. No adversarial or previously
stopped task is imported. The completed full-fixture parent and its manifest
remain unchanged. Budget: at most one bounded normal run, within 60 minutes.
