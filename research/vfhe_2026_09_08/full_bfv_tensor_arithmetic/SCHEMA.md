[DERIVED / EXECUTED] A public tuple has 56 BabyBear values: global row ID, then eleven little-endian radix64 digits for each of `a0,a1,p0,p1,p2`. All word digits are constrained below 64 and every reconstructed word below the profile's fixed q. The ID is bound by the exact public table, not used as an arithmetic operand. Runtime assigns `rowID=primeIndex*8192+NTTIndex`, with two contiguous 4096-row chunks per prime.

[EXECUTED] The exact nine primes appear in `artifacts/emission.json` and `Compiler/BfvTensorRow.lean`. Base four are about 50 bits; extended five are below 2^62. All fit eleven radix64 digits (66-bit capacity). `READY.json` gives each exact parameter/template/plan hash. Source/trace width is 2036; public table arity 56. Five shared canonical words and slack/range auxiliaries occupy slots 0–830; fixed q−1 digits/bits use 831–907; each of the three product quotient/carry blocks uses 376 columns from 908 onward.

[DERIVED] Each product uses a fixed factor 1, 2, or 1. Quotient has eleven six-bit digits. Twenty-three encoded signed carries have twelve bits and offset 2048; endpoints are pinned to 2048. Twenty-two column equations force `factor*lhs*rhs=output+q*quotient`. All field-side integer masses are proved below BabyBear, so accepted field equality lifts to integer equality. Shared canonical output bounds then force the unique residue.

[SOURCE] Native witness format and instruction semantics are inherited unchanged from `../rescale_native_emitter/SCHEMA.md`. The new plan sets its own register base and uses decimal-string signed literals, including the 62-bit moduli. All public fields are copied; a wrong output is not repaired. The interpreter is an untrusted witness producer, not the arithmetic authority.
