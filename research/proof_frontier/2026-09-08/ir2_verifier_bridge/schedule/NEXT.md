# Integration handoff

[OPEN] Root should copy the two frozen `Selvage/` modules through the shared IR2 verifier bridge. Instantiate `initial i` with reducedInput(i)=committedWord0(i), and `mask n k` with the actual next-word/native-fold equality. Use `weight initial mask n hn`, `QueryAccepts initial mask q`, and the exact/fresh query laws directly.

[OPEN] Root owns weighted native reconstruction, terminal-code membership, challenge timing, the actual executable adapter, and the whole-tree integration gate. This lane supplies concrete supplied-mask counting and sampling, with no standalone framework or manifest.
