[DERIVED correction] The sealed NOTE.md describes radius1/4 in the F17,n2,d1
counterexample as below the conservative UD threshold. It is equal to that
threshold, (1−1/2)/2=1/4. Replace that wording/radius by **radius1/5**, strictly
inside the threshold. On a two-position domain both radii allow only zero
errors, so the exact good-scalar counts and failure of joint agreement remain
unchanged. The same correction applies to the explanatory real-radius label
in RESULTS.json. The original finite computations used integer radius0.

[OPEN at correction time] The new CurveFullUDTeeth Lean module uses radius1/5,
with explicit strict-radius admissibility to be included in the checked falsifier.
The primary source theorem, degree-M target, and all other claims are unchanged.
Original sealed NOTE/RESULTS/MANIFEST bytes are preserved.
