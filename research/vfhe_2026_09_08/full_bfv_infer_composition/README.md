# One compiler-derived Infer contract

[EXECUTED] The complete retained Infer passed all **116 fresh proof checks**: 88 full MAC chunks and 28 reused square proofs. The consumer covered **360,448 MAC rows and 720,896 MAC output residues**, then joined the complete basis-extension, tensor and directed-rescale phases. The proof bundle is **145,628,066 bytes** and fresh verification took **58.093 seconds**. Evidence: `../full_bfv_multiply_successor/infer/results/run001/verification/result.json`. This is one saved workload under the explicit TCB below, not a shipped system or a proved teaching history.

[DERIVED] The headline theorem is `BfvInferContract.inferSound`. Its `AcceptedInfer` premise contains accepted source assignments and shared public array/index bindings throughout the dot and square; it forces `kernel = exactDirectedSquare(inferDot(model,query,key))`. No desired arithmetic-output equation is assumed. `uniqueOutput` and `wrongOutputRefused` give universal determinism and refusal for these bound inputs. The one shared base transform pair is used in both phases.

[DERIVED] This package composes the actual saved N8192 level-zero Infer from its initial ciphertext–plaintext product, nine column rotation-and-add stages, and one row rotation-and-add stage. Every arithmetic row is the existing paired four-term MAC source. `BfvInferComposition.inferDotSound` quantifies over every component, all four primes and every NTT coordinate at all eleven stages, and forces the final dot tensor from the same model, query, public evaluation key and preceding states.

[EXECUTED] `artifacts/linear_plan.json` is generated by Lean from the same permutation definition used in `BfvInferLinear.permutationCorrect`. The actual Infer verifier consumes its direct output-to-input permutation arrays. It no longer reconstructs the permutation schedule with a second implementation. The frozen 399,094-byte plan has SHA256 `ec98f40fa8437f03ccf0193774c705f4a488fd55b42fcdc46f6c3774c7c51f22`; `LINEAR_READY.json` records source and exporter pins. Generation took 3.300 seconds in `results/emission001.json`.

[SOURCE] The serialized evaluation key has ten keys, each with ciphertext level, key-switch level and log base all zero. The exact source branch is `/Users/ember/dev/breadstuffs/vendor/fhe-dregg/src/bfv/keys/key_switching_key.rs:234`: select each of four PowerBasis residue rows, lift it to all target moduli, transform, and multiply-accumulate with each output key half. `/Users/ember/dev/breadstuffs/vendor/fhe-dregg/src/bfv/keys/galois_key.rs:64` substitutes both components and adds substituted component zero. Equal level-zero contexts avoid its downswitch branch. The owned `full_bfv_keyswitch_arithmetic/PUBLIC_PROVENANCE.json` retains the public serialized metadata.

[SOURCE] `/Users/ember/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/fhe-math-0.1.1/src/rq/mod.rs:387` copies each selected coefficient row, calls `lazy_reduce_vec`, and applies lazy forward NTT. `zq/mod.rs:735` selects optimized reduction; `zq/primes.rs:9` defines its exact integer condition. The actual four primes are between 2^49 and 2^50 and satisfy that condition. For canonical source residue x, `x >> 50` is zero; the optimized lazy reduction leaves x unchanged, with x below twice every target modulus. Canonical reduction before target NTT therefore represents x modulo the target prime. `BfvInferLinear.liftCorrect` proves this natural-word arithmetic, including the lazy range. Rust bit operations and NTT implementation are not newly language-refined.

[DERIVED] `reverseIndex` reverses exactly thirteen bits. The native indexing equation is `substituted[reverseIndex j] = original[reverseIndex (((e-1)/2+e*j)%8192)]`; reverse-index involution gives the direct array read emitted in the plan. `stageExponents` proves the nine exponents are powers of three for shifts 8 through 2048 modulo 16384, followed by exponent 16383 for the row automorphism. No single-prime bit decomposition, rounding scaler or modulus-down step is introduced.

[DERIVED] `Bound` only identifies the public D, key, addend and output words with their roles. It does not assume a MAC output equation. `emittedChecked_sound` obtains that equation from `BfvKeyswitchRow.emittedSystem_sound`. The initial row uses D0=query, K0[0]=model0 and K1[0]=model1; other terms are zero. Later rows use the four lifted residues, both key halves, add0=old0+substituted0 modulo q, and add1=old1. Prefix induction supplies the final dot result.

[DERIVED] `BfvInferExhibits.rowPremiseInhabited` reuses a nonzero genuinely accepting q37 source assignment. `changedOutputRefused` excludes the still-canonical omitted-term output for every auxiliary assignment. A separate ten-stage modular prefix witness and omitted-final-stage falsifier test coverage. The actual large accepting assignments are exercised by the real proof pipeline; no huge closed Lean witness is reduced.

[OPEN] The input model and query are decoded canonical tensors, not a newly formalized byte commitment or semantic embedding. The callbacks name public forward/inverse NTT computation. Native parsing, seed expansion, SIMD encoding, serialization, proof-protocol/PCS soundness and the fixed index/phase join controller remain explicit runtime trust boundaries. The theorem does not imply BFV security, decryption/noise correctness, learning utility, model authentication or recipient privacy. No new AIR, witness executor, crypto run, source-check grid or full build is added here.

[DERIVED] `BfvSquareComposition.squareChecked_sound` composes the actual accepted public basis, tensor and profiled-rescale sources. Each full tensor is shared between its producer and consumer. The exact directed square keeps the four native common-modulus NTT rows unchanged and transforms only the five newly forced extension rows. An explicit input-specific `BaseRoundTrip` law connects the accepted copied PowerBasis rows to those native NTT copies. The final forward transform is a representation binding; it does not assume the square result.

[DERIVED] `nativeLiftCorrespondence` applies the proved lazy-lift word law to a canonical inverse-transform row. The additional input-specific NTT congruence law identifies forward NTT of x%q with forward NTT of x. That congruence and the common-modulus roundtrip are NTT trust-boundary laws; neither is silently claimed as a Rust implementation theorem. The main contract is exact for the supplied public callbacks. The retained runtime import additionally matched the actual native dot bytes before its one complete proof sequence.

[EXECUTED] The final five Compiler modules contain 32 theorem declarations with 32 exact axiom guards; all final individual module checks pass, using frozen predecessor oleans. Source, patch and retained olean pins are in `source_pins.json`. The sole plan emitter has no handwritten permutation table. Failed elaboration/guard-format attempts remain in `results/`. No old artifact, companion main tree, proof baseline or full tensor Lean interpreter was rerun. Root's integration lane owns the cumulative patch and import-only umbrella check.

[OPEN] Kernel exhibits establish nonzero generic arithmetic premise inhabitation, callback-law inhabitation and refusal/coverage teeth. The enormous actual full-layout conjunction is inhabited by the executed witness/proof pipeline under its proof-system TCB, not a closed kernel reduction. The approved model digest binds this Infer input; the saved revision-nine teaching history was not proof-gated and is not established here. After this accepted outcome the lane stops component expansion.
