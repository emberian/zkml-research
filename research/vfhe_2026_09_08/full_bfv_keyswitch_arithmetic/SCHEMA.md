[DERIVED / EXECUTED] Public tuple: rowID, then sixteen six-digit little-endian radix512 words in this order: D0–D3, K00–K03, K10–K13, add0, add1, out0, out1. Arity97. All word digits are constrained below512 and all reconstructed words below the profile's fixed positive q. RowID is bound by the exact public table. Runtime uses `stage*4*8192 + prime*8192 + NTTIndex`, with stage0 initial product and stages1–10 rotation/accumulation, two contiguous4096-row chunks per prime/stage.

[DERIVED] Source layout has2655 cells. Public cells0–96 are followed by sixteen121-cell canonical slack/range blocks, then six fixed q−1 digits and54 fixed bits at2033–2092. Each output has281 auxiliary cells: six quotient digits,54 quotient bits,13 carries and208 carry bits. Twelve schoolbook columns force the integer four-term product sum plus addend to equal output plus q times quotient. Carries have16 bits and offset32768, with both endpoints pinned to32768. Every field-side integer mass is proved below BabyBear, enabling exact integer telescoping.

[EXECUTED] All four actual primes satisfy `0<q` and `5*q<512^6`; the latter ensures each quotient fits six digits for canonical operands/addend. Exact prime/template/plan identities are in READY.json and artifacts/emission.json. The native format and operation meanings are reused unchanged from `../rescale_native_emitter/SCHEMA.md`. The producer copies all97 caller columns unchanged, calculates auxiliary ranges/quotients/carries only, and uses signed decimal-string BigInt literals.
