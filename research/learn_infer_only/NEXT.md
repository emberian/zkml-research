# Next work in the second authorized timed run

[EXECUTED authorization] The new run remains active until **2026-09-07 10am EDT /
14:00 UTC**. Follow the [end-to-end contract](experiments/end_to_end/CONTRACT.md).
Root owns shared ledgers and named-file commits. Companion trees and
docs/VERDICTS.md remain read-only.

1. [OPEN active implementation] Build the reviewed designated-recipient
   fixed-span DDH construction into a separate actual journal path. Carver owns
   `experiments/private_construction/designated_span/crypto/`; Einstein owns
   its `integration/`; root owns CONTRACT.md and shared ledgers. Start with
   normal 40 Learn/four Infer/eight expiry and orderly restart/retry. Recipient
   scalars permit the full fixed span on every retained input. They do not
   enforce finality or sign-only disclosure against the recipient itself.
   Decide full useful-workload and live-text runs after the small path passes.

2. [OPEN active review] Locke independently reviews the completed terminal
   joint-output lemma under private_ingress/provenance_review/terminal_joint/.
   Earlier parameter review passed with L>E correction. The terminal lemma
   supplies a complete earlier-package simulation and explicit conditional
   bound. Tiny primitive rates and random-setup-to-source-compatibility
   quantifiers remain separate; do not invent arbitrary secret advice in the
   source definition. Russell now audits designated-span prior art.

3. [EXECUTED complete] The generalized fixed-span proof passed review. The
   actual 577-coordinate/16-query DDH full run passes 384 Learn, 96 integer
   scores, 256 expiries and 384 queue-product checks in 1,537.085 seconds.
   Current payload is 9,765,888 bytes; all input ciphertexts total 56,819,712
   bytes. No master is exported, but erasure/private issuance are assumed.
   Do not rerun this completed workload without a changed question or failure.

4. [OPEN active source review] Turing reviews Bohr's completed base-FE audit.
   The one-copy quantum-advice bootstrap and pointwise-correctness supplement
   already passed independent review. The decisive remaining obligations are
   a coherent uniformly short public key, depth-upgrade compactness and charged
   quantitative quantum security/error rates. A conditional bridge is not an
   instantiated LWE-only primitive. Any direct-layout size contradiction must
   be scoped to that representation, not the source theorem or field.

5. [EXECUTED proof successor; OPEN refinement] Mixed-journal framing23 and
   source-noise/window34 are complete in the 53-module/708-pin run014. Use the
   compatible combined708 patch, not every historical proposal. Actual runtime
   correspondence, accepted-byte provenance, Rust/RNS/NTT/scaler semantics,
   retention compaction and physical persistence remain separate obligations.

6. [EXECUTED arithmetic and BFV path] The complete independently verifying BFV
   path passes all 384/96 events in 201.345 seconds with the persistent host.
   Live new text passes one Learn/two Infer in 15.418 seconds, including actual
   model encoding. Public integer/reference-NTT byte reconstruction also passed
   independent review. Universal accepted-byte/Rust refinement and noise
   correctness remain separate. No full BFV reader-key removal is claimed.

7. [OPEN utility and complete resident] Hooke completes the exactly configured
   teacher-only 3B semantic preflight. Original, quadratic, supervised-attribute
   and E5 utility attempts retain their failures; do not select a new encoder
   on an already inspected test set. The true-attribute control is privileged
   information, not successful natural-language understanding. Beyond this
   fixed-policy demo, preserve private nonlinear learning, actual encoder-image
   nonvacuity, unknown lifetime inputs, restricted release against all surviving
   credential holders, and physical continuity as the central open targets.

[EXECUTED search accounting] First-run Scry12 SQL/two schema; second-run
lockable-gate source audit adds two SQL/zero schema. Record further calls by
lane. Never print the credential or download an eprint PDF.

[EXECUTED retained interruptions] The original ideal-window disclosure task
and the later additional journal adversarial routing check stopped at automatic
safety review. Do not resume those tasks or infer mathematical conclusions
from their interruptions. Normal functional verification is separate.
