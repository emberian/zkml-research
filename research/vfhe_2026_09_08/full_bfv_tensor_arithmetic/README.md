# Whole extended-basis BFV square tensor

[EXECUTED] All 73,728 actual extended-prime NTT positions of the saved N8192 nonlinear square have passed the generated relation: 18 complete chunks, three products per position, 221,184 outputs. Saved runtime records contain 12,978,885 bytes of proofs, 45.523 seconds of proving, 10.397 seconds of native witness emission, and 65.633 seconds inside the tensor phase controller. The largest retained proof-process RSS is 888,504,320 bytes. These are one shared-host execution's measurements, not an isolated benchmark. `RESULT.json` pins and summarizes `../full_bfv_multiply_successor/results/tensor001/result.json`; this lane inspected the saved public records without rerunning cryptography.

[DERIVED, Lean checked] `Compiler/BfvTensorRow.lean:61`, `emittedSystem_sound`, forces five canonical integers and all three equations `p0=a0² mod q`, `p1=2*a0*a1 mod q`, and `p2=a1² mod q`, for every accepted assignment and each of the nine actual primes. The two input words are shared. Their bounds and the output bounds are source constraints, not supplied correctness premises. The generic multiplication theorem supports factors at most two with eleven radix64 limbs, twenty-two convolution columns, range-constrained signed carries and exact integer telescoping. The source equation is folded into IR2 by `EmitBfvTensor.lean`; no Rust AIR is handwritten.

[EXECUTED] All 29 theorem declarations have exact `#guard_msgs` axiom pins in `Compiler/BfvTensorAxiomPins.lean`, checked with empty output and exit zero. The census contains only subsets of `propext`, `Classical.choice`, and `Quot.sound`. There is no `sorry`, axiom declaration, or `native_decide` in the added source. `BfvTensorCore.Exhibits.nonzero_premise` constructs actual source acceptance for nonzero inputs 3 and 5 modulo 37, yielding 9, 30, 25. Its falsifier changes the cross-output to the still-canonical 15: canonical constraints remain satisfiable but `missing_cross_factor_refused` proves source rejection for any assignment with those values. `quotient_capacity` proves the doubled-product quotient fits eleven digits whenever the canonical inputs and deployed `2*q < 64^11` bound hold. Field-column and all nine prime capacities are checked too.

[EXECUTED] `READY.json` is the immutable callable handoff: nine exact templates and plans, source and executor hashes, width 2036 and public arity 56. Each template has 2183 arithmetic constraints plus the complete public-row lookup. Every native plan has 7707 instructions/registers. It reuses the frozen generic BigInt interpreter and the query plan's generic builder. All five supplied words are copied unchanged; only auxiliary witnesses are computed. Successful actual full-chunk acceptance is the execution evidence for this untrusted producer, not a universal witness-compiler refinement theorem.

[EXECUTED] `proposal.patch` adds seven files to minidregg after the existing compiler/query-plan prerequisites. `source_pins.json` records their origins and hashes; `results/dependency_artifacts.json` pins 177 read-only cached dependencies. The patch passes `git apply --check` against the companion and applies byte-identically in an owned staging directory. The companion import-boundary script passes. No companion source, shared build, prior package, key or learner artifact was modified. Single-file Lean commands and all earlier failed diagnostics remain in `results/`; the first axiom-discovery command had an unsupported pretty-print option, while the final exact guards pass.

[SCOPE] This is complete pointwise tensor arithmetic for the captured square. Public ciphertext parsing, linear NTT transforms and capture linkage remain runtime obligations; basis extension and directed rescale have separate source relations. This package alone does not prove a complete nonlinear Infer, rotations, key switching, plaintext encoding, FIFO authorization, model commitment, BFV decryption, or reader privacy. Root/runtime own the complete-operation join and its current status. No additional model, crypto, sample-proof or utility run was used here.

To consume a profile, pass its pinned plan, template and public rows to the pinned executor as `lean-bigint-witness PLAN TEMPLATE PUBLIC_ROWS NEW_OUTPUT_DIRECTORY`. Re-emission uses the exact Lean command and `LEAN_PATH` saved in `results/emission_001.json`; choose a new output directory to preserve the actual run's artifacts. The public schema is in `SCHEMA.md`.
