# Receipt-key capsule: completed bounded source tranche

[EXECUTED, 2026-09-08] Main deliverable: `AUDIT.md` (concrete algorithms,
credential lifecycle, exact selective-MIFE conditional theorem, signed-input
quantifier lemma, FHE auxiliary-correlation seam, missing restricted simulation
game, nonvacuity and a source-backed fixed-capability release lead).
`SOURCES.md` records actual access levels and every query. No crypto,
disclosure/routing/extraction control or malformed-input experiment was run.
Earlier safety-stopped tasks remain stopped; shared ledgers, companions and
VERDICTS were not edited. No commit was made.

[SOURCE/DERIVED] 2013/727's public-slot compatibility requires exact equality
on all accepted encoded inputs. Computational difficulty of producing a valid
signature is not its admissibility criterion. The separately useful dummy-key
FE theorem requires that exact equality and a common jointly generated
auxiliary package. Regenerating a matching public FHE key in each world, or
using an FHE challenge while secretly assuming its unknown key, is not a proof.

[SOURCE/DERIVED] 2024/1477 supplies an actual selective fixed-message SWE game
with other-tag signature queries and future honest unlocking. Strong puncturing
makes the challenge-tag signature mathematically nonexistent in the proof's
hybrid; this is a real repair of that kind of quantifier seam in its scoped
game. It can release a fixed restricted capability. It neither constructs the
restricted FHE capability nor protects a full FHE key after honest unlocking.

[EXECUTED validation] Reproduction command:
`python3 research/learn_infer_only/experiments/private_construction/receipt_key_capsule/notes/pin_sources.py`.
The saved `pin_sources.stdout.json` and `SOURCE_MANIFEST.json` record five
matching local PDF-to-text re-extractions, source hashes and artifact hashes.
This validates provenance; it is not cryptographic validation or an independent
review of the mathematical derivations. The previously saved 2013/727 text was
read and matched, not changed.

[EXECUTED new query totals] SQL2/schema0/web-search8/Kagi0; no retry. Three
web page opens failed; one public API metadata GET succeeded. Both Scry records
report zero spend under free slack. Zero network PDF downloads. No claim of
field-wide absence is made from this corpus/instrument.

[OPEN resume] Seek an actual source-backed, correlated-key restricted capsule
simulation theorem; alternatively, choose a finite predeclared restricted
capability library and prove its lifetime union matches the permitted learner
interface before applying SWE. Adaptive receipt-body tags, signatures exposed
on the challenge tag, malicious setup/all-signer corruption, actual PQ security
and concrete costs remain outside the positive source result. Root owns ledger
updates and review scheduling.
