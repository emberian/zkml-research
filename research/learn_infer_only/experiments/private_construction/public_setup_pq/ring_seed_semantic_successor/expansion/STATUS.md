# Expander v2 status

[EXECUTED] Source frozen and constructor/domain checks passed. Exactly
48-byte seeds are accepted; 32/64-byte downgrades are rejected. Binding is
32 bytes and protocol is `ring-seed-semantic/public-rows/v2`.

[EXECUTED] Zero expansion, SHAKE, setup or private-artifact reads. The
predecessor remains unchanged. Evidence and pins are in `CHECKS.json`,
`RUN.log` and `MANIFEST.json`; scope is in `PROVENANCE.md`.
