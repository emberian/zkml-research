# First designated fixed-span integration: closure

[EXECUTED] The actual designated DDH backend completed 40 Learn, 4 selected
Infer and 8 exact-original expiries. The independent recipient verifier and the
final public replay each checked all44 transitions. The four private signed
integers matched the independent integer oracle. An orderly close/reopen at
revision22 preserved the exact head; two exact historical requests reused their
original finalized envelopes without advancing revision44. Four ciphertexts were
publicly accepted before any private scalar decode. Private drain produced four
answers, then its repeated drain produced zero additional decodes.

[EXECUTED: precise exit history] The original normal harness completed those
checks and then exited1 at a packaging-only substring guard. It searched for
`signed_score` anywhere in public log text and therefore matched the public
parameter key `row_signed_score_bounds`. No actual scalar field caused this
failure. `source_original/normal_flow.before_guard_fix.py` and
`reports/first_harness_stdout.log` preserve the executed bytes and error.
`finalize_normal_001.py` then checked the completed journal, saved replay,
private aggregate comparison/dedup evidence and exact parsed public log fields,
and exited0. No cryptographic operation or private decode was rerun. The helper
now checks exact scalar/decode keys and actual decoder commands; the current
future harness has only that guard replacement. Its full flow was not rerun.
`reports/EXECUTION.json` records both outcomes without conflating them.

| [EXECUTED] Evidence | SHA256 |
|---|---|
| `evidence/normal_001/report.json` | `dbc7b6b41ff27a380755c210d9a1c190b3792fb1922f0a492add065312113ece` |
| `evidence/normal_001/MANIFEST.json` | `61d01bf761ba86b6cf71ca2b2d55463b0493405e62d028d55d7086229ce9bfe3` |
| Crypto source `crypto.py` | `d7dfc2f46044ea438e1de548b6f922720cf36418b02bc02dac095cbc5851adb0` |

[EXECUTED] Genesis `3b073b0d05f0db225040ca23d05c99015aa05438fbf856f37af37c28089a6683`
pins all eight adapted journal/reader/drain modules, four crypto runtime files,
canonical context and native dependency. No operational core source changed
during the run or guard finalization. The seven original BFV source files and
seven exact `source_original` copies remain unchanged. All32 exported evidence
files are pinned in the normal evidence manifest; private files and private file
hashes were not exported.

## Measured costs

[EXECUTED] `PUBLIC_COSTS.json` records actual top-level CLI calls and source
counter output, including the final independent replay. The public progress
checkpoint immediately after40Learn/4Infer was **2020.574594500 seconds**
(33min40.575s). The exact elapsed point after the two historical retries was not
persisted before the packaging failure, so that report field is deliberately
null. Private decode times remain in the private cost log and are not published.
The timing below is CLI elapsed time, not a summed E2E wall-time measurement;
outer role/RPC durations overlap their constituent CLI work.

| Role | Public CLI calls | Sum of its CLI elapsed seconds |
|---|---:|---:|
| Trusted setup / query encoding | 19 | 81.056588 |
| Issuer encryption | 40 | 45.600608 |
| Public host transport | 84 | 91.473493 |
| One-shot host proposals | 1057 | 1306.110461 |
| Authority | 151 | 230.708667 |
| Public recipient verifier | 151 | 249.731925 |
| Independent final public replay | 128 | 198.391181 |
| Total top-level CLI | 1630 | 2203.072923 |

[EXECUTED] Including33 setup child processes gives1663 crypto process
invocations. Source counters total **1,213,729 native modular powers** and
**1,026,248 subgroup checks**; the context counters record18 full validations,
288 checked row equations, and1627 pinned-context reuses. Top-level and setup
child counter totals are recorded separately before addition. No private drain
calls/counters are included in this public accounting.

[EXECUTED] The canonical public context is342331bytes, each state ciphertext
148147bytes, each designated output691bytes, and each dedicated scalar envelope
435bytes. All16 retained scalar files total6960bytes. The namespace inventory
finds no remaining `.delivery`, `.pending`, or temporary setup directory under
this run's `.private` tree. This is a named-path inventory plus source-reported
never-serialized master, not a claim about memory, backups or physical erasure.
The immutable host and authority CAS each retain100objects/11,886,238bytes; the
reader CAS retains88objects/11,862,466bytes (only the four selected public queries).
All three copies total35,634,942bytes; expiry does not delete old ciphertexts.

[DERIVED: cost forecast, not a benchmark] The one-shot host launched1013
inspection calls costing1200.593653s. A persistent host removes repeated public
membership work after a valid cached object, while keeping byte reads/hashes.
It does not remove issuer, authority, independent recipient or final-replay
work. Replacing excess host inspections before scaling these measurements to
384Learn/96Infer with256expiry suggests an order-of-three-hour full protocol
run is plausible; a two-hour bound is not established by the host-only speedup.
The parent owns that subsequent cost/launch decision. The fixed full utility
fixture has two independent histories, so its256expiry total must not be
implemented as an undeclared reset inside one continuing journal.

## Claim boundary and continuation

[DERIVED] This is a normal executable integration of a conditional classical
DDH fixed-span construction. The public verifier accepts encrypted outputs;
private designated scalar use is a separate phase/database. Later public
status/retry/persistence methods read no private answers. The trusted test
coordinator's comparison bit, report publication and phase scheduling are outside
the host view; the original coordinator schedules its final public replay after
private drain. A successor wrapper should finish all public checks before drain.
The recipient coalition holds all16 fixed keys and can evaluate its entire span
on retained individual inputs/snapshots without respecting this application.
No new query directions, recipient self-restraint, physical erasure, private OS
isolation, post-quantum security or independent hardware finality is supplied.

[DERIVED] The fixture/oracle retains all40 plaintext inputs and knows the initial
zero. It can reconstruct the full raw aggregate by replay. Consequently, this
actual experiment is not a demonstration that no person or coalition has any
read-all route to its state. The role-scoped host privacy argument excludes this
oracle and its files. The experiment verifies encrypted protocol behavior while
the stronger absence-of-every-surviving-capability goal remains conditional on
additional lifecycle assumptions and a nontrivial private development source.

[REPORTED] The crypto lane's read-only API check found no adapter mismatch;
its one nonblocking observation was that a private telemetry `metrics` entry is
null because backend counters are flat fields. No further tests were launched
after parent accepted finalization. The separate persistent-host and full-utility
lanes own any successor run. This lane changed no companion or shared ledger,
made no commit, spawned no agents and made no new search query. Cumulative search
accounting remains Scry SQL10/schema1, web30, Kagi0.
