# Exact native external product successor

[OPEN] Root-authorized new backend construction. Own this directory only; all predecessors, companions and stopped routes remain read-only. No process from decomposition continued over the interruption. No full PBS run, test grid or verifier rewrite.

[SOURCE] Existing TFHE1.6.3 raw GGSW API `encrypt_constant_ggsw_ciphertext`, raw-to-Fourier converter, public Fourier `add_external_product_assign`, and public `karatsuba_add_external_product_assign` have been read. Karatsuba uses the same SignedDecomposer/tensor iterator and raw level/row order with exact native wrapping polynomial products. The deployed profile is u32/N512/GLWE-size4/base-log10/levels2, with DEFAULT_PARAMETERS.glwe_noise_distribution (stddev9.315272083503367e-10 in torus units).

[OPEN planned one capture] The original raw evaluation key was not retained. Generate exactly one raw public GGSW encryption of the public selector1 using a fresh ephemeral binary GLWE secret and the actual profile. Save raw/Fourier public evaluation ciphertexts only, never secret key/seed/RNG. Existing initial LUT and delta have zero masks, so they are meaningful public plaintext accumulators under this fresh GLWE key. This does not preserve the original805-dimensional LWE key's selector identity and is not an original whole-PBS continuation claim. Verify all3 preceding public proofs before using their exact retained delta/digits.

[DERIVED new arithmetic backend] Compute every output coefficient of the2-level4×4 polynomial external product with a two-prime negacyclic NTT and exact signed CRT reconstruction. Use primes2013265921 and998244353, primitive roots31 and3; check primality,1024th-root order and CRT range in pure integer preflight. Center raw u32 key coefficients as signed32 and use exact signed digits. At most8·512 products contribute to each output coefficient, each bounded by512·2^31, giving |sum|≤2^52. The prime product exceeds2^53, so centered CRT is unique throughout the bound; only then reduce modulo2^32. This is deliberately separate from floating FFT arithmetic.

[OPEN planned evidence] One complete CRT-NTT native external product, comparison of every coefficient with the existing exact Karatsuba path and a direct signed integer reference, and one existing Fourier comparison with measured full-byte/toroidal differences. No parameter weakening; no FFT equality claim if bytes differ. An ephemeral decryption of this public selector1 result can report error against the public input delta without retaining client material. New public outputs/transforms and exact source pins will remain replayable.

[DERIVED efficient proof direction] Saved digits have exactly one nonzero polynomial: level1,row3. Dividing it by256 gives +1 on[0,44),0 on[44,162),−1 on[162,512). Thus `(1−X)D/256=−X^44−X^162 mod(X^512+1)`. For active raw key polynomial K reduced modulo2^24, the scaled output y has anchor `y0=ΣK[0..350]−ΣK[469..511]`, and recurrence `yj−y(j−1)=−(X^44K+X^162K)j`. A proved anchor is essential because2 is not invertible modulo2^24. This may permit a complete O(N) generated affine/wrap proof of the actual component; parent has been informed. Existing BfvQueryMul supports general modular products, and SignedMatrix/ProfiledMatrix supports affine accumulation. No handwritten duplicate AIR or ideal FFT oracle.
