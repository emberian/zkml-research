# Next

[EXECUTED] Consume the saved public template, rows and proof with the native verifier; no fresh proof or private files are required. `MANIFEST.json` is the package handoff.

[OPEN] Next constructive increment: bind and prove the actual initial negacyclic LUT rotation and first signed decomposition, using the saved native rotation state and current source APIs. Retain the q=`2^32` convention and native rounded digit behavior. A later full blind-rotation component must address the actual FFT/torus-conversion relation rather than substitute an ideal product. No continuation or runtime launch is authorized merely by this note.
