# Public-seed durable journal normal join

[DERIVED authorized scope] Root's renewed user instruction authorizes completion
and execution of the prepared public-seed journal join. The earlier deadline
prohibition and proposed independent-review launch gate are superseded. This
successor preserves the complete prepared predecessor and frozen adapter bytes;
it uses the existing source and execution pinning mechanism, with no new review
or authorization queue.

[DERIVED fixed workload] One fresh runtime/normal_001, with33 Learn operations
on route zero, capacity32, and Infer rows0/1/2/3 after Learn counts1/16/32/33.
The known vectors are x[n,j]=((n+3)*(j+5) mod17)-8, for577 coordinates. There
are37 finalized events, exactly original Learn1 expiry at Learn33, and final
FIFO inputs2 through33. Route one stays empty. Authority and keyless acceptor
close and reopen at revision18 with exact head preservation. After revision37,
one exact historical Infer1 retry must return its original signed envelope and
leave the head and four selections unchanged. These are ordinary supported
operations; there are no crash, malformed-input, extraction or routing tests.

[SOURCE dependency boundary] The original36-entry SOURCE_PINS.json and every
listed dependency are copied byte-for-byte, including crypto, journal, verified
reader, persistent host and the complete nested seed adapter packet. The nested
adapter keeps its accepted source identity. setup_join.py and private_oracle.py
are unchanged prepared wrappers. Changes are limited to the driver's obsolete
review gate/progress reporting, launcher phase plumbing, and postshutdown public
closure checks. WRAPPER_ORIGINS.json pins the predecessor wrappers; driver.py
prepare pins the exact executable successor and environment.

[DERIVED setup and authority] The unchanged seed wrapper creates all16 fresh
independent signing registrations and recipient-owned scalars before its one
fixed seed candidate, full-domain SHAKE256 recipe and capped first acceptance
(128). Complete public seed/context replay precedes operation. Genesis binds
registry, announcements, full transcript, context, verification and validation
records, seed/suite/cap/domain, complete setup source identity, nested adapter
manifest and join source. Existing genesis hashes carry these identities into
authorizations, signed finalizations and durable heads. No scalar master or
scalar projection delivery is generated by this path.

[DERIVED public-before-private] The public worker completes the fixed history,
supported reopen/retry, exact FIFO checks, all37 public arithmetic replays, final
seed/context replay, public log checks, CAS inventories and orderly service/host
shutdown. A separate public checker then verifies signatures, exact history,
closed SQLite heads, CAS bytes and closed compressed log snapshots without
private files or group arithmetic. Whole process groups must be absent. Only
then are PUBLIC_SEAL and PUBLIC_GATE written, using integer nanoseconds compatible
with the strict JSON reader. All public evidence is closed before the separate
private worker opens the one four-answer drain and direct integer comparison.
Only aggregate correctness is published; private values/hashes/decode timings
stay under ignored runtime/.private paths.

[DERIVED launcher] Invoke only python3 -B launch.py from this successor. The
launcher checks exact prepared pins, requires a fresh runtime/launch record,
uses a separate process group for each worker, and enforces the existing
2,700-second overall execution cap with bounded group cleanup. A failed phase
never permits private continuation. Keep outer launcher stdout/stderr outside
reports/normal_001 so the launcher cannot modify its own sealed public files.
Failure records and partial runtime are preserved; ordinary implementation
repairs, if needed, use a separate successor instead of rewriting launched bytes.

[DERIVED limits] This is an arithmetic/durable-service join on a known fixture.
All16 recipient credentials retain the full per-input fixed row span, including
expired inputs. Software selection, dedup and expiry do not attenuate that
authority. Honest registration, concrete SHAKE execution, conditional classical
ROM/DDH, Ed25519, compiled runtime correspondence and shared-OS trust remain
distinct assumptions. Timing-bearing genesis records are outside the mathematical
transcript guarantee. No selected-only release, operator isolation, hidden
nonlinear learner, physical erasure, QROM/PQ or timing-privacy claim follows.

[OPEN execution] Normal execution and its public/final results will be recorded
under reports/normal_001. Root owns shared ledgers and commits.
