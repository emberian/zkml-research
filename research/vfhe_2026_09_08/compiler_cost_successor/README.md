# Share repeated assertions in the generated rescale relation

[EXECUTED] The actual six-to-three rescale template now contains **39,329 arithmetic constraints instead of 45,209**: **5,880 repeated assertions removed, a 13.0% reduction**. Its 37,665 witness columns, all 88 public coordinates, exact-public lookup and existing backend parameters remain unchanged. Both original and optimized templates prove the same frozen 32-row workload covering 24 actual BFV coefficient positions. Fresh verification accepts the optimized proof and rejects a changed output.

[DERIVED] [AirAssertionShare.lean](Compiler/AirAssertionShare.lean) adds a reusable source pass before the existing `Signature.fold` IR2 emitter. `share_preserves` proves acceptance equivalence for every assignment and every source constraint system. `optimize_preserves` composes this with the existing `AirSimplify`; `emitShared_accepts_iff` and `emitShared_forces` connect the pass to the existing descriptor emitter. [BfvRescaleAssertionShare.lean](Compiler/BfvRescaleAssertionShare.lean) applies it to the actual frozen rescale source, preserving its existing exact integer `rowSound` conclusion with no new arithmetic premise.

[SOURCE] The previous IR2 exporter folded `simplifySystem system` directly. The separate flattened descriptor's CSE did not remove repeated source assertions from that path. In the rescale source, both sides of each weighted balance impose the same range checks on their shared result wires: twelve source balances contribute `12×57×7 = 4,788` duplicates and six target balances contribute `6×26×7 = 1,092`. The emitted full-syntax count independently confirms their sum, 5,880. The weighted/expiry templates inspected initially each had 336 duplicate assertions; those counts informed the choice, but the proof workload here is the actual rescale instance.

[DERIVED] The pass builds a complete ordered syntax key by a fold of the existing AIR signature. A tail-recursive hash-set walk retains the first occurrence of each key and restores its source term. The key includes operation, field constants and variable identity. Hash collisions are resolved by full equality; the equivalence theorem accepts arbitrary lawful equality and hash implementations. Kernel-checked controls use colliding hashes for two distinct variable assertions, show that exactly one repeated assertion is removed, exhibit a nonzero accepted witness and emitted-premise inhabitation, and refuse a changed distinct variable. A deliberately dropped distinct assertion accepts that bad assignment, making the control meaningful.

[EXECUTED] Two proof modules, 277 lines including **16 exact guarded axiom outputs**, pass Lean 4.30.0. The count includes the lawful-equality instance as well as every authored theorem/helper. Only `propext`, `Classical.choice` and `Quot.sound` occur. Final checks are [share-final.log](results/share-final.log), [rescale-final.log](results/rescale-final.log) and [patched-emitter-final.log](results/patched-emitter-final.log). No full project build was duplicated; maintainer integration remains the project-wide gate.

[EXECUTED] One clean sequential native pair, baseline first, measured **588.002 ms → 565.346 ms** for proving, and **292.111 ms → 291.131 ms** for self-verification. This is a modest observation from one pair, not a distributional speedup estimate. Proofs remain about 8.59 MB because the trace width is unchanged. The initial pair, 619.313 ms → 765.548 ms, is retained separately: its after-side overlapped this lane's Lean check, so one clean pair was run after compilation finished. No benchmark grid or soundness-parameter change was made. Exact command/output/resource records and proof bytes are under [results/](results/); [RESULTS.json](RESULTS.json) collects both pairs.

[SOURCE] The unchanged native binary is `proved_rescale/target/release/vfhe-proved-rescale`, SHA256 `11bdb1c15236ac169a156a742207d7d665a991687b056215cc2d0f602ea41239`. Both sides read the same `proved_rescale/results/case001/` and `arithmetic_rescale/artifacts/trace.leu32`. The old template SHA256 is `842a74f0a9950243847bf10427ce5394ad5314eb48488aeedeb68abccca55822`; the new one is `a37f98a7a088665fae369a6ab498cc97b213221812040406147a3e80a70e8e38`. The baseline source/export/proof packages stay unchanged.

[EXECUTED] [EmitSharedRescale.lean](EmitSharedRescale.lean) emitted both templates through the same serializer. The baseline re-emission is byte-identical to the frozen artifact. [check_emission.py](check_emission.py) verifies that the new template is exactly stable full-AST assertion sharing, with every other JSON field and the public lookup unchanged. It also checks the preserved native binary and proof/refusal outputs. Run `python3 check_emission.py` here to reproduce those checks.

[EXECUTED] [assertion-sharing.patch](assertion-sharing.patch), SHA256 `fef25bcad5366132bead0b0ac1e0cac2dc5b37689655360c60cc25d854ac04d5`, adds the two compiler modules and changes the existing rescale exporter to use the proved `sharedSystem`. It introduces no handwritten AIR. A temporary-tree apply/reverse check reproduced every proposed source byte. To use the generic pass in another existing exporter, import `Compiler.AirAssertionShare` and replace `simplifySystem system` by `AirAssertionShare.optimize system` before its existing fold. The standalone two-template emitter is experiment evidence; the proposal modifies the original exporter.

[EXECUTED] One requested liveness count found only **211 unused columns out of 37,665 (0.56%)** in this exact rescale template, including all arithmetic and lookup references. [column-liveness.json](column-liveness.json) records the indices. Simple liveness compaction therefore offers a small gain here. Uniform result/carry widths and carry-bit ranges remain the larger possible next seam; this package does not narrow them or assert that the newer nine-to-four layout has the same count.

[OPEN scope] The Lean result preserves the source relation and its existing emitted-descriptor refinement. JSON emission and the unchanged Rust backend were exercised, not formally refined here. The workload is the existing small BFV rescale fixture, with its existing convolution/provenance and security boundaries. This compiler change supplies no new end-to-end vFHE soundness or security label.
