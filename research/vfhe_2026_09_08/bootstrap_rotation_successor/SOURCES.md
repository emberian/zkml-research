# Exact source boundary

[SOURCE] Primary TFHE1.6.3 source: `/Users/ember/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/tfhe-1.6.3/src/core_crypto/algorithms/polynomial_algorithms.rs:544–583`, `polynomial_wrapping_monic_monomial_div`. It computes `remaining_degree=b%N`, `full_cycles_count=b/N`, and either copies or wrapping-negates the two slices depending on cycle parity. This is the exact function called for each accumulator polynomial by `core_crypto/fft_impl/fft64/crypto/bootstrap.rs:314–325` after reading the modulus-switched body.

[DERIVED, kernel checked] For `0≤j<N=512`, the slice source is `(j+b)%512`. `library_sign` proves that the source's cycle parity plus its tail-slice toggle equals `((j+b)/512)%2`. The row theorem lifts the signed coefficient rule with explicit native-u32 carries. The new relation does not substitute a prime modulus or a power-basis/NTT conversion.

[SOURCE/EXECUTED] The prior genuine capture and modulus-switch proof are frozen in `../bootstrap_successor/MANIFEST.json`, SHA256 `8ea53aba3a790148e1e912f1b0bfbc5458662f253af33cb6336b416eb9d45f1f`. Its native source constructs the Boolean accumulator, then calls this exact library monomial-division routine. `fixtures/normal_001/join.json` records six origin-to-copy hashes; the joined consumer reconstructs its statements from the copied native ciphertexts and prior proof inputs. No other fixture or stopped artifact is read.

[SOURCE] The construction motivation remains eprint2026/1127, §§2.4.2 and5.2, read from the local mirror in the first package. Its blind-rotation decomposition begins with monomial rotation and an external product. This successor extends the proved integer prefix and does not instantiate the paper's prime-ring folding protocol. See the first package's `SOURCE_SCOPE.md` for the inspected construction and runtime mismatch. This lane used zero web/Kagi/Scry searches and no new PDF fetch; local source discovery used `rg`, `sed` and direct reads.

[SOURCE] The compiler imports the already proved first modulus-switch package and uses existing `rangeGadget`, simplification and term-fold interfaces. The proof runner links the existing `dregg-circuit` descriptor backend and shared public-preprocessing adapter. All selected source/dependency/executable hashes were recorded before the new proof in `execution_pins.json`; the complete source graph is identified further by Cargo.lock and the predecessor's source pins. The final integrity report checks those recorded files without another cryptographic execution.
