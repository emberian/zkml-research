# Exact continuation point

[DERIVED] The main task has a complete candidate answer in PROPOSAL.md:
replace direct sampling by verified capped hash-to-field lists, preserve
random-sign root simulation, and pay a padded seed-index factor N. The
displayed registry must be honestly independent and fixed before grinding.

[OPEN immediate] Independently review these four load-bearing steps before
promoting the candidate theorem:

1. Conditional product-uniform law of `(tau*,U*)` after revealing the fixed
   registry and coalition scalars, and its preservation until first touch.
2. Finite `P_u(W)` tape lemma on both successful and all-rejected lists.
3. Exact event identity `Pr[B_i=1|b]=Pr[A=1,S=i|b]`, common setup rejection,
   and signed averaging to the `2*M*N` DDH coefficient.
4. Pre-registration Bad event and `Q_pre/q^m` bound under the exact complete
   registry encoding; no key reuse or selective registration resampling.

[OPEN later, different work] A concrete hash/XOF suite would need a precise
byte encoding, backend assumption, raw-query transcript and implementation
review. That is not permission to alter the frozen setup/journal or launch
a crypto run. QROM, adversarial registrations, multiple deployed challenge
keys, recipient key-use proofs and physical timing remain separate tasks.

[EXECUTED resume data] finite_witness.py and finite_witness.log retain the
public arithmetic; SOURCE_PINS.json records dependencies; SOURCES.md records
all access levels and counts. The ignored source text can be regenerated
from the read-only local mirror. Root owns any integration, ledgers, commit
and VERDICTS proposal. No commit was made by this lane.
