[AUTHORIZED OUTCOME] One fresh two-class live nonlinear teaching/query run, using the exact existing degree-8192, four-prime profile. This directory contains one continuing implementation; previous sources, parameter choices, cases and proofs remain frozen.

[SOURCE] Initialization creates a fresh public/evaluation/full-reader key set and canonical literal coefficient-zero ciphertext. That native setup, public text encoding and key membership are trusted. Each teaching issues one encrypted example into its class's next cyclic lane; the generated paired-MAC source forces out=acc+fresh−old for both components, all four primes and all 8,192 NTT positions. The durable journal supplies exactly the oldest committed issued ciphertext on a full eight-example FIFO, otherwise literal zero. A teaching becomes visible only after eight fresh proof checks and an authoritative parent check within SQLite's write transaction.

[SOURCE] A query snapshots the accepted global revision/model root and all active classes. Every class produces 116 fresh proofs: 88 packed multiplication/rotation/reduction, four basis extension, eighteen tensor product and six rescale. Every class must pass the complete fresh consumer, with exact model/query/evaluation-key/output identities and complete position coverage. No saved ciphertext/proof is configured or reused. Only after all class checks and the current-head check under the journal write lock does the gate durably publish public acceptance. The first full-reader command follows that acceptance. An omitted active class or stale head cannot satisfy this predicate.

[FIXED INITIAL EXECUTION] fixture_builder/fixture.json fixes two new teaching texts (one per class, lane0), one new ATM-fee query and exactly one fresh E5 batch of three texts. No tuning, parameter changes, alternate examples or benchmark sweep. Public class kernels are produced and proved sequentially; one native host at a time, four Rayon workers in proof processes. Maximum per-step times are explicit in pipeline.py; any failure/timeout stops the run without retry or private decoding of failed outputs. Each class has one full reader call after acceptance, two calls total. Compare all sixteen returned kernel lanes and their two sums, then the two-class ranking, with the exact plaintext arithmetic after those reads. Report intended-label success as observed, with no substitution if it fails.

[SOURCE] The same Live object/CLI supports later supplied text teachings and queries against the durable accepted model, including exact eight-entry FIFO expiry. This small run does not execute an expiry or a utility benchmark. New request IDs and bounded fixed-profile feature vectors are required; class set is fixed at initialization. No concurrent hostile-writer or reader restriction claim is made.

[PHASES] Freeze code/profile/helper inputs before launch. Retain public command results, generated proof files, model/query acceptances and hashes as each step finishes. All arithmetic proof checks finish before reader invocation. After private receipt, only expected comparison and read-only cost/provenance collection plus report writing remain; no additional public cryptographic operations or negative grid are planned. Raw witnesses are removed only after successful proof generation and compressed transport identity retention, under ignored work/. Large live state/proof files stay in ignored runtime/ with report references; they are not pasted into Git.

[SCOPE] Actual fresh two-class nonlinear classification query under explicit public encoder, encryption/initial setup, raw parser, public seed expansion, NTT/linear-map, proof-backend and durable-controller TCB. The full reader remains. This is not a private resident, cryptographically restricted recipient, BFV noise/security proof, shipped system, or general accuracy claim.
