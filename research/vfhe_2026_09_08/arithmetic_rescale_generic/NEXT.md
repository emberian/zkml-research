[OPEN] Reuse this generic frontend for a larger explicitly selected batch or full24,576-position rescale after assessing actual memory/cost. Reuse the existing assertion-sharing pass in a new artifact path. Preserve current JSON and trace hashes.

[OPEN] Join the extended product coefficients to a compiler-derived convolution/NTT relation and then the output transform. The current actual-constant/native-word bridge does not discharge those operations.

[OPEN] For scaling factors with nonzero native theta_gamma, add its v-dependent signed correction to the generic source and word-model theorem. Current exact-gamma family intentionally scopes that case out.
