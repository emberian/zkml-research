# Independent initialized-reference proof review

[DERIVED verdict, 2026-09-08] **Accepted at the stated Lean/source scope.** The new package proves preservation of the existing `ForwardValid` predicate through the existing CSE pass for F2 descriptors, derives that predicate for every emitted F2 term list, and specializes it to the actual frozen Learn and Infer descriptors. Its separate Option-store theorem establishes lookup presence after arbitrary payload writes. It adds no gate evaluator and proves no Rust or TFHE refinement.

[EXECUTED identity] The independently checked author manifest is SHA256 `1c8d73ab40a06bde6dbed4b045d78d67d7fced01c5013141047ac40dc305c5c5` (15 files). [Source](../../../formal/cse_initialized_references/Compiler/CseInitializedReferences.lean) SHA256 is `1e8037c2c4ce35727a804912560b13a89a673682a8ca7405f2d1d4504d884386`; [patch](../../../formal/cse_initialized_references/minidregg-cse-initialized-references.patch) SHA256 is `39afffcab611adc4a3a292bd3935ea551ae5f22e264660c9d2c0523256c74c6c`. The checker rehashed all author files, 96 frozen predecessor files, four proposal dependencies and four reused companion sources. It independently applied the exact patch in a temporary directory under this review package: the only Lean changes were the byte-identical new module and its umbrella import. Companion HEAD, status and umbrella bytes remained unchanged. Evidence: [evidence_results.json](evidence_results.json), [actual checker stdout](evidence_check.log).

## CSE substitution and emission coverage

[SOURCE/DERIVED] The statement is `CseInitializedPreservation` at source line 20, using the existing predicate defined in `EmittedScheduleExecution.lean:49`. It is not a new acceptance predicate. `InitGood` at line 34 carries four necessary facts: future keys remain unmapped, every available original wire maps to a retained output or base input, signature hits name kept gates, and every kept gate operand has retained/input support. `cseGo_initialized` at line 154 follows the actual existing `cseGo` hit/miss cases from companion `Compiler/EmitShare.lean:125`.

[DERIVED proof audit] A hit substitutes the current output with an output justified by signature support; strict growth prevents that insertion from changing an earlier available key. A miss retains the gate with already supported substituted operands; future-key freshness makes its own newly available output map to itself. No assumption that arbitrary HashMap values are initialized is hidden in the conclusion. The theorem explicitly requires `InitGood`, and `empty_initialized` supplies it for the actual initial CSE state. The final argument also transfers output roots through the final substitution.

[DERIVED proof audit] Retained-reference membership alone is global and would not establish evaluation order. `cse_initialized_preservation` at line 236 obtains order and bounds from the separately proved `cse_structure_preservation`. `ready_head` and `ordered_refs_forward` at lines 55 and 72 then exclude a referenced producer at the current or a later output using the strict operand bound. This recovers prefix availability. `shape_refs_valid` at line 100 correctly retains an explicit `AllRefs` premise; it does not identify allocation bounds with initialization.

[SOURCE/DERIVED] `flattenSystem_covers` at line 107 lifts companion `Compiler/AirFlatten.lean:449` (`flatten_covers`) over the emitted list, splitting each allocation interval at the current expression's next index. It does not replace the existing recursive term compiler or reimplement its gate semantics. `raw_covers` at line 120 shifts auxiliary indices by m using the actual `emitGate` layout. `raw_bounded_ready` separates the original input prefix from those produced auxiliaries. Together with existing raw SSA/bounds, this gives `raw_forward_valid` at line 140 and then `emitted_initialized` at line 260.

[SOURCE/DERIVED actual consumers] `learn_valid_kernel` and `infer_valid_kernel` at lines 264 and 267 apply that universal theorem to the existing `learnTerms : List (Expr 42)` and `inferTerms : List (Expr 34)`, through the existing `shared = cse ∘ raw` definitions. Their `validCheck ... = true` conclusions are obtained from the existing proved `validCheck_iff`; they are not imported compiled Boolean results or fresh finite-normalization checks. Thus the actual Lean descriptors have `ForwardValid` without an initialization premise. Their connection to saved JSON and Rust remains the predecessor's emission evidence and the runtime boundary below.

## Arbitrary payloads, witnesses and the hole

[SOURCE/DERIVED] `LookupSuccess` at line 285 quantifies over `α : Type`, input values, a total gate-payload function and a constant interpretation. For every decomposition into completed gates, the current gate and a suffix, both current operands read `Some`; final outputs also read `Some`. `valid_lookup_success` at line 349 derives this from `ForwardValid`. The actual Learn and Infer conclusions at lines 364 and 367 discharge that premise using emitted initialization.

[DERIVED scope] `prefixStore` is only a fold of `Function.update` that writes `some (payload g)` at `g.out`. It never examines the gate operation or computes an operand-dependent result. `readOption` returns a supplied constant or looks up a wire. Consequently this is a universal statement about storage presence, independent of value semantics, conditional on the modeled writes occurring. A backend can still fail to produce a payload or disagree about its value; this theorem does not address either event. The total functional store is not a finite Rust vector.

[SOURCE/DERIVED nonvacuity] The two keystone propositions precede their proofs. `raw_forward_valid` inhabits the CSE preservation premise for every source list, and actual Learn/Infer descriptors instantiate the conclusion. For the storage statement, Bool inputs/payloads/constants are available inhabitants; it does not gain its practical force only from an empty type. The hole descriptor at line 370 has no input slots, two allocated slots, and a gate writing wire 1 after reading wire 0. `hole_has_shape` proves SSA and allocation bounds. `hole_not_initialized`, `hole_lookup_fails` and `hole_lookup_success_refuted` separately prove failed forward validity, the actual `None` read, and failure of the quantified lookup property. This is a substantive falsifier for dropping producer coverage while retaining shape and bounds.

[EXECUTED finite public controls] [presence_check.py](presence_check.py) reads the unchanged frozen JSON schedules and inspects addresses only. Learn has 42 inputs, 538 writes and 32 output reads; all 1,108 operand/output lookups succeed. Infer has 34 inputs, nine writes and one output read; all 19 lookups succeed. Both pass with four unrelated payload maps, including tagged `Some None`, without evaluating a single gate operation. Their 10,982 and three unused allocation slots illustrate why holes may exist while referenced holes are forbidden. The two-slot falsifier fails precisely at unproduced wire 0. These finite checks corroborate the actual public artifacts; the universal claim comes from Lean. [Results](presence_results.json), [stdout](presence_check.log).

## Axiom census and remaining trust

[EXECUTED independent census; REPORTED kernel execution] The independent lexical census finds exactly 29 theorem declarations and exactly 29 matching `#guard_msgs`-pinned `#print axioms` commands, agreeing with the author's census. Comments and strings are excluded from forbidden-token checking. There are no `sorry`, `admit`, new `axiom`, `unsafe`, `native_decide` or `implemented_by` identifiers. Two pins are axiom-free; four use only propext; twelve use propext and Quot.sound; eleven also use Classical.choice. These are explicit standard Lean dependencies, not an axiom-free claim for the package.

[REPORTED/EXECUTED evidence binding] Saved [lean_006.json](../../../formal/cse_initialized_references/results/lean_006.json), SHA256 `ceba811f8783c1c4f14705e92260e98080734d687059269606fdf6271308baa8`, records the exact final source hash before and after, exit 0, empty stdout/stderr and 9.213185625 seconds for the isolated guarded check. Its four proposal dependencies have saved successful fresh compile commands and hashes; companion and external dependency oleans were reused. This reviewer verified those saved bindings and both saved import-boundary results, and ran no Lean command or redundant whole-tree build. Root owns full integration.

[OPEN TCB] Lean emission/JSON bytes, parsing and schema tags, serde, dimensions and resource caps, Rust `usize`, `Vec<Option<_>>` allocation/indexing/borrowing/cloning, Lean/Rust compilation, backend completion, TFHE semantics and ciphertext serialization remain outside this proof. It adds no replay-determinism, noise, learning-utility, confidentiality or post-quantum result. Existing value correctness remains in the predecessor theorem chain. This review used no cryptography, private artifacts, web queries or Scry queries.
