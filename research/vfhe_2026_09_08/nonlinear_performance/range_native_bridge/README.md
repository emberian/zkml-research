# Native nibble ranges reach the compact MAC and Infer theorem

[DERIVED] The actual four-bit decomposition loop now has a Lean range equivalence for widths **2, 9, 11, 14, 15 and 16**. `NativeNibbleRange.Equations` follows the native loop's recomposition and partial-top Boolean equations; `sent` follows its byte-bus sends. `supportedSound` obtains the canonical BabyBear bound from those equations and the support of the sent keys. `NativeNibbleComplete.complete` constructs auxiliaries for every bounded input. Consequently the native relation preserves the existing source's accepting set, rather than strengthening it with an uninhabited premise.

[SOURCE] The source is the pinned `/Users/ember/dev/breadstuffs/circuit/src/descriptor_ir2.rs`: `LIMB_BITS` at line 468, geometry at 2070–2083, `eval_decomp` at 3965–4000, and its unconditional calls on **every row** at 4360–4365. The full-top16 branch sends all four nibbles. The partial-top9 branch sends two nibbles and constrains its third limb with one Boolean auxiliary. This proof does not instantiate the old partial-top emitted helper as a proof of full-top16. Exact hashes and inspected locations are in `SOURCE.json`.

[DERIVED] `NativeNibbleBus.field_sound` derives individual send support from **global exact field histogram conservation** against the sixteen canonical table values, plus a per-key send-count bound below the BabyBear characteristic. Receive multiplicities remain arbitrary natural representatives of field multiplicities; they are not required to be small. The natural-histogram variant also holds. These are algebraic extraction lemmas, not a proof that accepted grouped LogUp constraints imply histogram equality.

[DERIVED] `NativeRangeKeyswitch.allRowsSound` combines that bus result with native decomposition equations and the existing compiler-derived arithmetic assertions for every row. It concludes the existing integer MAC law for all four BFV primes. Its `semanticLookups` supplies the faithful-table `LookupChecked` proposition used by the earlier range bridge; its `inferSound` reaches the unchanged `BfvInferContract.result`, retaining the existing square and transform premises. `nativeRanges_iff` and `checked_iff` prove equivalence to the earlier range/source acceptance predicates. No new arithmetic AIR, executor or witness plan is introduced.

[DERIVED] The compact source sends exactly **524** nibble keys per row. `allSends_length` proves the total is `n * 524`. For any trace of at most **131,072** rows, `noWrap_of_domain` discharges the per-key count bound using the total upper bound **68,681,728**, below **2,013,265,921**. This is a sufficient mathematical domain bound, not a new measurement of a runtime trace or a security-bit estimate.

[DERIVED] Nonzero witnesses accept 511 at width9 and 65535 at width16. The omitted-top-send falsifier retains the native recomposition equations while admitting an invalid full-top value; the actual four-send relation refuses it. A one-row terminal overflow is refused. The bus witness is nonzero, and a separate symbolic theorem shows why the field count bound matters: a characteristic-sized list of invalid key16 sends balances against empty receives in the field. Completeness additionally supplies native auxiliaries for every existing bounded source assignment, so the bridge does not rely solely on these finite examples.

[EXECUTED] Four individual Lean modules passed with **54 exact guarded theorem footprints**, using only the standard axioms named in their guards. `CHECKS.json` identifies the final commands, source hashes and retained logs. The additive `native-range-bridge.patch` passes `git apply --check` against minidregg base `6937394e1dc2c2aaff986c7d4b3a258aca5d16fd`; the companion import-boundary script passes. All additions are in `Compiler/`. No companion source, frozen formal/backend/runtime artifact, or prover process was changed. No cryptographic replay was run.

[OPEN] A native proof's acceptance must still be connected to these exact local field equations and to exact byte-bus histogram conservation, or to explicitly priced failure events. This includes PCS extraction, grouped LogUp's probabilistic reduction, its nonzero-denominator/batching conditions, Fiat–Shamir, the coherent accepted byte-table rows and the verifier's pinned table height. The existing `ByteTableEmit.value_is_the_row_index` and `served_key_is_canonical` establish the table-source fact; Rust parsing, layout offsets, `Vec` access and execution refinement are not proved here. Source-level arithmetic and range correctness should not be reported as a completed native-verifier soundness theorem.

The patch is additive after the sibling compact range and backend Lean patches and their frozen MAC/Infer prerequisites. Reproduce the bounded module checks, in dependency order, with `python3 check_one.py NativeNibbleRange`, `NativeNibbleComplete`, `NativeNibbleBus`, then `NativeRangeKeyswitch` from this directory. The runner uses isolated output files and the existing frozen dependency build overlays; it never writes to minidregg.
