[PREDECLARED] `fixture.json` fixes two teaching texts (`card_arrival`, `cash_withdrawal_charge`) and one query before model execution. Each class receives one lane0 observation; there is no expiry in this minimal initial run. The intended query label is recorded to compare with the actual result, without tuning or an accuracy estimate. An exact-string check found none of these three texts in the earlier selected training/test files; no semantic score was computed during preparation.

[SOURCE / DERIVED] `TextEncoder(cache)` reuses the frozen local E5 encoder and keeps it loaded across calls. `.encode(list[str])` returns 576-integer rows using the existing projected/quantized map followed by `numpy.rint(first576/4)`, with coordinate bound32 and squared-norm bound20000. `.stats` exposes existing issuer timing/counts. Caller-owned caches may persist across restarts. This API neither predicts, changes model state nor decrypts. `encode --out NEWDIR` uses that same implementation for exactly one fresh three-text batch and records vectors/statistics. No alternative encoder or feature map is introduced.

[SOURCE] `native_argv` exposes the pinned existing native `keygen`, `issue`, `learn`, `infer` and explicit `read` command arguments. The enclosing driver owns launch, process lifecycle, accepted-state selection and private-phase authorization. Create a fresh issuer directory before `keygen`; its `zero.ct` is literally the canonical difference of a fresh ciphertext with itself. `issue` takes a 576-integer vector file and lane0–7. `learn` performs `acc+fresh`, optionally subtracting the old lane ciphertext. The durable journal owns queues/counts/lanes, so later teaching may use the same commands with exact FIFO expiry. No automatic read or receipt release occurs in this helper.

[SOURCE] `capture --issuer DIR --model-ct ACCEPTED_CT --query QUERY_JSON --out NEWDIR --revision REV --label CLASS` runs only the existing public native Infer via the shared `common.command` lifecycle. It emits fresh `descriptor.json`, `basic.json`, `basic.dot.ct` and `basic-000.ct`, plus public command/capture records. The native source captures actual pre-/post-directed-rescale PowerBasis arrays and checks its output against the existing Multiplicator. The descriptor binds the exact model/query/evaluation-key identities and caller-supplied revision; it does not itself establish journal acceptance. These are fresh producer artifacts for the whole Infer proof pipeline, not references to an old ciphertext fixture.

[SCOPE] The text/features and trusted encoder are plaintext. The existing BFV full reader key remains. The shared driver must accept all required update/query proofs and close public work before calling explicit `read` for the two class outputs. The source pins cover eight helper/native/encoder sources and nine public model files; the enclosing driver separately pins `common.py` and its phase policy. Preparation ran only ordinary hashing, exact-text comparison, AST parsing and `--help`: no model forward, crypto operation, key read or private comparison. The initial helper revision is retained under `preparation_history/`; active pins are `SOURCE_PINS.json`.
