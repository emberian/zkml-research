# Exact dynamics extension of PrivateAddressEma

[DERIVED statement-first target] Reuse the checked source operation
`Minidregg.Theory.PrivateAddressEma.candidate`, `learn`, `history` and `infer`.
No alternate learner, Boolean executor, compiler artifact or crypto operation
will be authored. The new module proves useful consequences of the existing
word-level arithmetic theorem.

[DERIVED keystone] From every state satisfying `Within120`, six consistently
labelled updates at one selected address force the corresponding inference
sign. Arbitrary updates at other addresses may be interleaved. Unrelated
cells that are never targeted retain their values. A count of at least six
selected events is required; route-wide counts do not substitute for it.

[DERIVED ATLAS obligations] Before proof work, declare the keystone `Prop`.
Use zero or endpoint byte states and labels ±120 to inhabit its premises;
provide a nonempty satisfying subject. Five updates from -120 with +120
leave -4, and five from +120 with -120 leave +2, supplying sharp sign
counterexamples. These are theorem statements, not `#guard` unit tests.

[DERIVED bounded supplement] If feasible within the hour, prove exact
constant-input fixed-point asymmetry and one-step signed-floor residual
bounds. Keep real EMA asymptotes distinct from signed-floor dynamics.

[EXECUTED workflow] Read companion CLAUDE.md/ATLAS.md and the existing
PrivateAddressEma proposal. Use a private source/olean overlay under this
directory and its cached Lean 4.30.0 dependencies, never writes to either
companion tree. Preserve single-file attempts, exact axiom pins, named source
hashes, fresh patch application and import-boundary checks. Root owns umbrella
integration. No source-web or Scry calls are needed for this proof tranche.

[OPEN scope] The inherited module is a standard BitVec semantic specification,
not a universal Rust/TFHE refinement theorem. This extension inherits that
boundary and does not claim cryptographic privacy or removal of the reader key.
