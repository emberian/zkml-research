# Next

No native arithmetic work remains here. Preserve this package. Support the
query_gate_successor's first substantive pipeline execution if an integration
issue appears; any emitter acceleration is opt-in through an additive version
and must preserve the existing source/template/trace semantics.

The service owns request/recipient/accepted-head checks, all-class proof coverage
and proof-before-private-receive ordering. This package never decrypts.
