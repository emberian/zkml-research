# Native grouped LogUp trace equation

[SOURCE] Read the unchanged local Plonky3 lookup implementation at the paths and hashes below. `logup.rs:216–251` enforces an exclusive prefix: the first row has `s[0]=0`, transitions through every row before the last use `(s[next]-s[row])*D=N`, and the last row uses `(c-s[last])*D[last]=N[last]`. The final row is not dropped and there is no assumed wraparound transition. `logup.rs:314–323` rejects unless the sum of all cumulative values for the global bus is zero. `logup.rs:566–569` describes the honest exclusive-prefix producer; the theorem does not assume that honest producer was used.

[SOURCE] `logup.rs:135–145` builds the common denominator and signed numerator. The existing frozen `Compiler.LogUpGrouping.transition_iff` supplies the four-term division identity only when every denominator is nonzero. `bus.rs:55` sends with positive multiplicity; `bus.rs:75` negates the table receiver multiplicity. The sixteen-row byte table has one such receiver, and the compact MAC source has 524 unit sends arranged in 131 consecutive groups of four, recorded in `../../backend/checks/010-compact-shape.json` and `../../backend/README.md`. This task did not regenerate the runtime or rerun native proofs.

[SOURCE] Challenge timing is not interchangeable: `batch-stark/src/prover.rs:213–244` commits the main traces, then samples lookup challenges, then produces permutation traces. `batch-stark/src/transcript.rs:74–102` shares lookup challenges across contexts on the same global bus. `:104–118` observes the permutation commitment and cumulative values afterward, before sampling the distinct constraint-folding challenge. Therefore permutation values and cumulative values may depend on both lookup challenges. The source's later constraint-folding `alpha` must not be conflated with LogUp's denominator challenge also named `alpha`.

[DERIVED] `Compiler/GroupedLogUpTrace.lean` defines source-shaped `Gates4` and `Gates1`. `telescope` and `inclusive_sum` derive the total from the first, transition and final gates. `sound` and `single_sound` remove denominators under explicit nonzero conditions. `native_byte_sound_general` then derives rational global balance for any source-selected number of fixed-four unit-send columns and the one negative receiver on all sixteen table rows; `native_byte_sound` specializes it to the MAC’s 131 columns. Global rational balance is its conclusion, not a premise. Auxiliary traces are unrestricted inputs; `adaptive_trace` explicitly permits arbitrary challenge-dependent trace and cumulative-value functions.

[DERIVED] The witnesses exercise positive and negative nonzero contributions. A full native-sized premise includes 131 sender columns and all sixteen receiver rows. Its arbitrary resolved denominator values witness this algebraic boundary; it does not itself witness canonical tuple compression or an actual proof. Omitting the final-row equation allows cumulative zero despite a positive trace total. Omitting the final global cumulative check allows individually valid columns with a nonzero aggregate.

[OPEN] This module does not establish extraction from an accepted STARK proof, low-degree testing, commitment binding, fixed pre-challenge main-table values, Fiat–Shamir/QROM security, or the probability of denominator collisions or nonzero histogram defects. It only removes a previously assumed rational-balance equality from the mathematical native-row boundary. Those other bridges remain separately named.

## Source pins

- `/Users/ember/dev/zkml-research/research/vfhe_2026_09_08/nonlinear_performance/backend/p3-lookup-grouped/src/logup.rs` — SHA-256 `f525316cb028409896ff0aac59bd6d95da0ff29cb430f5b1339d02ad730fd754`.
- `/Users/ember/dev/zkml-research/research/vfhe_2026_09_08/nonlinear_performance/backend/p3-lookup-grouped/src/bus.rs` — SHA-256 `2f1f541321341f2a31a18ae324f5cb0e6a36d59692327ad90c90a060c9ee34a2`.
- `/Users/ember/.cargo/git/checkouts/plonky3-7d8a3b21a665a86f/82cfad7/batch-stark/src/transcript.rs` — SHA-256 `c87529d6f569bec6f88016eef7b931ba2f0517c7e641e6679972b2a1ecaf681e`.
- `/Users/ember/.cargo/git/checkouts/plonky3-7d8a3b21a665a86f/82cfad7/batch-stark/src/prover.rs` — SHA-256 `9c57a99fd5fe3736376f0846d7771b04c43539f6a8dc16bc29e499c07d46d070`.
- `/Users/ember/dev/zkml-research/research/vfhe_2026_09_08/nonlinear_performance/backend/Compiler/LogUpGrouping.lean` — SHA-256 `7442ee4b9220df0d10ceb7f276f2b6556db033ae7cfc0ba9cbdcabcbcdc7b6d8`.

[EXECUTED] `python3 research/vfhe_2026_09_08/nonlinear_performance/range_native_bridge/grouped_logup/check_one.py GroupedLogUpTrace` passed at `checks/GroupedLogUpTrace_006.json` with exit 0. All 12 theorem declarations have exact guarded axiom footprints. Frozen Lean source SHA-256 `e5bd043f446970f457f23be6c65cfc438b5784ef1fbf97cf2580565a80f41418`.

[DERIVED] The companion `GroupedByteFractions.lean` defines `sendListN` in group/row/limb order and proves `send_rationalN` and `receive_rational`: its list inverse sums equal the source-selected number of native four-term sums and the sixteen multiplicity-weighted receiver terms, respectively. `mem_sendListN` derives inclusion of each sent key directly from the flattening. The original `sendList`, `send_rational` and `mem_sendList` APIs specialize this single path to 131 groups. These are unconditional finite-sum identities and introduce no global balance premise.

[EXECUTED] `python3 research/vfhe_2026_09_08/nonlinear_performance/range_native_bridge/grouped_logup/check_one.py GroupedByteFractions` passed at `checks/GroupedByteFractions_005.json` with exit 0. All seven theorem declarations have exact guarded footprints. Frozen source SHA-256 `1c6e0851b13346cd92f8681fb19234da27d6ed45114510961b73b79869698a5e`.
