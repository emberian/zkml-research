# Complete the running system

[REPORTED] The latest instruction is to build the whole system, fix its bugs and evaluate what it accomplishes. Do not restart a chain of small formal or documentation milestones. Companion trees and VERDICTS remain read-only.

1. Observe the existing lifecycle process, not a replacement run. It adopts the HTTP-initialized instance at `continuing_system/runtime/lifecycle/resident`; its controller is adjacent `resident.workload/`. Twelve teachings, four complete two-class queries, FIFO expiry and process restart are scheduled. Resume only after a terminal failure using `run.py resume` on the same root. The local application at `http://127.0.0.1:8847` reports actual progress.
2. Fix concrete whole-run failures while preserving completed requests and failed attempts. Evaluate exact arithmetic, useful classification, costs and surviving capabilities together. The fixed plaintext held-out results are20/20 →19/20 →18/20, with identical linear-comparator accuracy; do not claim learning or nonlinear utility gains absent from those observations.
3. The cost owner is building an optional complete linear-score backend in `continuing_system/linear/`, leaving the active quadratic workload unchanged. Removing the square computation is an architectural alternative justified by the comparator, not a parameter sweep. Do not launch its proofs concurrently with this lifecycle.

[SOURCE completed support] The native range/LogUp and nearby-column AIR/PCS theorem packages are complete and held for batch integration. Keep their native extraction, challenge, hash and Fiat–Shamir residuals explicit. No further theorem chain is queued. Published browser examples already use corrected whole-row proofs; refresh publication after the complete system outcome.

[OPEN privacy] The executable learner retains its full reader. The source-backed iO continuation and post-quantum provenance-certificate constructions remain conditional, with no practical private-resident implementation or efficient indefinite renewal. Do not revive exposed-writer routes, the flat ACE splice, or stopped TensorSketch/restricted-ring/full-PBS campaigns.
