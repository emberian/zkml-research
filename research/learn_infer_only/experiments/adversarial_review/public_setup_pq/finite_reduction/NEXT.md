# Handoff

[OPEN immediate] Finish the independent review against the frozen theorem
hash in `STATUS.md`. Integrate its report by reference; do not mutate the
earlier frozen construction, fixed-coordinate or quantitative regularity
notes. Root owns the global status, publication wording and commits.

[OPEN finite implementation boundary] To turn the conditional finite
statistical ledger into an actual finite-bit QPT reduction, give a sampler
and a tuple-level discrepancy/resource certificate for `δs`. The necessary
accuracy covers all anisotropic discrete sampling, covariance and square-root
computations, continuous intermediate draws, truncation and rounding. A
sampler's marginal noise accuracy alone does not suffice for the joint hint
distribution. No cryptographic execution is required for a mathematical
existence/accuracy proof.

[OPEN exact computational hypothesis] Preserve secret dimension `n-d`,
discrete width `βq=αq/(2ξ)`, sample bound `m`, and the same quantum-advice
class when stating LWE hardness. The passing profiles do not measure
`εLWE`. A sharper gadget norm could improve the required base noise width;
the present certificate supplies an explicit sufficient condition.

[OPEN cost integration] The passing 1024 profile keeps encryption/setup
parameters unchanged. The passing 4096 profile uses `M=L=2^23`, twice the
original row count; it needs a fresh size/correctness row. The construction
author received the exact parameters and owns that separate calculation.
Do not label the non-LWE term as security bits, combine it with a different
noise-rate assumption, or fold correctness failure into privacy implicitly.
