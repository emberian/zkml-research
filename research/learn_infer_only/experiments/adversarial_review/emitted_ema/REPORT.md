# Independent review of frozen emitted EMA

[DERIVED / decision] **Accept the frozen compiler proposal and generic runtime for the stated formal and finite executed scope.** No concrete mathematical, schema, or interpreter mismatch was found. The Lean source now proves its own ripple terms realize the existing trace relation and composes that fact through the reused compiler to the original word-level Learn/Infer laws. The Rust parser/executor and physical serialization remain outside Lean. The small successful replay sample does not establish general ciphertext determinism.

[EXECUTED / target] The reviewed formal source is `formal/private_address_ema/emitted_schedule/Compiler/PrivateAddressEmaSchedule.lean`, SHA256 `2c89af0fa69ceca9d9a7f0ed3a7a8746255dded42b63d2dc021315ba429d0d1b`; its patch is `c96679078731512508994cd3804c111e096e9e8cb14409592154d1d8bf9cbb30`. All thirteen formal source/dependency pins in the author verification record match; see `formal_source_pins.json`.

## Physical JSON to universal theorem

[EXECUTED] The exact source and its frozen 28-pin word dependency were independently compiled in `/tmp/minidregg-emitted-ema-review-20260908`, using private output oleans and existing cached compiler/Mathlib dependencies. All 46 schedule axiom guards passed. The IO runner was then executed in a separate emission directory against the freshly compiled proposal. Both full JSON byte strings match the frozen artifacts, not just their parsed structures. See `lean_word_dependency.json`, `lean_schedule_replay.json`, `emission_replay.json`, and `physical_json_link.json`.

| Frozen JSON | Full byte equality after independent emission | SHA256 |
| --- | --- | --- |
| Learn | [EXECUTED] 28,772 bytes, equal | `94e8369ffc8fcdf57b8351b278d5af90dc49a26d83600d40f0bd6a0da359dde8` |
| Infer | [EXECUTED] 556 bytes, equal | `b742ed7710d2680119eceeff0c553d675e1ce187a503981fc500a6a4a683b80c` |

[SOURCE / encoder] `EmitPrivateAddressEma.lean`:10–33 encodes `DWire.cnst` as the Boolean test `c.val == 1`, wire indices unchanged, `.add` as `xor`, `.mul` as `and`, the descriptor headers unchanged, and `d.zeros` as the JSON `outputs` reference list. Its only emitted descriptors are `learnSchedule` and `inferSchedule`. Over F2, every constant is zero or one, as also covered by `enc_dec` in the checked source. No second gate-generation algorithm or numbering pass exists in this IO encoder.

[SOURCE / layout] In the checked proposal, `learnTerms`:330 and `inferTerms`:334 are the actual source lists used by `learnSchedule`:337 and `inferSchedule`:338. `baseInputs`:340 packs state at positions 0–31 in register-major, LSB-first order; address bits occupy 32–33; Learn label bits occupy 34–41. The input-state, input-label, and both input-address theorems prove this layout. `learn_schedule_correct`:432 and `infer_schedule_correct`:439 quantify over every state, address, label where applicable, and every input-pinned, gate-satisfying F2 valuation. Their conclusions are precisely the original `stateBits (learn s a u)` and `[infer s a]`.

[DERIVED / link scope] The physical bytes are therefore connected by executed exact reproduction and an inspected direct encoder to the very descriptor constants named by the universal layout theorems. This is stronger than merely checking counts or matching a separately authored JSON file. It is still not a Lean theorem about JSON serialization, parsing, or file reads; those are explicit execution boundaries.

## Reused compiler and freely valued output roots

[SOURCE / actual compiler] `raw`:36 calls the existing `emit Fin.val 0 m`; `shared`:37 calls the existing `cse`. In the read-only companion, `Compiler/AirFlatten.lean`:145 defines `flatten` as `fold flattenAlg`; `Compiler/Emit.lean`:187 lays out flattened gates and roots; `Compiler/EmitShare.lean`:136 rewrites operands/root references and removes duplicate gates while preserving original retained output indices. Their inspected source hashes match the frozen verification pins. The proposal adds source expressions and proof composition, not a replacement flattening or CSE algorithm.

[SOURCE / no zero-output assumption] The descriptor's `zeros` field normally names zero-check roots. This interface intentionally reuses those references as outputs. `SharedOutputCorrectness`:39 assumes only input pinning and gate equations, then concludes the root values equal source expression values. It does not assume `descriptorHolds (shared es) wv`, which would impose inappropriate zero obligations.

[SOURCE / CSE proof] `shared_gates_backward`:118 constructs `{ raw es with zeros := [] }`, obtains SSA from the existing emitter theorem, and applies `holds_of_cse_holds` only to that gate-only descriptor. `shared_output_correctness`:126 transports valuations through `cseSubst`, proves input indices are unchanged, and maps the original expression roots through the CSE substitution. `shared_premises_inhabited`:142 similarly clears zero obligations before `cse_holds`; the existing `fillAux_gates_hold` and `fillAux_getD_of_lt` supply a valuation for every source list and every input assignment. Neither direction needs source expressions to evaluate to zero.

[SOURCE / trace composition] `adder_trace`:182 constructs the actual `RippleTrace` for the new `addE`/`carryE` source terms. `adder_value`:191 invokes the frozen `ripple_bitvec`; `candidateE_value`:263 proves sign-extension, complement, initial carries, concatenation and extraction refine the existing `candidate`. This closes the former mathematical source-trace seam for these emitted source terms. It does not retroactively prove operational semantics of the old handwritten Rust loop.

[SOURCE / inhabited teeth] `learn_premises_inhabited`:446 and `infer_premises_inhabited`:451 construct a gate valuation for every concrete input. `learn_changed_subject`:457 supplies a changed selected register and its actual shared-gate outputs. `wrong_learn_output_refused`:467 rejects the unchanged all-zero output under the same inhabited premises. `infer_negative_subject`:474 supplies `[true]` at an actual gate valuation, and `wrong_infer_output_refused`:484 rejects `[false]`. Thus the gate conditions cannot hide an impossible premise or an all-zero-output interpretation.

## Generic Rust and schema audit

[SOURCE / corpus] Read all three owned Rust files: `emitted_runtime/src/lib.rs`, `src/ciphertext.rs`, and `src/main.rs`. The absence claim here is limited to that corpus: it contains no handwritten EMA update, ripple adder, address-select equation, MUX implementation or sign-selection tree. The operation enum fixes ingress/output dimensions; all actual computation follows the parsed XOR/AND gate list.

[SOURCE / parser] `Schedule::parse`, `src/lib.rs`:102–147, permits only the versioned schema, operation-specific dimensions, exact reference alternatives, known operators and fields, and bounded counts. It tracks initialized slots, rejects input overwrites, out-of-range/uninitialized reads, duplicate writes and non-increasing gate outputs. CSE holes are permitted. `Schedule`'s descriptor field and descriptor constructors are private; the public constructor validates the full schedule.

[SOURCE / evaluator] `src/lib.rs`:177–202 initializes the input prefix, leaves sparse slots absent, creates false/true constants, resolves each operand, performs the indicated generic operation and stores at the validated output index. It reads the ordered output references without testing or forcing them to zero. The Boolean backend uses `^` and `&`; `src/ciphertext.rs`:31–44 forwards the same operators and constants to the TFHE interface. The saved eight-test log covers sparse writes, reference/field/type rejection, duplicate JSON fields, dimensions, generic Boolean outputs and bincode compatibility; these tests were inspected, not rerun.

[EXECUTED / physical inventory] The independent checker validates every gate/reference/output in the frozen JSON and reproduces the retained parser inventory. Learn has 42 inputs, 538 gates (342 XOR, 196 AND), 32 outputs and 11,562 allocated slots, of which 10,982 are unused. Infer has 34 inputs, nine gates (six XOR, three AND), one output and 46 slots, with three unused. Every frozen output is an initialized wire reference. The allocation bound and retained gate count are deliberately different. See `results.json` → `inventory`.

[SOURCE / ingress and envelope] `src/main.rs`:21 validates the schedule before reading the key/data; lines 40–46 concatenate the existing state bytes and operation-specific address/label/query bits in exactly the theorem's layout. `src/ciphertext.rs`:59–72 explicitly uses fixed-width bincode decoding, rejects trailing data, and checks canonical reserialization. Lines 86–107 check the PEMA0001 marker, kind, length, bit count and data variants. Lines 110–125 reject non-Encrypted output variants, create a new output file and sync it. Internal public constants are separate from artifact output policy. A generic constant-output schedule can parse but is rejected at the encrypted artifact boundary; this behavior agrees with the schema.

[OPEN / remaining concrete boundary] No parser-to-Lean decoder theorem or Rust loop refinement is present. Schema validation plus source inspection supports the expected gate-satisfying interpretation, but Rust/serde/bincode behavior, compilation, TFHE gate correctness, same read-time bytes and serialization are still trusted execution components. A failed write can leave a partial new file; the driver treats nonzero process status as failure. No adversarial ciphertext decoder or atomic journal claim was reviewed.

[DERIVED / program binding] The standalone host intentionally accepts any schedule satisfying the schema. It does not intrinsically authorize only these two Lean descriptors. The external frozen manifest pins this particular research run's descriptors and binary. Its before/after hash checks are finite integrity observations, not a theorem measuring each actual file read or ruling out transient external mutation.

## Saved public comparisons and smoke

[EXECUTED / independent reconstruction] `review.py` rebuilds the public input order and floor-division oracle without calling the author comparison script or runtime. It matches the reconstructed input/expected hashes, validates the compressed stdout hashes against command metadata, and compares every saved output bit. All 25,600 Learn rows and 49,152 Infer rows agree. The Learn corpus is the recorded 12,288 public utility transitions, 2,048 selected-byte/endpoint-label/address cases and 11,264 selected-boundary/all-label/address cases; it is not every four-byte state. Infer covers all four addresses for each saved selected-route post-transition state. See `results.json` and `review.stdout.txt`.

[EXECUTED / successful public artifacts] The checker rehashes all 29 public files in `freeze.json`, matches its hash to the smoke/summary records, checks the eight host stdout records and counters, and compares all four archived successful first/replay pairs using their complete bytes. Each pair has equal bytes, different recorded wrapper PIDs and equal invocation arguments apart from output path. Public input hashes and archived output hashes match their recorded values. No new runtime or cryptographic operation was executed.

[REPORTED / private comparisons] The saved author record reports both logical states, untouched registers and selected signs matching the integer oracle. This review checked the public record's consistency but did not open private audit files or decrypt any ciphertext. Those private correctness indicators remain attributed to the author run.

[SOURCE / ordering qualification] `run_smoke.py` completes its prerequisite public execution/replay/output-hash checks before the reader audit. It additionally calls `check_frozen("after_private_audit")`. The retained final integrity check therefore exceeds the frozen contract's literal all-checks-before wording. The source, actual metadata order and explicit discrepancy are preserved; there is no evidence of an extra encrypted evaluation or retry in this run.

[REPORTED / retained separate failure] Root reported that the older handwritten-runtime long workload stopped at its fourteenth Learn replay mismatch after thirteen matching pairs. This review does not inspect that failed output, rerun it, or diagnose its cause. The four positive emitted-runtime pairs do not supersede that evidence or establish general deterministic ciphertext replay. FFT/planner diagnosis remains a separate root-owned review.

## Validation and closeout

[EXECUTED] Frozen patches apply in the isolated checkout after copying the explicitly dirty companion `Compiler.lean` root as the patch baseline; the theorem files match their assigned hashes. No companion source was written. The import-boundary check passes. The first schedule replay failed because the review overlay shadowed the compiler namespace without its cached dependencies; that environment failure is retained in `lean_schedule_first_replay.json`. Adding read-only cache symlinks repaired the overlay, and the exact frozen source then passed. No source proof change was needed.

[EXECUTED] The independent public checker exited zero with empty stderr. It executed no Rust binary, no crypto workload, no private reader, and opened no private files. It only read public sources/metadata and successful public ciphertext bytes for identity/equality checks, alongside the single-module Lean checks and emission replay. Web/Kagi/Scry counts are zero. Root owns full umbrella integration; prior packages remain frozen.
