# Next

1. The whole-row/PCS owner can use `NativeRangeKeyswitch.allRowsSound` and its proved `524 * n` send count when joining extracted native equations to the compiler source. Supply exact field histogram conservation or a priced failure alternative; do not substitute an accepted LogUp accumulator equation for it.
2. The compact matrix/rescale owner can reuse `NativeNibbleRange.supportedSound` and `NativeNibbleComplete.native_iff_range` directly for the new widths 2/11/14/15 alongside 9/16. Neither requires another range implementation or runtime replay.
3. Land the additive patch after its existing compact-MAC and backend prerequisites. Retain the explicit Rust interpretation, byte-table coherence/height, grouped LogUp, PCS and Fiat–Shamir boundaries described in the README.
