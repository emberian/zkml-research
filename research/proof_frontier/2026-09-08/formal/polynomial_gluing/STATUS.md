# Polynomial gluing port — complete

[EXECUTED] The full generic corrected Polishchuk–Spielman theorem was backported to minidregg's Lean 4.30.0 / Mathlib `1c2b90b13009c65b090d95a83c98e248deafb6f1`. `Theory/PolynomialGluing.lean:968`, `polishchuk_spielman`, checks with exact guarded axioms `[propext, Classical.choice, Quot.sound]`; `polynomialGluing` at line 1028 discharges the named contract. There is no result-shaped assumption, added axiom, or admitted proof in this closure.

[EXECUTED] The main theorem signature is byte-identical to the pinned ArkLib source signature, SHA-256 `f2e07122a5e397acd97861965f527af4e5e54d622a0580443bdc971ce506f39a` (`signature-comparison.json`). The generic statement retains both local quotient degree bounds, all four polynomial degree bounds, `A ≠ 0`, positive grid-cardinality lower bounds, the strict rational degree ratio, the global factorization, both quotient degree bounds and both large cancellation subsets.

[EXECUTED] `Theory/PolynomialGluingStatement.lean:50` declares `PolynomialGluing` before the proof closure. `polynomialGluing_premise_inhabited` at line 67 proves every premise for the explicit rational all-one instance. `polynomialGluing_satisfiable` at line 72 supplies its quotient and subsets. `polynomialGluing_teeth` at line 80 proves the conclusion false for `A=Y, B=1` by evaluating at zero. All three have exact axiom pins. These are proofs about explicit witnesses, not finite unit tests.

[EXECUTED] The build passed as `lake env lean -o .lake/build/lib/lean/Theory/PolynomialGluing.olean Theory/PolynomialGluing.lean`; the first clean whole-module output is `lean-full-third.log` (empty). Final exact-artifact execution and source hash are recorded in `validation-final.json` and `lean-full-final.log`. The helper build passed in `lean-bivariate-third.log`; the contract build passed in `lean-statement-fourth.log` (one style warning). Failed backport attempts are retained in preceding numbered logs.

[EXECUTED] The import-boundary script returned zero, and `git apply --reverse --check polynomial-gluing.patch` returned zero; full commands, output and statuses are in `package-checks.json`. The patch adds only `Theory/PolynomialBivariate.lean`, `Theory/PolynomialGluingStatement.lean` and `Theory/PolynomialGluing.lean`. File hashes are in `SHA256.json`. The isolated checkout is `/tmp/minidregg-polynomial-gluing-20260908`, baseline `6937394e1dc2c2aaff986c7d4b3a258aca5d16fd`. No commits, pushes, shared-ledger edits or companion-tree writes occurred.

[SOURCE] The proof is adapted from ArkLib `/Users/ember/src/ArkLib-2026-09`, revision `22dbd4e836c15a21f68889afa69b7130da04abbb`, `ArkLib/Data/CodingTheory/PolishchukSpielman/{Degrees,Resultant,Existence,PolishchukSpielman}.lean`; upstream main theorem line 45. Construction/proof source was inspected, not merely an abstract. Exact source hashes and acquisition counts are in `SOURCES.md`. Apache-2.0 notices are preserved in the modules and `ArkLib-Apache-2.0.txt`.

[DERIVED] The port restricts the dependency closure to Mathlib nested polynomials and source helper facts. It excludes the native CompPoly representation, weighted degree, multiplicity, Reed–Solomon definitions and probability definitions. `degreeX_mul` uses the existing variable-swap equivalence; degree-preserving Y evaluation uses the corresponding X lemma under swap. Other proof adaptations concern the 4.30 coefficient constructor, the triangular-determinant lemma name and elimination of an empty finite sum.

[OPEN] This closes the assigned algebraic gluing seam. Full unique-decoding integration, umbrella build and reachability remain owned by root and proof_frontier; this lane does not claim a complete proof-system soundness result. The downstream lane has been told the exact imports and may consume the unchanged source theorem name.
