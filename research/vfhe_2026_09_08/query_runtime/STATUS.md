# Complete: active-class query arithmetic and reusable pipeline

[EXECUTED] One full8192-position/16384-final-residue proof passed on the first
attempt. Fresh verification accepted and changed final output was rejected.
Both signed ct×pt products and final subtraction are inside the generated joined
relation. README/REPORT/RESULT record exact costs, API and explicit TCB limits.

The native source/binary, run.py and PIPELINE.json are frozen for the service.
query_gate_successor owns the new substantive wrapper execution and private
receive gate for all active classes. No old query was retroactively gated.
