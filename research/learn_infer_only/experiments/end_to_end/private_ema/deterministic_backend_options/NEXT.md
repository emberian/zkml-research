# Next

[HYPOTHESIS] The next bounded artifact is an isolated, type-checked native-u32 PBS adapter composed from existing modulus switch, Karatsuba blind rotation and sample extraction. Start with a public synthetic polynomial/external-product comparison and cost bound, then decide whether a new setup/crypto experiment is justified. ADAPTER_CONTRACT.md specifies the API and acceptance boundaries.

[OPEN] Establish a retained standard/seeded public BSK contract for a future setup. Do not inspect or convert current run keys, decode failures, mutate the frozen fixed-Dif4 runtime, or replace its running trajectory. No such action is part of this source-only task.

[OPEN] An alternative fixed-FFT proof needs a precise binary64 operation graph, rounding/conversion, finite-state and SIMD/serialization contract. Fixing Dif4 alone has not proved those premises. Native32 CRT integration and u64 BNF embedding are separate follow-ons, not drop-in feature flags.
