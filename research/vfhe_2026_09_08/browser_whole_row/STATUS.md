# Status

[EXECUTED] Complete: three new all-row proofs over the unchanged public expiry
and two `fast001` query cases. Three native self-checks passed. The unchanged
actual WASM accepted all three in one recorded Node run; the existing update
button's row-zero changed-output control was rejected. No browser UI run, key
generation, private read, new text, decryption or malformed-proof replay.

[EXECUTED] Public assets, exact source origins, preserved original request,
replacement application pins and result logs are ready. Root owns publication;
this lane has not edited the microsite. See README and ASSET_MAP.json.

[OPEN] These are selected coefficient equations with all-row scope, not a
complete nonlinear learner browser verifier or a universal native soundness
theorem. Backend/lookup/transcript and application bindings remain explicit.
