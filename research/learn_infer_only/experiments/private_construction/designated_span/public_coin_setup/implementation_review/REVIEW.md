# Independent review of the public-coin setup implementation

[OPEN review status] Source/prerequisite review is complete; final disposition awaits the author's sealed public evidence package. No crypto operation or private file read is part of this review.

## Mathematical prerequisite and actual source path

[SOURCE] The accepted proposal is `../PROPOSAL.md`, SHA256 `4d30d6013642666081ebecfbcc4dbb0446afeda1e58d94f294202507a20f71c9`; the independent mathematical review is `../review/REVIEW.md`, SHA256 `ba063f308c66794a348517ef6ebc20e06ba1da4c76d35d35cd60d79c753cc04b`. I read both completely, including the review's implementation prerequisites and its distinction between accepted public values, an ideal rejection tape and an OS generator state. The mathematical acceptance is conditional classical fixed-coalition security for the exact honest experiment. It is not a proof that any public group sampler has no auxiliary logarithm knowledge.

[DERIVED from source] `adapter/setup.py` implements the accepted algebra using the unchanged original 16-by-577 full-rank row matrix. `matrices` computes the first-16-column determinant and inverse, checks the recorded determinant `-812032080`, verifies `Yp*R=I` modulo q and every `Yp*L+Yf=0` entry, and uses those public matrices to complete pivot coordinates. `complete` computes `B=U^2`, `H=A*g^tau`, then `h_pivot_i=product(H_l^R_il)*product(B_j^L_ij)`. Returning `pivots+free` restores the intended original order because these fixed pivots are precisely columns 0–15; no unimplemented general column-permutation procedure is claimed. These are source-inspected checks; I do not rerun group completion or its modular powers.

[SOURCE] `sample_interval` draws `secrets.randbits((upper-1).bit_length())` until `lower <= candidate < upper`. Recipients call it on `[0,q)`, and the public constructor independently calls it for 16 tau values in `[0,q)` and 561 U values in `[1,p)`. It does not sample a scalar c and form `U=g^c`. Tau zero and `U=1,p-1` remain legal. The constructor publishes the accepted tau/U values and public announcements/context, not a seed, rejected words, or internal OS generator state.

[DERIVED] Under independent ideal bit words this literal rejection rule is unbiased, with acceptance probability above one half and expected fewer than two words per sample for the fixed safe-prime parameters. The concrete `secrets.randbits`/OS source does not itself prove ideal independence or a concrete cryptographic security level. The theory's optional complete ideal rejection-tape simulator is not an assertion about a CSPRNG seed or correlated state, and this adapter does not export that tape. Its per-subprocess timeout and global harness wall budget are operational limits, not the theory's explicit R-word sampler cap or a proved finite-time sampling correction. Physical timing and budget-related outcome observations lie outside the claimed accepted-value transcript.

## Registration, credentials and cache trust

[SOURCE] The fixed rows and slot ordering are loaded before `auth-init`. That command creates independent Ed25519 authentication keys and a registry of the 16 verification keys and row hashes. Each separate `recipient-init` process checks its registry slot and authentication key, samples its own scalar a, saves only its own pending scalar, and publishes a signed announcement containing `A=g^a`. The signature uses its independent Ed25519 key, not a. `positive.py` obtains all 16 announcements before invoking `public-build`.

[SOURCE / precision] `public-build` authenticates and range-checks all announcements, and checks the fixed matrix before drawing public coins. Group membership of A is rechecked in the subsequent `complete` call. In this honest executed path A was already produced as `g^a`; this order satisfies the reviewed honest-registration experiment. It would be too broad to claim that an arbitrary hostile registration protocol or every subgroup check finishes before public sampling. Such a protocol is outside the accepted theorem.

[DERIVED from call graph] The public constructor receives only registry/announcement/context/transcript/output paths. Its private state consists of public matrix arithmetic and directly sampled public field elements. It never computes a scalar master s or projection deliveries `k_i`; the logarithms used in the mathematical coupling are not algorithms in this function. Importing the frozen backend initializes public constants/rows/counters but does not invoke its `main` or dealer branches. The operational path deliberately uses `recipient-finalize`, which validates and wraps each recipient-owned pending a; it does not invoke legacy `keygen`, `initializer` or `recipient-register`. Those unused commands remain in the copied backend and must not be mistaken for the executed setup.

[SOURCE] `verify-public` rechecks registry/source/native pins, announcement signatures, fixed coordinate selection, every public completion equation and byte-identical context. The driver then invokes the backend's uncached `validate-context`, checking all subgroup and token equations under the frozen core sources. Later recipient finalizations, encryption, addition, transform and decode use the same pinned validated-context cache. Every recorded operational call is expected to reread/hash sources and native dependency; context-cache trust still requires the actual trusted validation and matching digest, not a self-authenticating arbitrary hash string.

[SOURCE] Pending a files are removed after finalization; the surviving objects are 16 dedicated scalar envelopes and independent registration-authentication keys. This removes a redundant recipient-secret copy in a named path. It is not the former dealer-master erasure step, because that master and its scalar projection delivery files were never constructed on this path. Recipient scalars, private encryption randomness and any equivalent copies still require the stated private-role boundary. Source control flow and namespace counts do not prove physical memory erasure or isolation under one Unix account.

## Normal continuation and claim limits

[SOURCE] `positive.py` uses the unchanged crypto backend for 33 Learn operations, selected Infer operations at steps 1, 16, 32 and 33, and one exact-original expiry at step 33. It retains original ciphertexts and compares each accumulator's bytes with their direct group product; it compares four decoded signed integers against a separate plaintext queue formula. The fixture is deterministic public integer data, `((t+3)*(j+5) mod 17)-8`, with fresh private encryption randomness supplied by the backend. This exercises normal encrypted arithmetic; it is neither a private development source nor model/text utility evidence.

[DERIVED: meaning of the improvement] The honest public constructor no longer needs to hold an unrestricted scalar master or private projection deliveries and then promise to erase them. The accepted theorem explains why exposing its accepted U roots does not add a capability beyond the prior fixed-span view. This is stronger setup-lifecycle evidence than merely renaming a credential. It remains conditional on honest direct sampling, independent honest recipient registration and absence of extra correlated setup secrets. Public recomputation alone does not establish those premises; a different sampler with retained logarithms is not covered even if its public marginal distribution is uniform.

[DERIVED] All recipients together retain their entire fixed projection span on all issued ciphertexts, including expired inputs and copies. The bounded integer decoder is not a cryptographic selected-output or magnitude policy. The setup adapter does not add journal finality, a public-ACK/private-drain protocol, arbitrary recipient registration, new query directions, adaptive corruption security or nonlinear protected learning. The separate rank-eight semantic feature fixture does not satisfy this unchanged 16-independent-row recipe. A future adapter must not silently reuse this proof on that different matrix.

[DERIVED] The known public input fixture lets anyone reconstruct its raw aggregate; no-master scalar generation is distinct from absence of every way to know a state. Shared-account execution and researcher access to recipient files do not demonstrate operator confidentiality. Research timing, private decode aggregate counters and overall completion/reporting are outside the accepted-value cryptographic transcript. Classical DDH and independent Ed25519 authentication have separate assumptions; no post-quantum security is supplied. No cryptographic execution, private file opening/hashing, disclosure/routing/malformed test, metered query, companion edit or shared-ledger change was performed by this review.

## Sealed evidence check

[OPEN] Awaiting final artifact manifest and stable exported public records from the author. Source-level conclusions above will be tied to those exact bytes before final disposition.
