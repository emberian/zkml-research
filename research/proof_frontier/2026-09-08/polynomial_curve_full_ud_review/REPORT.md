# Degree-M full-UD curve review

[DERIVED verdict: accepted within stated scope] Source/math review found no defect in the degree-M core, its probability conversion, or the fixed-coefficient M7/M8 applications. [EXECUTED] The frozen artifact audit passed on author manifest `fed8141f3d1606cd7ffa25bdd85918acbbdcb1ac8037307359adf1ff05994729` and patch `3fa34548e2f1ec0f286ba04594374f3fc5aa636fda5dd5f7583bdeeca6dd7d0f`. All ten modules, 42 declarations and 42 exact standard-only guarded axiom pins were independently censused; every source matches an author final check with exit zero and empty output. No Lean build was performed by this reviewer. [REPORTED root] Clean run018 predates the curve modules; a later joint integration build is a separate gate.

[DERIVED scope] The result is a degree-M correlated-agreement/proximity-generator theorem over the existing Reed–Solomon code, plus M=7 and M=8 single-scalar coefficient-curve applications. It is not an arity-eight transcript, a multi-height soundness invariant, a Rust execution refinement or a changed query/security budget. The frozen affine predecessor remains selected as an unchanged dependency.

## Contract and degree audit

[SOURCE] All source references below are relative to `../formal/polynomial_curve_full_ud/src/`. `Selvage/CurveFullUDStatement.lean:15 CurveFullUDCardCore` states the actual hypothesis and conclusion before the proof: for an injective domain of size n, more than M*n scalar combinations within e errors of the existing code force all M+1 coefficient words to agree with codewords on one common set of at least n−e positions. `CurveFullUDPremise` at line23 includes M≥1,d≥1,2e+d≤n, the strict good-set threshold, and actual Hamming proximity. No premise assumes the desired common set, polynomial quotient or final exceptional-set bound.

[SOURCE/DERIVED] `Theory/PolynomialCurve.lean:11 poly`, `eval_poly`, `coeff_poly` and `natDegree_poly` identify the finite coefficient vector with its ordinary Mathlib polynomial and its coefficients, with degree at most M. `Theory/CurveRSInterpolationMatrix.lean:19–71` reuses the existing `BW_homMatrix`. Only its e+1 locator columns contain the challenge polynomial; numerator columns have degree zero. Each determinant term therefore has degree at most M(e+1), rather than M times the total column count.

[SOURCE/DERIVED] `Theory/CurveRSInterpolationKernel.lean:28 curve_det_eq_zero_of_specializations` obtains a genuinely nonzero specialized kernel vector from its nonzero locator block, restricts it along arbitrary row maps, and forces each maximal determinant to vanish at every distinct good scalar. Its strict requirement is `M(e+1)<|good|`. At line107, `curve_bounded_kernel_of_specializations` chooses e+d distinct rows, obtains a constant invertible negative Vandermonde block D, and forms the Schur complement. D and D⁻¹ have challenge degree zero. The complement has entry degrees ≤M and vanishing (e+1)-minors; the selected successor kernel supplies a nonzero locator vector of degree ≤Me. Multiplication by the degree-M block, then the degree-zero inverse, gives numerator degree ≤M(e+1). The rank/cofactor primitive itself is delegated to the separately reviewed 26-pin successor, SHA256 `f510b6cc0c648a3583e3eefa49e283b2186add24d2e4df71f3afeb9b06a3fa91`.

[SOURCE/DERIVED] `Selvage/CurveFullUDInterpolation.lean:30 curve_bw_bounded_kernel_of_many_close` constructs specialization certificates by applying the existing checked single-word error-locator lemma to `(curveWord u z,0)` at scalar zero. It does not import an affine proximity conclusion as a curve assumption. Since e+1≤n, the stronger input |good|>Mn implies the determinant threshold. `CurveFullUDBivariate.lean:20 curve_bw_bivariate_of_many_close` assembles and swaps ordinary nested polynomials, preserves nonzero A, and obtains A bidegree ≤(e,Me), B bidegree ≤(e+d−1,M(e+1)), with B(x,Z)=u(x,Z)A(x,Z) at every domain point.

[SOURCE/DERIVED] `Selvage/CurveFullUDCore.lean:39 curveFullUDCardCore` constructs each good-scalar quotient from an actual nearby codeword of degree <d. The residual B(X,z)−p_z(X)A(X,z) has degree ≤e+d−1 and at least n−e distinct roots. The integer-radius hypothesis gives the required strict inequality e+d−1<n−e, including 2e+d=n. Thus the residual is zero. The quotient in the other direction is the actual curve polynomial, of degree ≤M; natural subtraction gives `(e+d−1)−e=d−1` and `M(e+1)−Me=M`.

[DERIVED] The Polishchuk–Spielman input at core lines221–252 is exactly

`(e+d−1)/n + M(e+1)/|good| < (e+d−1)/n + (e+1)/n = (2e+d)/n ≤ 1`.

The first comparison is strict because |good|>Mn and e+1>0. The proof uses positive rational denominators derived from the actual finite sets. The unchanged PS theorem returns a global quotient P and a cancellation set Qx of size ≥n−e. Pulling that set back through the injective domain preserves cardinality. Its coefficient polynomials have degree <d and evaluate to the original u_j on the common set (core lines253–289). The argument does not cancel A at points where it vanishes.

## Edge cases, probability and applications

[SOURCE/DERIVED] M=0 and d=0 are explicitly excluded by the public core. The generalized matrix primitive permits M=0, but that does not enlarge the curve theorem's advertised scope. e=0 is allowed throughout: A has bidegree (0,0) and is nonzero, denominators and quotient bounds remain valid, and the common set has all n positions. d≥1 plus 2e+d≤n proves n>0; no empty-domain premise is hidden. B=0 is allowed. If Mn≥|F|, the probability upper bound is valid but can be uninformative, and the many-good premise cannot hold over that finite field.

[SOURCE/DERIVED] `Selvage/CurveProximityGapFullUD.lean:23 curve_correlatedAgreement_of_many_close` sets e=floor(δn), derives 2e+d≤n from `d<(1−2δ)n`, and transports the actual Hamming and common-set bounds. `curve_bad_card_le` at line49 takes the contrapositive, giving at most Mn close scalars when actual correlated agreement fails. The geometric generator at line12 has seed space F, coefficients (1,z,…,z^M), and normalized uniform weight; no independent randomness for the coordinates is asserted. `reedSolomonCode_isGeometricProximityGenerator_fullUD` at line72 has B=(1+d/n)/2 and error Mn/|F|, on the existing strict radius interval. The mutual-agreement head at line91 reuses the existing RS minimum-distance/generic MCA theorem with the same constant error; its maximum radius bound is discharged, not postulated.

[SOURCE/DERIVED] `Selvage/CurveFriChallenge.lean:50 arity8_curve_sound` applies M=7 to eight fixed coefficient words. `arity8_injected_curve_sound` at line60 applies M=8 to `Fin.snoc u g`, and `curveWord_snoc` proves that this is precisely `curveWord u z + z^8*g`. Agreement of all nine implies agreement of the first eight, so failure of the latter supplies the needed hypothesis. No premise requires g to be a codeword. `arity8_injected_fixed_prefix_sound` at line75 is pointwise in a prior prefix: u and g can depend on that prefix, but not on the fresh z quantified inside the probability. The public generic algebra/core uses universe-polymorphic field types; these uniformProb wrappers use the existing Type-valued probability interface.

[DERIVED scope] The coefficient identities fit the separately reviewed p3 transport arithmetic: a width-eight fold uses one beta with internal powers beta,beta²,beta⁴; equal-height injection uses beta^8. The present Lean modules do not import that runtime adapter, a BabyBear folding tower, canonical root resolution, or a proof that reduced-opening input vectors are fixed before beta. The missing reconstruction/farness transport and adaptive multi-height/query composition are still necessary to apply this result to the active Rust verifier. In particular, the nineteen independent binary challenge theorem and its 3603-query proposal are not relabeled as an eight-ary result.

## Witness and refusal audit

[SOURCE] `CurveFullUDStatement.lean:43 zero_premise_inhabited` inhabits M=1,…,8 over F101,n5,d2,e1 with all 101 good scalars; its impossible-radius theorem rejects 2e+d>n. The stronger `CurveFullUDTeeth.lean` supplies legal degree-one words and one-coordinate corruptions. `corrupted_premise_inhabited` shows every curve point is within one error, and `corrupted_distance_one` plus `corrupted_not_mem` proves that each coefficient word is actually outside the code. `zero_error_premise_inhabited` includes M7 and M8; `zero_error_core_fires` invokes the actual M7 core at e=0. These prevent the positive-radius and zero-error interfaces from being witnessed only by impossible premises.

[SOURCE/DERIVED] `CurveDegreeTeeth.words` embeds the cubic z(z−1)(z−2) in Fin8 over F17, with domain {0,1}, d1 and δ1/5. Its three roots are close scalars, exceeding n2; one nonconstant coefficient rules out correlated agreement on the required two positions. `strict_radius_admissible` proves δ lies strictly inside the conservative range. Thus `unscaled_affine_bound_falsifier` rejects carrying the affine n/|F| error unchanged into curves. It need not have exact degree seven to refute a bound claimed for every degree-at-most-seven family.

[EXECUTED] `python3 check_finite.py` passed using public exact integer, Fraction and small prime-field arithmetic. `FINITE_RESULTS.json` records 187,264 admissible degree/PS parameter cases (M1…8,n1…64), including e0 and every integer-radius boundary, plus 118,617 exhaustive normalized constant-code curve families. Subtracting the first coordinate polynomial from every coordinate preserves both constant-code proximity and common coefficient agreement, so normalization is sound. The controls separately include M7/M8 product-of-roots examples over F101, all source corrupted witnesses for M1…8, and legal e0 witnesses. These are independent finite checks, not Lean proofs or cryptographic runtime evidence.

[EXECUTED sharp-threshold refusal] Over F7 take n3,d1,e1,M2 and coordinate polynomials `[0, Z+Z², 1+2Z+6Z²]`. Exactly six scalars `{0,1,3,4,5,6}` are within one error of the constant code, but the largest common coefficient agreement set has size one, below n−e=2. Thus weakening the integer core's `|good|>Mn` to `|good|≥Mn` is false. The F5 affine example `[0,Z,1+2Z]` analogously has exactly three good scalars `{0,2,4}`. These examples sit on the integer-radius boundary; they are not presented as counterexamples inside the probability head's strict real-radius interval.

[EXECUTED timing refusal] Over F101,n2,d1,δ1/5, set u0=(0,1),u1…u7=0. Choosing g after z as `(0,−z⁻⁸)` for z≠0 makes 100/101 challenges close, exceeding the purported fixed-M8 bound 16/101. This finite mutation demonstrates the importance of the coefficient timing condition; it does not refute the fixed-prefix Lean theorem.

## Source provenance and verification scope

[SOURCE] Local BCIKS20 mirror `2020/654.pdf`, printed p28 Theorem6.1 and pp29–30 §6.1, states the degree-l curve Mn threshold and the same scaled BW/PS degree argument. Its maximal polynomial degree k corresponds here to d−1. The module headers preserve the reused ArkLib authorship and Apache2.0 license. This review read the pinned local text extraction and the actual Lean sources; it made no new web/Scry requests and downloaded no PDFs.

[EXECUTED] `python3 check_artifact.py` passed. `ARTIFACT_CHECKS.json` pins 114 author package files, independently censuses all 42 declarations/guards (including the private helper), verifies all 12 declared dependency hashes, and records the eight direct nonowned imports plus the candidate-independent Theory import closure. A fresh temporary Git directory reconstructed any base targets at commit `6937394e1dc2c2aaff986c7d4b3a258aca5d16fd`; `git apply --check` and application passed, and all ten resulting files match the published source bytes exactly. The patch contains only those ten new modules. All ten frozen affine sources and all three selected PS successor sources match both their frozen packages and the author isolate. The selected kernel hash matches the separate 26-pin successor. The audit checks logs and source bytes; it does not independently replay Lean compilation.

[EXECUTED exact selected heads] `CurveFullUDCore.lean` SHA256 `fa041cb91bcb807abc2cfd450dbc68182637727bf2ecd50e665498d2f89dcb14`; `CurveFriChallenge.lean` SHA256 `9de6c75ee6428629e178e24d91dfcebe97e4e7169cf6e5f827ead597f0f59547`; `CurveFullUDTeeth.lean` SHA256 `d0c20fa2c1785478155424fe488193196f1dfb16809dde48c59bed44111bcc96`. Full source/patch/dependency pins are retained in the machine-readable audit.

[OPEN integration/application] The parent owns the fresh joint closure build. An actual arity-eight folding/farness adapter, pre-challenge input-resolution timing, multi-height invariant and coherent query/protocol composition remain outside this accepted coefficient-curve result. No frozen predecessor, runtime source, ErrorBudget, shared verdict or security label was edited.
