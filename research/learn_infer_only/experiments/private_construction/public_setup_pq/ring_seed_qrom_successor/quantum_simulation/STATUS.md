# Finite quantum simulation status

[DERIVED] `SIMULATION.md` gives a coherent prefix/overlay wrapper with one
fallback query per exposed query using phase kickback. Its finite oracle
can be removed exactly with k=2Q+c independence, including all honest base
evaluations, under explicit input/output/work bounds and the stated public
modulus advice condition. Arbitrary hybrid tables are common channels, not
asserted random oracles.

[SOURCE] Original 2q-wise and mixed c+2q-wise primary lemmas were read from
local PDFs. `SOURCES.md` records locations and the two web discovery queries.

[EXECUTED] `COUNTS.json`, `PHASE_CHECKS.json` and `RUN.log` retain deterministic
resource arithmetic and symbolic parity checks. No quantum, random, crypto
or estimator simulation was performed.

[OPEN] Parent owns the full reprogramming/regularity/FE theorem and concrete
running-time interpretation. No concrete SHAKE256 or uniform field-generation
claim is added here.
