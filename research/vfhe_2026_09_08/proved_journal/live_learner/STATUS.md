[EXECUTED] Completed live001: fresh text encoding, fresh BFV key/candidate,
production proof generation, failed-proposal unchanged-head check, actual
journal verification/commit, committed-head query and matching post-receive
integer score. Both declared classes are retained; one is active. A fresh
adapter reopened the exact head. Source/runtime pins still match. The reusable
teach-query command and separate stage/commit interface are complete.

