# Parameter delta — completed audit

[EXECUTED] Exact rational old/new calculation completed in `derive.py`; command, exit0 and output are retained in `execution.json` / `derive-output.txt`. Main findings and conditional proposal are in `ANALYSIS.md`; exact theorem/configuration addresses and source scopes in `SOURCE_MAP.md`; inspected file hashes in `source-pins.json`. Results are in `results.json` and CSV tables.

[DERIVED] Full UD changes the rate-half open direct band from 1/6 to 1/4 at the same n/fieldCard. Every inherited ErrorBudget error expression stays identical. Current query ledger already uses the full-UD endpoint formula and receives no new saving.

[EXECUTED] Fair ideal coherent-sampling candidate: fixed initial radius `2/5`, `n=2^20`, `d=2^19`, `m=19`, field BabyBear4, common challenge bound `m*n/field`. Old uniform-gap optimization admits `tau<1/108`; new full UD allows `tau=1/95`. Exact optimal old/new query counts for target 55 are 4,099 / 3,603, saving 496. An interior old `tau=8191/884736` attains 4,099. Every minimum has an exact preceding-query failure. The fixed challenge term alone exceeds `2^-100`, so no query count reaches 100 for this field/m-round bound.

[DERIVED] These are theorem-instantiation and arithmetic results, not a claimed deployed security uplift. Concrete fullUD/fold, tower/domain, farness, binding, coherent-layout and FS/shared-execution premises remain as identified in ANALYSIS.md. No independent error term was discounted or deleted.

[EXECUTED] No companion write, VERDICTS edit, commit, external search or runtime benchmark. The separately authorized universe/pin integration correction is saved in ../formal/polynomial_gluing/universe_successor/ and preserves all predecessor bytes.
