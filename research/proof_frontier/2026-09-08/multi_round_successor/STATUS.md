[DERIVED] Complete: two actual arity-eight transitions compose into terminal RS-membership soundness over the existing coherent sampler. Prefix-dependent second input and middle commitments are allowed; the proof derives and sums conditional challenge failures, then pays one sampling tail.

[DERIVED] The supplied-opening theorem uses the same extracted middle checkpoint in both rounds and retains the observed finite-log Failure residual. Its actual BabyBearExt4 specialization is `(2^20+2^17)/2013265921^4 + (9/10)^q + Pr[Failure]`, with the certified `2^20→2^17→2^14` tower and degrees `2^19→2^16→2^13`.

[DERIVED] The 128→16→2 schedule has a provably far initial word, a second injected word that actually depends on the first scalar, accepted zero seeds and rejected seed one. A separate complete supplied-path/log witness inhabits source extraction, farness, checkpoint and supplied-path inclusion, acceptance and zero observed failure.

[EXECUTED] Five additive modules, 43 exact theorem axiom guards, five final changed-module checks (exit 0, empty output), import boundary and exact patch replay pass. Main and frozen one-round/curve/kernel/carrier/tower dependencies are unchanged. No broad rebuild or new independent-review campaign was run. See MANIFEST.json and checks/.

[OPEN] This is a terminal-codeword theorem for two external rounds. Runtime packed MMCS, actual terminal-polynomial equivalence, transcript/log completeness and cryptographic pricing of observed Failure remain explicit boundaries. No whole-protocol or security-bit budget is added. Root owns integration.
