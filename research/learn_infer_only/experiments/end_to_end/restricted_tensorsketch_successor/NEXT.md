[REPORTED] Stop this lane per root's architectural reassessment. The full-task loss exceeds the frozen five-percentage-point gate despite preserving the small known slice. Do not retune the seed, map, quantization or old compact32 construction; do not launch cryptography or finish an issuer adapter.

[OPEN] Root will choose whether a different learner architecture could change the actual capability. This packet supports that decision with a retained negative paired result, not a general rejection of TensorSketch or nonlinear restricted learning.
