# Independent review of CSE structure preservation

[DERIVED verdict] **Accept the selected generic preservation theorem and unconditional Lean EMA execution corollaries. No blocking source defect found.** The proof supplies SSA and allocation bounds for the actual CSE result without evaluating hashes. Those facts discharge the existing `fillAux` hypotheses and remove the prior actual-schedule structural premise. The conclusion is about the existing Lean array evaluator; initialized-reference validation and Rust option-storage refinement remain separate obligations.

[SOURCE exact target] Module: `research/learn_infer_only/formal/cse_structure/Compiler/CseStructure.lean`, SHA-256 `9828878c2ac7b175c9d9928348b0a858573ad951a569a90997684732e0289608`. Patch: `minidregg-cse-structure.patch` in that package, SHA-256 `ca0d13b93fba0e685800535c5f84ffa64f4d74c276fd6ec40a8c4cba315a155d`. Below, **C** denotes this module; **E** denotes the frozen `research/learn_infer_only/formal/emitted_schedule_execution/Compiler/EmittedScheduleExecution.lean`; **S** denotes the frozen `research/learn_infer_only/formal/private_address_ema/emitted_schedule/Compiler/PrivateAddressEmaSchedule.lean`. Companion paths are rooted at `/Users/ember/dev/minidregg/`.

## Generic invariant and induction

[SOURCE / DERIVED] C:27 `CseStructurePreservation` quantifies over every `ConstraintDescriptor F`, with `F : Type u`, `DecidableEq F` and `Hashable F` (C:24–25). Its only descriptor hypotheses are the existing `d.SSA` and `d.WellFormed`. The conclusion is `(cse d).SSA ∧ (cse d).WellFormed`. No field structure, gate arithmetic, gate/output equations, compiled decision, hash injectivity or collision-resistance premise is present. The pass being proved is the existing `Compiler/EmitShare.lean:125` `cseGo`, and `:136` `cse`, not a copied algorithm.

| Invariant or premise | Exact source | Why it is sufficient |
|---|---|---|
| Substitution does not increase an index | C:39 `CseGood.subst_le` | [DERIVED] A bounded operand remains bounded after one `DWire.subst` lookup. This is proved at C:46 `subst_bounded`; no recursive/transitive substitution assumption is needed. |
| Every signature hit names an actual kept gate | C:40 `CseGood.sig_kept` | [DERIVED] The lookup value is an output of a retained gate with exactly the operation and rewritten operands in the key. It cannot be an arbitrary representative detached from the retained list. |
| Retained gates satisfy allocation and operand bounds | C:42 `CseGood.kept_shape`, C:35 `GateShape` | [DERIVED] Every retained output stays in `[lo,hi)` and each rewritten operand is below that gate's output. |
| Retained outputs descend in the prepended list | C:43 `CseGood.kept_descending` | [DERIVED] Reversing the final retained list gives the ascending output order required by the actual SSA definition. |
| All retained outputs precede every unprocessed output | C:92, premise of `cseGo_good` | [DERIVED] A signature hit therefore selects a strictly smaller output; a newly kept head can be prepended while maintaining descending order. This premise is maintained against the entire remaining suffix, not just its head. |

[DERIVED] In the hit case (C:102–110), `sig_kept` plus the retained-before-remaining premise gives `w < g.out`. `insert_subst_le` at C:60 proves that inserting `g.out ↦ w` preserves non-increase for every key, including the inserted key and all untouched keys. No gate is added, so the signature support, retained shape and retained order remain valid. The retained-before-remaining premise restricts to the tail.

[DERIVED] In the miss case (C:111–128), the new retained gate keeps `g.out` and substitutes only its operands. Non-increase preserves both operand bounds. `insert_signature_kept` at C:70 handles both key equality and inequality, tying the inserted signature to the newly prepended gate while preserving support for old entries. The retained-before-head premise places the new output above every prior retained output; original strict output order places it below every remaining output. Thus both list-order invariants continue to hold.

[DERIVED] `empty_good` at C:130 establishes the invariant without a semantic witness: the empty substitution is identity, signature support is vacuous and the kept list is empty. `cse_structure_preservation` at C:141 derives input `GateShape` from the actual SSA/allocation hypotheses, runs the induction, reverses the kept list, and uses substitution non-increase to preserve bounded final output references (C:158–160). Public/variable/wire counts are unchanged by `cse`. This proves the complete existing WellFormed structure, including output-reference bounds, not merely bounds on retained gates.

[SOURCE / DERIVED arbitrary hash] The critical map laws are Lean 4.30's `Std/Data/HashMap/Lemmas.lean:263`, `getElem?_insert`, and `:452`, `getD_insert`. Their `EquivBEq`/`LawfulHashable` requirements are available because the key equality is lawful decidable equality; `Init/Data/LawfulHashable.lean:29` derives `LawfulHashable` from `LawfulBEq` for any `Hashable` function. `Compiler/Emit.lean:79` derives decidable equality for `DWire`; the signature key is the existing product type `GateSig` (`EmitShare.lean:102`). Hash collisions do not imply key equality and do not undermine the proof. The result applies to arbitrary hash implementations under the stated Lean types, not to an arbitrary inconsistent Boolean equality oracle. No opaque hash evaluation is used to derive the preservation theorem. Exact hashes of both inspected toolchain files are retained in `RESULTS.json`.

## Execution and exact source application

| Result | Exact source | Premises and scope |
|---|---|---|
| Raw emission inhabits both structural hypotheses | C:162 `raw_premises_inhabited` | [DERIVED] Every source term list at every input dimension is covered by existing `emit_ssa` (`Compiler/EmitShare.lean:552`) and `emit_wellFormed` (`Compiler/Emit.lean:446`). No result of CSE is assumed. |
| Actual shared descriptor has both shape properties | C:167 `shared_structure` | [DERIVED] Applies preservation to `raw es`; `shared es` is the frozen definition `cse (raw es)`. |
| Actual evaluator equals source-term evaluation | C:172 `shared_execution_total`, named proposition at C:31 | [DERIVED] Every finite source term list and every typed input vector are covered. No structural-check, gate-value, output-value or arbitrary-valuation premise remains. |
| Actual Learn schedule produces the next state bits | C:185 `learn_execution_total` | [DERIVED] All four-register signed-byte states, all two-bit addresses and all signed-byte labels. The only explicit arguments are state, address and label. |
| Actual Infer schedule produces the selected sign bit | C:193 `infer_execution_total` | [DERIVED] All such states and addresses; the only explicit arguments are state and address. |

[SOURCE / DERIVED] C:172–183 uses exactly `Compiler/DescriptorEval.lean:191` `fillAux_gates_hold` and `:212` `fillAux_getD_of_lt`. `Array.ofFn` supplies the input-length equality. The valuation passed to S:126 `shared_output_correctness` is the computed `fillAux (shared es) (Array.ofFn inputs)` array; it is not the independently existing raw-emission witness. C:185 and C:193 then reuse S:410 `learn_source_outputs` and S:421 `infer_source_outputs`. Therefore the semantic chain is not circular, and it genuinely removes the conditional structural premise previously present at E:158/E:167.

[SOURCE / DERIVED] The frozen `execute` at E:61 merely reads the descriptor's output references from `fillAux`; it is unchanged. The descriptor field named `zeros` serves as outputs in this source. No requirement that those output values vanish is introduced. The source operation remains the full signed-byte/private-address EMA from the frozen word theorem; no restricted input range or label set is newly assumed. JSON serialization and byte identity are not part of these equalities.

## Inhabitation and orthogonal controls

[SOURCE / DERIVED] C:162 supplies universal premise inhabitation for source emission, stronger than a single empty-descriptor example. C:200 `learn_total_subject` applies the actual unconditional theorem at zero state, address three and label 120, and proves that the selected state byte changes. C:206 `wrong_learn_answer_refused` proves the actual schedule output cannot be the unchanged zero state. The small word-level decisions use `decide +kernel`; the fixed CSE descriptor is handled by the generic theorem, not kernel hash normalization or a compiled verdict.

[SOURCE / DERIVED] C:216 `singleton_cse` proves that the actual CSE pass leaves every singleton descriptor unchanged, using empty-map laws. It makes both structural counterexamples about the result of the actual pass:

- C:230 `missing_ssa_refused` exhibits `selfRead.WellFormed` together with `¬ (cse selfRead).SSA`. Its sole gate reads its own output; allocation bounds do not repair operand causality.
- C:246 `missing_bounds_refused` exhibits `inputWrite.SSA` together with `¬ (cse inputWrite).WellFormed`. Its constant-input gate writes input wire 0; SSA does not supply the missing auxiliary-output lower bound.

[DERIVED] These controls are orthogonal at the theorem's two top-level premises: each explicitly inhabits one premise and falsifies the required conclusion when the other is absent. The self-read example tests the operand-order component of SSA. As a singleton, it does not separately demonstrate necessity of pairwise strict output order; that obligation is instead visible and used in the generic induction. No claim of separate pairwise-order falsification or complete parser testing is warranted by these controls.

## Precise remaining initialized-reference and Rust gap

[DERIVED strict counterexample] SSA plus WellFormed does not imply E:49 `ForwardValid`. Consider the F2 descriptor with `nPublic=0`, `nVars=34`, `nWires=36`, one gate `add (wire 34) (constant 0) → wire 35`, and output `[wire 35]`. The gate operand is below its output (`34 < 35`), its output is auxiliary and in range (`34 ≤ 35 < 36`), and its final output reference is bounded. A singleton has the required pairwise order. Thus it satisfies SSA and WellFormed. But only wires 0 through 33 are initially available; wire 34 is an unwritten hole, so the initialized-reference predicate fails. C:216 proves singleton CSE is unchanged, so the same separation holds after CSE. `check.py` verifies the finite arithmetic and missing-set membership; this is a derived review counterexample, not an additional Lean theorem or a runtime execution.

[SOURCE / DERIVED] The current Rust parser in `research/learn_infer_only/experiments/end_to_end/private_ema/emitted_runtime/src/lib.rs:102` checks initialized operands at lines 136–139 and final output readiness at lines 143–145, via `validate_reference` at line 206. Its `Schedule::evaluate` at line 177 stores initially absent auxiliary wires as `None`; the existing Lean evaluator stores zeros. The counterexample's dimensions and one output match the Infer interface, but its unwritten reference fails that Rust readiness check. The new CSE theorem proves only the weaker shape conditions needed by the zero-filled Lean evaluator. It does not prove all CSE source descriptors pass the stronger parser discipline or that option-valued storage simulates the array.

[OPEN] A stronger initialized-reference preservation/result theorem may hold for genuinely emitted source descriptors; this package does not prove it. The counterexample refutes deriving readiness from SSA and WellFormed alone, not readiness of the actual frozen EMA schedules. Those schedules retain predecessor compiled validity evidence. Even a future readiness theorem would still need a representation/refinement proof for Rust parsing/storage and backend execution.

[OPEN runtime scope] Rust/serde/JSON parsing, schema and resource constraints, allocation, machine arithmetic, indexing, the Rust/compiler implementation, backend constants/XOR/AND, TFHE correctness/noise, encryption, ciphertext serialization and replay-byte behavior remain outside the kernel result. The predecessor's exact JSON reproduction remains compiled evidence. No crypto, security, utility or runtime resource claim is raised by this review.

## Independent checks

[EXECUTED] Command: `python3 research/learn_infer_only/experiments/adversarial_review/cse_structure/check.py`. `RESULTS.json` retains the result and isolated subprocess commands. Checks passed for the requested source and patch hashes, all 14 manifest-listed files, all 81 frozen predecessor files, and six directly reused/dependency source hashes, including the unchanged 19-pin execution module.

[EXECUTED] A comment-aware declaration census finds exactly 18 theorem/lemma declarations, each paired with its exact guarded `#print axioms` output. All pins use only `propext`, `Classical.choice`, `Quot.sound`, or no axioms. The selected source contains no `sorry`, `sorryAx`, `axiom`, `admit`, `native_decide` or `unsafe` token outside comments/strings. Its sole module import is `Compiler.EmittedScheduleExecution`. Exact source signatures confirm both EMA application heads and the generic source execution head have no structural/gate/output premise.

[EXECUTED] Independent patch replay in a temporary directory seeded with `Compiler.lean` from companion commit `6937394e1dc2c2aaff986c7d4b3a258aca5d16fd` reproduces the exact module bytes and adds only its umbrella import. The patch touches only `Compiler.lean` and adds `Compiler/CseStructure.lean`. The author's pinned `results/lean_005.json` is clean, with exit code 0, empty output and matching before/after source hashes. This review verifies retained compilation evidence; root owns the next independent fresh joint-overlay build. No Lean build, runtime execution, crypto operation, network query or frozen-input edit was performed by this review.

[DERIVED integration wording] The selected successor closes the structural premise for execution of the exact emitted EMA source in the existing Lean evaluator. It preserves the predecessor source and its conditional theorem rather than altering their historical scope. Maintain the separate initialized-reference/Rust refinement boundary until its own proof is checked.
