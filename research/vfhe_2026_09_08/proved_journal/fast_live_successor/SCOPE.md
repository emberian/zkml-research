# Scope

[EXECUTED] One new isolated BFV key/model, two actual teachings of the fixed
semantic texts, two fresh update proofs/commits, two fresh class-query proofs,
gate reconstruction/verification of every active score, one omitted-active-class
refusal, durable public acceptance before the first two private receives, later
integer score comparison and exact accepted-head reopen. No baseline, accuracy
benchmark or large negative grid was rerun.

[SOURCE] The new native generators execute frozen Lean-emitted witness plans.
Their emitted templates must hash to the original approved relations; actual
native proofs and independent verification are still required. Their prior
complete trace equality evidence belongs to `update_native_emitter` and
`query_native_emitter`; this service run does not regenerate an interpreter
baseline or claim byte equality between different fresh ciphertext instances.

[SOURCE] Update proofs cover all 8,192 emitted rows of the actual
`acc + fresh - old` ciphertext arithmetic. Query proofs cover all 8,192 rows of
both signed ciphertext/plaintext products and final subtraction for each active
class. The two approved template hashes are respectively
`1afc2b3a120f59fdd79d273c6d32887373c5718225cb6e94b82a7231010c3aa7`
and `f42c5efcaa994656b0c9ef2d1270aa2d6eb7dae0e5ba85938d23dbb3dc46c10d`.
No modulus/backend substitution or proof weakening was introduced.

[SOURCE] The local plaintext E5 encoder and issuer, imported canonical zero,
FIFO/counts, source-event metadata, parser/canonical NTT encoding, approved
template choice, durable model/recipient binding, reader decoder and shared OS
remain trusted boundaries. These arithmetic proofs do not prove natural-language
meaning, key membership, initial model history, authenticated user intent or
general application correctness. The normal path is serialized; no concurrent
load, power-loss or fault-tolerance experiment was added.

[SOURCE] The instance retains a **full BFV reader key**. It could decrypt retained
ciphertexts outside this service. Proof acceptance controls this adapter's
successful receive path, not cryptographic decryption capability. There is no
restricted-release, history-bound-key or confidentiality claim. This BFV service
is separate from the restricted-key ring successor, whose deterministic
verification and cryptographic key scope are different.

[EXECUTED] The outer 21.201762-second record measures the complete ML subprocess;
the 20.623207-second internal record starts inside the already imported demo.
The predecessor's 306.241377-second record is internal/demo wall time. The runs
use the same text fixture and templates but different new keys/randomness and
occurred at different times. They establish observed practical runtime, not a
controlled speedup or production throughput guarantee.

