# Next

Root can land the one cumulative minidregg-complete-infer.patch against the selected base checkout and join it to the accepted runtime. Do not stack it on the older proposed patch. README gives exact application and build commands; PROVENANCE resolves all source origins, dependencies and overlap.

This integration lane is complete. Preserve the old 51-file package and new cumulative handoff. The maintainer owns the eventual full-tree build; no theorem rebuild, exporter rerun or runtime work remains in this lane.
