[OPEN] This successor produces and freshly consumes one complete kernel proof bundle for the saved public `live001/query01/class000` model/query/evaluation-key/output digests in `runtime/REQUEST.json`. It runs no encoder, key generation, encryption, or private reader. It does not repeat or revise the historical two-class learner run.

[SOURCE] The historical emitters used transition-only `gate` assertions and therefore did not constrain arithmetic on each chunk's last row. This successor requires every arithmetic assertion in all four phases to use whole-row `window_gate` with `on_transition:false`. The shared strict migration preserves normalized arithmetic bodies, lookup tuples, and metadata for extension/tensor/rescale. Compact MAC emission uses the corrected Lean-owned fold directly. Historical accepted proofs are preserved as executed evidence and are not reused as complete arithmetic proofs.

[DERIVED] Compact MAC range completion reconstructs the omitted bit witnesses and applies the existing paired-MAC theorem. The native range backend keeps its four-bit table. An isolated lookup patch groups at most four consecutive global interactions on the same bus, preserving tuples and signed multiplicities. This changes the committed trace/permutation profile and degree. Existing numerical soundness/extraction results for an older query profile are not inherited. The pinned native profile, formal source lemmas, protocol assumptions, and remaining Rust/parser/NTT/controller boundaries are reported separately.

[EXECUTED] The symbolic native shape is retained at `backend/checks/010-compact-shape.json`: MAC main width 1293, 132 Ext4 permutation columns, degree 6, eight ZK quotient chunks. The 349-column source trace expands through the existing range compiler. FRI blowup 3, 38 queries, query proof-of-work 16, extension degree 4, four salts/random codewords, and witness hiding remain unchanged. No parameter sweep is authorized.

The single sequence emits and proves 88 MAC, four extension, 18 tensor, and six rescale chunks on the same exact saved public cases. Every proof has its normal prover self-check. A final consumer independently reconstructs the public rows and checks all 116 proofs, approved input/output digests, shared dot/square/rescale bindings, and frozen bytes. No private phase follows. Any operation error, timeout, or mismatch stops this attempt and preserves its output; there are no retries. Trace transport is compressed and ignored by Git, while proof objects and costs are retained.

The cost comparison uses the prior recorded class000 costs without baseline reproving. It explicitly changes semantics as well as representation, so it is not a controlled same-relation benchmark or a general timing guarantee. The approved model is an input ciphertext; this measurement does not repair or reprove its historical teaching chain. It verifies one class kernel, not a new complete multiclass decision.
