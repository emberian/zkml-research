# Fast live learner workload

[SOURCE] Select the fixed native update and query witness plans explicitly in
`PIPELINES.json`, preserving the original live/gated instances and configurations.
Native generators remain untrusted. The approved arithmetic templates, actual
fresh proofs, independent native verification and accepted-head service checks
are unchanged. No proof or template over a different backend is substituted.

[DERIVED planned execution] One fresh isolated BFV key/model, two declared
classes, the same two teaching texts and ATM-fee query as
`query_gate_successor/run_demo.py`. Load the existing local E5 model once; encode
the three new texts in this fresh instance. Stage each actual update, generate
its native-plan witness and fresh proof, verify again and commit. Generate and
prove each of the two active class-query results, verify both again against
exact bytes and accepted revision two, write public acceptance, then receive.

[DERIVED planned checks] Preserve the existing omitted-active-class refusal:
no private read and no changed head. Compare the two decoded scores with integer
computations only after receive, using cached vectors. Reopen the accepted head.
Record complete subprocess wall time, stage costs and each proof pipeline's
export/emission/prove/verify timings. One attempt, 900-second outer limit; no
baseline rerun, alternate arithmetic, model tuning or negative-test grid.

[SOURCE] Plaintext encoder/issuer, zero checkpoint, FIFO and class counts,
query/ciphertext parsing and NTT encoding, journal/recipient binding, full BFV
reader and shared OS remain explicit boundaries. The full key survives; this
execution does not restrict decryption or establish confidentiality. It is a
two-class semantic mechanics fixture, not an accuracy benchmark.
