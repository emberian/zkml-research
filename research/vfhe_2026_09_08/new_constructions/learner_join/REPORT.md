# Exact text learner scores through the native ring prover

[EXECUTED] One real batch over the running text learner's cached revision 128 prototypes and eight actual test queries passed: **64/64 signed integer scores and 8/8 class decisions match the exact reference**. Four new ciphertexts were created under this ring backend. The complete saved proof passed the unchanged native verifier in a fresh process before recipient decoding. This joins useful learner data to the previously executed ring-matvec prover; it does not reuse or translate the learner's BFV ciphertexts.

| [EXECUTED] Phase | Time |
|---|---:|
| Matrix adaptation and fresh encryption | 1.666 ms |
| Public preprocessing, including transform cache | 98.427 ms |
| Whole proof, all three WHIR openings enabled | 637.339 ms |
| Producer verification including preprocessing | 80.276 ms |
| Fresh saved-proof verification including preprocessing | 81.534 ms |
| Recipient decoding and exact comparisons | 8.211 ms |

[SOURCE data] `input_reference.json` pins the five approved public files from `research/learn_infer_only/experiments/end_to_end/useful_learner_2026_09_08/`: config, cached features, selected public data, plaintext learner state and exact reference results. The public rule selects the first original test row for each class, indices `[0,280,160,40,200,240,120,80]` in config class order. Each class prototype is the sum of its last eight active576-coordinate feature vectors. The 577th bias coordinate is exactly zero and is omitted. No encoder, training, private-key file or prior ciphertext is read or rerun. This reused eight-query slice has **5/8 correct labels**, exactly the original predictions. The separate original full 320-row utility result remains unchanged; this batch is an integration check, not a new accuracy estimate.

[DERIVED encoding] A single large plaintext modulus would lose the noise margin needed for these exact scores with the inherited Gaussian sigma 2^33. Instead, encode each prototype twice using coprime plaintext moduli 16381 and 16369, at scales `floor(q/p)`. Their CRT product 268,140,589 exceeds twice the conservative signed bound 74,322,432. Place the two 576-coordinate vectors 576 positions apart, with each1152-coordinate class block wholly inside a degree 4096 chunk. Three classes fit per chunk; four ciphertexts give power-of-two padding to 16384 input coordinates. A query row selects the first block; the existing reverse-and-negate matrix lift makes output coefficients 0 and 576 the two residue scores. Centered CRT recovers the exact integer. `RELATION.md` gives the full coefficient identity, rounding/noise inequality and scoped ideal-Gaussian availability bound. Native public checks validated the actual matrix lift and signed decoding before the sole crypto attempt.

[EXECUTED exact result] `SCORES.csv` lists every reference and returned score and the resulting labels. `normal_001/result.json` retains all 64 returned signed integers. Selected scores lie between 1,549,522 and 1,890,544; modular residues were not treated as classification scores. All eight final decisions agree, including the three original misclassifications. The sole command started `2026-09-08T17:02:35.353088Z`, PID 36730, finished exit 0 in 0.9933 seconds under a 600-second cap. No retry occurred. The frozen source/input hashes still agree. Both public verifiers accepted before the public-close checkpoint at 928,742,208 ns; recipient score decoding followed that checkpoint. The exact decoded values are the producer's aggregate result; no key or encryption-error tape was saved.

[EXECUTED public artifacts] The complete statement is 12,845,169 bytes; the full proof is 4,439,648 bytes. `normal_001/statement.bin` carries the actual public matrix, new ciphertext inputs and selected output ciphertexts. `normal_001/learner.proof` includes every output coefficient, both complete sumchecks and all three PCS opening claims/bytes. The unchanged `verify_saved` source reconstructs public preprocessing and transcript, checks caller-selected output, and verifies the complete proof. `public_artifacts.json` pins the five run files; `REPLAY.md` gives the reusable adapter and consumer commands. Native replay is executed; reuse in the browser consumer has been handed to independent_review.

[SOURCE implementation] `learner-matvec.patch` applies directly to upstream matvecmul commit `00379074cad457367a86dde2ecee9d0f318a7e12`, with exact replay checked. The prover, verifier, public wire codec, sumcheck, PCS and transcript match the frozen scaled predecessor byte-for-byte. The new learner adapter supplies signed query coefficients, coordinate-dependent residue scales, exact modular signed-error addition, nearest-residue decoding and centered CRT. Upstream rank-one binary-key sampling, uniform mask and sigma 2^33 Gaussian error family are retained. The scale/error encoder corrects the original demonstration's 2^64 wrap behavior for this signed, mixed-scale use. Source/build and the exact input/contract were frozen before launch in `source_pins.json` (`17fc4dfc740f59f327fa5b7dc191e8c9b081c12e93d6c1d902a570aee1ad1fc5`).

[SOURCE/OPEN scope] Ring degree 4096, matrix 64×16384, ring inner dimension 4, two RLWE components, Goldilocks base field and quadratic proof extension are explicit. WHIR retains its security-level parameter 100, `ConjectureList`, rate 1, folds 4/4 and locked source/dependencies. Cached spectra have 16 MiB of u64 payload. These are unpriced research security parameters; neither this execution nor the ideal-Gaussian bound proves security of the floating-point sampler or the inherited protocol. The proof verifies the supplied encrypted ring computation; it does not prove encoder execution, input encryption correctness or the class decision computation. Those joins are checked by this recorded native execution. The complete recipient key can decrypt all output coefficients. No BFV compatibility, full private resident, release-only credential, ciphertext interoperability, end-to-end privacy or timing-privacy claim is made.

[EXECUTED sources and ownership] No new web, Scry or Kagi query; no paper download. Only the new learner_join directory was written. Earlier evidence, learner files and companion implementation trees are unchanged. Root owns any integration or shared-ledger update.
