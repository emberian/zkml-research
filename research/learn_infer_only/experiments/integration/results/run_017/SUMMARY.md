# Combined overnight core integration

[EXECUTED] Run017 passes70 selected modules/915 exact theorem pins and all four umbrellas, with actual patch application/content comparison, both import-boundary checks and unchanged input/companion sources. Elapsed750.475898seconds.

[EXECUTED] Patch SHA256 `00175cda5228163b1626eabf94559ac3d8f406b7635f70dd01976b5301e5c3c6`. Explicit manifest: `modules_overnight_core.json`. Standard axiom-set census: `{"(none)": 50, "Classical.choice,Quot.sound,propext": 738, "Quot.sound": 4, "Quot.sound,propext": 81, "propext": 42}`. Existing dependency oleans are reused; this is not a clean whole-project/dependency rebuild or Rust refinement.

[EXECUTED retained instrument failures] Run015 rejected Lean's valid module-prefixed private declaration names. The checker now normalizes only a matching private declaration and verifies its module identity; five corruption controls are rejected. Run016's combined patch omitted the EOF marker on the upstream Apache license without a final newline. The patch emitter now preserves that byte sequence with a valid EOF marker. Neither failure changed proof bodies. Run017 applies the complete licensed patch and elaborates every exact guard.

[DERIVED additions] Prior708 + signed-word28 + emitted schedule46 + adaptation dynamics28 + polynomial/full-UD closure105. The latter is positive-degree conservative full unique decoding over existing RS objects; its probability errors are unchanged. BabyBear carrier and sampled-query follow-ons are separate and not included in this snapshot.
