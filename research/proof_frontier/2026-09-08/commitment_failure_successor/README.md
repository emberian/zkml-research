[DERIVED] This additive successor closes the observed finite-log Failure residual of the frozen five-round supplied-opening theorem in a finite classical fresh-query game. It also constructs a verifier that returns its actual hash-call log, with exactly the frozen Boolean verification result. Main minidregg and every predecessor remain read-only.

[DERIVED] The composed head is `Minidregg.Selvage.CommitmentFreshTrace.BabyBear.sound` in `src/Selvage/CommitmentFailureSoundness.lean`. For the existing BabyBearExt4 tower at levels 0→3→6→9→12→15, it proves

```
Pr_(hash coins c, independent FRI challenges/query seeds x)
  [InitiallyFar(E,c) AND actual supplied five-round acceptance(E,c,x)]
≤ 1198336 / 2013265921^4 + (24/25)^q
  + ((3*Q^2+Q)/2 + 11*Q) / N.
```

[DERIVED] `N>0` is the cardinality of the separate typed hash-answer alphabet `Fin N`; Q counts distinct fresh hash inputs. No digest size or security bits are asserted. Initial extraction farness is deliberately inside the event: a colliding hash tape can change extraction, so the theorem does not require the far-source premise on every tape. The FRI term and actual carrier/tower/radius/degree arithmetic are inherited unchanged from `ArityEight.Schedule.BabyBear.terminal_sound`.

[DERIVED] `Execution Q N q` contains concrete strategy certificates, not event-bound assumptions. For each fixed FRI challenge/query vector, a fresh hash query and its declared roots depend only on earlier hash answers. For each fixed hash tape, word/input checkpoints depend only on their permitted FRI challenge prefixes. Each checkpoint's retained entries are exactly a fresh-log prefix up to membership (replay duplicates allowed); its root is declared before every later fresh response. Checkpoint logs occur in the supplied prover log, and every prover/verifier entry is covered by the actual Oracle response log.

[DERIVED] The hash suite used by the verifier is the canonical lookup completion of that cache. `oracle_answers` and `oracleLog_eq_entries` connect adaptively selected fresh inputs to the existing `Oracle.respond`/`oracleRun` handler. `cachedSuite_entry` and `covered_cached_valid` derive actual hash-call consistency; no independent consistency assumption is used in the composed result. The fixed-Q fresh-program interface does not automatically compress an arbitrary repeated-query runtime or variable stopping-time execution; obtaining those execution certificates remains a caller task.

[DERIVED] The residual proof first reuses the existing birthday bound Q(Q−1)/(2N). At fresh query t, all previous node children, current node children and at most R declared roots form a prefix-fixed target list of size at most 2(t+1)+R. A finite-alphabet prefix-conditioning lemma sums these target hits. Thus `bad_probability` derives `((3Q²+Q)/2+RQ)/N`, and `schedule_failure_probability`/`oracle_schedule_failure_probability` cover the actual frozen Failure event. The composed BabyBear theorem uses R=11 for six word and five injected-input checkpoints. Merely listing all eleven future roots is not a causal declaration certificate.

[DERIVED] `collisionWitness` and `lateWitness` search only retained records. Their soundness and completeness are proved. The collision finder examines at most L² record pairs and makes no oracle queries; the late finder scans L records and their checkpoint targets. `covered_collision_typed` derives two distinct typed hash inputs with equal canonical hash output. This includes leaf-versus-node collisions, which the separate leaf/node alternatives in the older `BinaryMerkle.Collision` definition do not cover. No efficient collision advantage is assumed as a premise.

[DERIVED] `recomputeLogged`/`openingRun` produce the verification result and records together. Their exact result/log equalities hold even for malformed paths. `rowRun_check` identifies the new Boolean result with frozen `ArityEight.rowCheck`; `verificationLog_logged` discharges every supplied-opening log-inclusion premise from constructed runs. One row costs at most `8*(sourceDepth+1)+2*(destinationDepth+1)` hash records. On the actual five rounds this is 204+174+144+114+84 = 720 per shared query seed. The earlier informal 770 estimate was corrected by the checked arithmetic before freezing. These are emitted hash-record counts, not measured total running time or list-allocation costs.

[DERIVED] `sound_call_budget` substitutes Q≤B+720q when the prover log has at most B records and all counted cache entries are covered by prover plus generated verifier calls. Thus the residual is `((3*(B+720q)^2+(B+720q))/2+11*(B+720q))/N`. Repeated calls consume records while reusing a single cache entry. Root list length is separately proved as 2m+1; it does not count cryptographic hash invocations.

[DERIVED] `CommitmentTraceWitnesses` supplies a genuinely adaptive two-query strategy over Fin17: the second node query contains the first answer, while leaf/node tags force distinct inputs on every tape. A good tape has no bad event; two other tapes produce an actual late-root hit and a retained leaf/node collision. Its checked bound is 9/17. Choosing a root equal to a newly seen response gives a late hit on every tape but violates `CheckpointOrigin`, showing the timing premise is substantive.

[DERIVED] The instrumented checker accepts the existing far F17 source's honest row zero, rejects row one, retains all required supplied paths and emits exactly 44 records on the accepted row. Separately, `CommitmentExecutionWitness.execution` inhabits every finite-digest causal/log certificate for any q by replaying one leaf query; positive q rejects its deliberately absent paths. These are distinct controls: the accepted far-source toy-tree witness is not an instantiated finite-digest random-oracle experiment, and the finite-digest rejection witness does not claim initial farness.

[EXECUTED] Seven additive modules, all theorem axiom guards, exact changed-module checks, the normal import boundary and exact additive patch replay are recorded in MANIFEST.json and checks/. The standalone probe evaluates the collision and late-hit finders and the instrumented accepted/rejected rows. No broad build, independent review campaign, new web search or Scry query was run. The patch depends on frozen finite/one-round and efficient-opening packages; DEPENDENCIES.json records the exact closure.

[OPEN] To apply this to a concrete prover/verifier, construct the causal cache/checkpoint certificates, account for actual prover preprocessing queries, and transport the packed MMCS/variable-arity/index/terminal-polynomial implementation into the existing scalar supplied-opening semantics. Uniform fresh hash answers and uniform FRI challenge/query coins are separate assumptions of this classical finite game. Fiat–Shamir transcript generation, quantum queries and deployed hash security are outside the result. The weak `(24/25)^q` coherent-query tail remains unchanged and is the next proof target. No ErrorBudget, packed-runtime equivalence or security-bit claim is added.
