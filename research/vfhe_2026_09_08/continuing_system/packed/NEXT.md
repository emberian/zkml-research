# Next

The whole lifecycle and fresh browser flow are complete. Preserve the frozen result packages and existing instances; do not repeat the workload for timing polish. Root owns publication and subsequent architectural work. Future changes to bound code require a distinct profile/instance or explicit compatible migration, not silently changing existing genesis pins.
