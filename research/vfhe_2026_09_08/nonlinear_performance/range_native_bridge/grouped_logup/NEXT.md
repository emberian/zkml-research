# Next

Integrate this frozen package once into the complete continuing learner. The AIR/PCS owner can supply the actual fixed source rows and byte-table interpretation, use `NativeByteExtraction.sound` to replace the former histogram-conservation assumption with a priced fresh-coin failure event, then feed `checked_for_infer` to the existing Infer composition. Keep the source-list binding and native/Fiat–Shamir boundaries explicit.

The fixed-four sender-group count is already generic for the current MAC, extension and rescale profiles. No further lane expansion, replay campaign or theorem chain is queued.
