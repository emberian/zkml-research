# Check the exact feature ambiguity witness

[EXECUTED] Two distinct continuing int8 feature histories agree on every one of the actual sixteen registered semantic query projections, including each issued input and all six memory snapshots. A real unregistered query distinguishes the final card-arrival memories by 190 score units. This is a feature-domain construction, not a pair of natural-language embeddings or a cryptographic attack test.

Verify the retained certificate using only Python's standard library:

```sh
python3 verify.py
```

The checker reads the frozen public registry, six-input fixture and saved public feature array. It does not need private keys, a model, a cryptographic backend or LLL. [REPORT.md](REPORT.md), [PROOF.md](PROOF.md), [WITNESS.json](WITNESS.json) and [VERIFICATION.json](VERIFICATION.json) contain the result.

[SOURCE] `construct.py` records the one exact integer LLL construction. Its existing FLINT/NumPy environment is `../../private_construction/public_setup_pq/ring_implementation/.venv/bin/python`. The completed construction outputs should be preserved; the verification command is sufficient to check the claim.
