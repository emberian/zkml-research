# Paired modular MAC for the actual nonlinear Infer

[DERIVED, Lean checked] This source forces both equations `out_h = (add_h + sum_i D_i*K_hi) mod q`, sharing four canonical D words across two outputs. All sixteen input/addend/output word bounds are source constraints. `Compiler/BfvKeyswitchCore.lean`, `Sound` and `sound`, state and prove the universal implication for every accepted assignment. `Compiler/BfvKeyswitchRow.lean`, `emittedSystem_sound`, instantiates it at the four actual moduli and the public 97-column layout.

[EXECUTED] The exact saved evaluation-key protobuf has ten Galois keys, all with ciphertext level zero, key level zero and `log_base=0`. Each contains four c0 polynomials and a 32-byte public seed for c1 regeneration. This selects the existing multi-prime residue-row lift and paired four-term MAC; no bit decomposition or key-switch context reduction is executed. `PUBLIC_PROVENANCE.json` binds the public key, exact model ciphertext, query, revision9 model manifest, frozen operation source, retained Infer command and known square endpoints. `inspect_public.py` reads only these public bytes. The old one-class `card_arrival` run had nine teaches and one expiry; its saved model manifest is not a historical journal commitment.

[EXECUTED] Four exact modulus profiles have been emitted in `artifacts/`: trace width 2655, public arity 97, 2816 source arithmetic constraints plus the complete public-row lookup, and 8783 generic native witness instructions/registers. `READY.json` pins all callable sources, templates, plans and the existing BigInt executor. These bytes are frozen. The runtime's `../full_bfv_multiply_successor/infer/` owns the complete 88-chunk execution, binding the initial ciphertext–plaintext multiplication and ten rotation/accumulation stages to the same saved square. Its final verification status is separate from source readiness here.

[DERIVED] Two additive words let this same relation cover each complete rotation-plus-accumulation step: public linear transforms supply `add0=oldc0+substitutedc0` and `add1=oldc1`. The one-active-product, zero-addend instance covers initial ciphertext–plaintext multiplication. The relation proves the nonlinear modular MAC; public residue lift, key-seed expansion, canonicalization, Galois permutation, NTT, query encoding and stage linkage remain the enclosing runtime/formal composition obligations.

[EXECUTED] All 33 theorem declarations have exact guarded axiom censuses in `Compiler/BfvKeyswitchAxiomPins.lean`, with clean exit zero and empty output. Only subsets of `propext`, `Classical.choice`, and `Quot.sound` occur. No added source uses `sorry`, an axiom declaration or `native_decide`. The concrete source witness modulo37 has D=[1,2,3,4], K0=[2,2,2,2], K1=[3,3,3,3], addends7/5 and outputs27/35. Changing output27 to19 omits the final2*4 term: all sixteen words remain canonical, but `wrong_refused` proves source rejection. The universal quotient bound, explicit premise inhabitation, field-column capacity and all four deployed modulus capacities also compile.

[EXECUTED] `proposal.patch` adds seven minidregg files after the existing compiler/query-builder prerequisites. It passes read-only `git apply --check` in the companion and applies byte-identically in owned staging. The companion import-boundary script passes. `source_pins.json` records source origins and `results/dependency_artifacts.json` pins 183 cached read-only dependency artifacts. Single-file Lean checks and initial diagnostics remain in `results/`; the initial scaffold script there is historical, while the current Compiler files are the patch source. No full Lean build, native crypto, learner/model rerun, secret-key read or private comparison was performed by this lane.

[OPEN] The source implication is not a cryptographic security theorem or a universal refinement theorem for the native witness interpreter. A faulty witness producer may fail; it cannot alter the generated relation. Complete Infer needs the runtime's exact public linkage and fresh proof verification plus the separately owned public transform composition. Reader scope and model commitment policy remain properties of the enclosing application.
