# One native terminal-row control

[EXECUTED] `results/control001` confirms that the old query template admits an
arithmetic error on its physical last row, and that the generated whole-domain
repair enforces the corresponding gate there.

The helper reused the saved public arithmetic witness and public tuples from
`fast_live_successor/fast001/class0`. It changed exactly one cell: row 8191,
column 43, the first output digit, from 11 to 10. The matching exact-public tuple
received the same change. No other witness cell changed. Original files remain
unchanged. This constructs an IR2 raw-row statement, not a modified FHE container;
it performs no learning, setup, encryption or private-key operation.

The actual native `ir2_eval_accepts_i64` gate evaluator returned `true` for the
old template and `false` for the repaired template. That evaluator intentionally
omits lookup-bus checks. To establish the complete old-backend acceptance, the
helper then produced **one** proof for the malformed old statement. The backend
self-verified it, and the helper serialized, strictly deserialized and verified
it again using the unchanged backend. Thus the accepted old proof includes PCS,
quotient and global LogUp verification, not just the gate oracle.

The same proof was also checked against the repaired statement and rejected with
`OodEvaluationMismatch { index: Some(0) }`. This is additional statement-binding
evidence. The separate native gate-oracle contrast establishes that the repaired
last-row gate fires; rejection of a proof for another statement alone would not
establish that. No proof was generated for the repaired malformed statement.

The repaired template was generated by the theorem owner's corrected generic
Lean emission, SHA256
`504ec55e5421ec3116fff563974a1ef6f849fb08caea20daec3f0fcf72c9c933`.
Its 2,744 arithmetic bodies, lookup and metadata are preserved; their selector
becomes whole-domain, with the corresponding local-leaf expression dialect.
This helper authors no arithmetic constraint or template transformation.

| Observation | Result |
|---|---:|
| Old native main gates | accepted, 0.598192 s |
| Repaired native main gates | refused, 0.579668 s |
| Old proof generation including backend self-verification | 4.147961 s |
| Explicit old proof deserialization/verification | accepted, 0.104234 s |
| Old proof under repaired statement | refused, 0.107498 s |
| Entire command | 5.943987 s |
| Peak child RSS | 2,367,062,016 bytes |
| Retained old malformed proof | 842,358 bytes |

Exact evidence is [results/control001/artifacts/result.json](results/control001/artifacts/result.json),
SHA256 `8afc17f7d3820f5cd514508b24fc0d225340a4ed1eebb46d9c17854adf7493fe`;
the command/source/input pins are in [results/control001/command.json](results/control001/command.json),
SHA256 `0f44fedd62052438db8e547efbbc0c12d5db1db0df06ac7b06eceea982990148`.
The old malformed proof hash is
`3497e4a37522cc9d188d0d11bcecc86cb194af0580f336d2b8860205e24714c8`.

To build this isolated helper from the repository root:

```sh
cargo build --release --offline \
  --manifest-path research/proof_frontier/2026-09-08/ir2_verifier_bridge/air_pcs_join/native_terminal_control/Cargo.toml \
  --target-dir research/vfhe_2026_09_08/query_runtime/target
```

The single executed control command was:

```sh
python3 -B research/proof_frontier/2026-09-08/ir2_verifier_bridge/air_pcs_join/native_terminal_control/run.py \
  research/proof_frontier/2026-09-08/ir2_verifier_bridge/air_pcs_join/whole_domain/generated_query/template_ir2.json \
  504ec55e5421ec3116fff563974a1ef6f849fb08caea20daec3f0fcf72c9c933 \
  research/proof_frontier/2026-09-08/ir2_verifier_bridge/air_pcs_join/native_terminal_control/results/control001
```

The launcher requires a fresh output directory and the exact supplied repaired
template hash. The native helper asserts the old approved template hash and
fixed 8,192×2,509 trace/57-column public shape. All outputs are public evidence;
the original 82 MB trace is read in place and not duplicated. The completed
control is sufficient; no negative grid or further proof run was performed.
