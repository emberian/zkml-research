# Degree-M polynomial kernel successor

[EXECUTED — complete] Generalized the single existing determinant/rank proof in Theory.PolynomialMatrixKernel to entry degree M and kernel coefficient bound M*e. All old degree-one signatures and guard blocks are preserved; the two main old proofs now specialize M=1. Isolate: /tmp/minidregg-polynomial-curve-kernel-20260908. Existing frozen packages and companion main remain read-only. Interface agreed with proof_frontier.

[EXECUTED] Final single-module Lean check passes with empty output, 26/26 exact standard-only pins, parametric nonzero witnesses and degree/determinant teeth. Complete new-file patch, clean application/byte equality, import boundary, source pins and API preservation are packaged. See README.md and manifest.json.

[OPEN] Root must select this successor instead of the old kernel patch and owns full integration. The curve lane owns its coding/proximity consumer.
