# Handoff and remaining obligations

[OPEN immediate] Finish the independent source/math review of the frozen
candidate, ring regularity and finite parameter notes. The reviewer owns
`experiments/adversarial_review/ring_fixed_coordinate/`. Respond to required
repairs without changing earlier frozen scalar/ALS derivations. Root owns
shared ledgers, commits, publication and any further execution authorization.

[OPEN exact computational hypothesis] Assess the specified uniform-secret
Ring-LWE tuple over R=Z[T]/(T^4096+1), the exact certified q, 64 ring samples,
and coefficient errors with mass exp(-πe²/2^20). The Gaussian standard-
deviation counterpart is 2^10/sqrt(2π), not 2^10. The expanded matrix is
structured. Do not silently substitute an ordinary-LWE matrix model,
normal-form secret, different modulus, or an empirical security level.

[OPEN implementation] A concrete sampler/representation contract must cover
recipient Gaussian rows, encryption Gaussian errors, uniform modular draws,
scalar floods and any logged rejection coins. Supply tuple-level statistical
error and actual resources. No public master/absent secret is needed by the
mathematical algorithms. Any NTT implementation must match the exact
negacyclic constant-product signs and chosen q/root convention.

[OPEN application] Preserve full centered B-transform inputs, nearest actual
Δ-codepoint decoding, signed scalar coefficient bound W, exact ciphertext
expiry and per-input recipient projection leakage. Supply a semantic-image
nonvacuity witness before claiming a hidden learner beyond the ambient
F_p^d construction. This theorem does not establish integrity or selected-
output simulation.

[DERIVED possible improvement] The k=2 ring lemma masks a complete extra ring
output before projecting it to one scalar. A sharper mixed ring/scalar
entropy theorem could lower σK. It is not needed for the current finite
witness and should not replace its proof without a separate review.
