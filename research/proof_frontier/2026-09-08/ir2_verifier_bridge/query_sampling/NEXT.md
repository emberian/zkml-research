# Integration handoff

[OPEN] Import `Selvage.BabyBearModuloSampling` into the shared IR2 verifier adapter and instantiate `fresh_bits_independent_bound ell hell q E t ht` at its actual supported log height and composite survival predicate. The two frozen sources are in `Selvage/`; `CHECKS.json` carries their exact checks.

[OPEN] Root owns verifier-to-model connection and any challenger-distribution premise. Integrate through the shared outcome; no standalone patch or manifest is supplied by this lane.
