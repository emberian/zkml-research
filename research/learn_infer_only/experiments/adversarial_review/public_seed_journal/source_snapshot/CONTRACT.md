# Public-seed setup joined to the durable designated journal

[DERIVED contract, 2026-09-08] One fresh normal durable integration witness: 33 Learn,
four Infer at Learn counts 1/16/32/33 using rows 0/1/2/3, and one exact-original FIFO
expiry at Learn 33 on route zero with capacity 32. Route one stays empty. The complete
16-by-577 row matrix and known vectors `x[n,j]=((n+3)*(j+5) mod17)-8` are fixed now.
The final queue contains original inputs 2 through 33. This is arithmetic and durable
integration, not semantic utility or a private-state experiment.

[DERIVED added evidence] The completed primitive public-seed adapter has no durable
journal, while the prior public-coin journal used separately sampled accepted values.
This successor joins the concrete complete-registry SHAKE recipe to actual authorization,
durable authority/independent-acceptor history, public host computation, storage and
deferred recipient drain. It does not rerun either prior workload or strengthen its
cryptographic confidentiality claim.

[SOURCE code boundary] SOURCE_PINS.json inventories byte-identical frozen crypto,
eight journal/service modules, public log checker and persistent host with its frozen
sources. The complete 12-file seed adapter source/contract/helper packet and its
SOURCE_PINS are copied unchanged under source/seed_setup/. That nested manifest retains
its accepted adapter identity; the new outer inventory separately binds copies/origins.
No gate evaluator, crypto arithmetic, host policy or journal implementation changes.
Only new setup/orchestration, fixture, public closure and launch wrappers are authored.

[DERIVED setup] All 16 fresh independent signing registrations and dedicated recipient
scalars are created before the single fixed seed candidate is derived. The unchanged
seed wrapper verifies complete announcements, invokes SHAKE256 with full canonical
domain/seed/role/coordinate/counter framing and capped first acceptance (128), and
recomputes its full raw tape and context. Nested seed identity remains exactly the
accepted primitive contract. No beacon, secret seed, key reuse, key redraw, seed search,
alternate domain or post-rejection retry is used. A cap failure stops before context
creation and forbids private drain. This workload does not deliberately exercise failure.

[DERIVED genesis binding] The new public_seed_setup genesis extension binds the full
public transcript, registry, all 16 announcements, successful full verification record,
context and context-validation record. It also binds the seed bytes/suite/cap and full
domain digest, the transcript's complete setup source_identity, seed-adapter manifest,
new join source and outer dependency inventory. Existing canonical genesis hashing
propagates that extension through each authorization, state, signed finalization and
installed head. The unchanged services enforce the pinned genesis, rather than executing
a new setup verifier or proving honest registration. Timing-bearing verification and
validation records committed into genesis remain outside the theoretical ROM/DDH view.

[DERIVED fixed history] There are 37 finalized events. Authority and independent
acceptor close and reopen orderly at revision 18 (after Learn16 and its Infer), retaining
the exact head and accepted selections. After revision37, the exact saved Infer request
from Learn1 is submitted once as an authorized historical retry. It must return the
identical signed envelope, add no revision or selection and preserve the final head.
This is ordinary supported dedup behavior, not fault injection or a cryptographic
restriction on the recipient. No crash, extraction, routing, malformed-input, grinding
or stopped-path case is included. There is only one private drain, with four comparisons.

[DERIVED public acceptance gates] Every authority envelope must equal the independent
acceptor's complete ordered history. Existing full public arithmetic replay must recompute
all 37 transitions and the exact original expiry/final queue. Full seed/context replay,
source/native/interpreter/signature pins, public log omission, signed history, public
CAS/storage inventories and orderly service shutdown must succeed before private drain.
A separate public-only checker runs after the public worker and all its descendants
have exited. Its exact report and hashes are bound into a final public gate. Public
records are sealed before any recipient decoder process starts. No failed public stage
permits private drain, even if partial outputs already exist.

[DERIVED bounded launch] Only runtime/normal_001 and reports/normal_001 are permitted.
Preparation makes source/environment pins without crypto. Launch requires the independent
reviewer's accepted disposition for that exact preparation and root notification. The
outer launcher owns a separate process group for each public/check/private worker and
enforces a 2,700-second (45-minute) overall deadline across them. It stops blocked RPCs
or subprocesses by terminating the whole group and checks descendant-group absence
independently of leader status, escalating within a bounded cleanup grace period. Any
failure, timeout or cleanup failure stops the sole attempt; no new source, resumption,
retry run or private continuation is permitted. Source pins are checked at phase gates
and by the inherited cryptographic calls. Small source/AST/public-helper checks may run
before review; cryptography may not.

[DERIVED private boundary] Only after all public work, final public verification and
process-group closure do four recipient decryptions occur through the unchanged private
drain. A separate direct integer-window formula checks the four saved answers; private
scalars, answer values/hashes and decode costs stay under ignored .private/ paths. Only
aggregate success is published outside the closed cryptographic transcript. The public
acceptor has no recipient scalar path. All16 recipient keys still exist and their
coalition retains the full per-input row span, including expired inputs; software-selected
outputs and durable dedup do not attenuate that authority.

[DERIVED remaining assumptions] Honest independent registration, classical DDH and ideal
oracle assumptions are distinct from concrete SHAKE execution, Ed25519 authentication,
SHA256 addressing, compiled runtime correspondence and OS-backed randomness. The ideal-bit
one-candidate availability bound remains below 577/2^128; this run cannot prove a concrete
hash probability, QROM/PQ security or physical timing privacy. Known public fixture inputs,
shared-account processes and full recipient keys remain. No operator confidentiality,
selected-answer-only release, hardware finality, hidden nonlinear learner or physical
erasure claim is added.

[OPEN launch] Sources are being prepared for independent prelaunch review. Root owns
shared ledgers/commits. No cryptographic launch, sampling, model execution or service
start has occurred in this successor.
