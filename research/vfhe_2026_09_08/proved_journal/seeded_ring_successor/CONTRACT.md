# One shared seeded semantic ring service run

[DERIVED planned execution] Select the completed sixteen-query general-B
semantic registry before setup. Use the new transport's fixed 384-bit public
seed policy in one fresh candidate-full setup, with sixteen honest recipient-
local keys/registrations. The transport builder supplies the implementation and
theorem application; this service launches the only shared setup/workload.
No old key, old setup, universal reader or missing-row secret is an input.

[DERIVED planned execution] Reuse the six public cached training vectors and
two-class capacity-two contract: six fresh encodings and six committed updates,
with actual oldest-input expiries on events five and six. Each candidate must
match complete canonical recomputation of `acc + fresh - old` in this exact
ring before an atomic current-parent commit. Bind the selected setup/seed
policy, registry, recipient registrations, input identities and full model root
into the durable genesis/requests/receipts.

[DERIVED planned checks] Before event five, change one canonical candidate
residue and adjust its declared hashes; require recomputation refusal with no
head change. Reopen the journal in a fresh public process at revision four.
Require a stale-parent refusal and an exact historical update retry. Finish
all public transition/recomputation and source/setup checks before private
recipient queries.

[DERIVED planned execution] Deliver final accepted revision six to sixteen
registered fixed-query recipients under explicit request/delivery IDs. Each
recipient opens only its own new key in its sandbox. Retain an exact delivery
retry without another key invocation and a changed-ID-binding refusal. Compare
the 32 decoded scores and sixteen decisions with the known public fixture only
after public completion. No encoder/model rerun or new accuracy claim.

[SOURCE scope] Complete deterministic verification is not a succinct/compiler
proof. Fixed keys remain usable outside the journal on retained inputs and
allowed combinations; a coalition obtains its per-input registered span.
Service continuity and narrower fixed keys do not give cryptographic history-
bound release. Seeded setup has the builder's conditional QROM interpretation;
SHAKE is its concrete heuristic instantiation, not an unconditional ideal-
uniform distribution. Honest registry/setup/sampling, issuer, codec/arithmetic,
delivery ledger and shared OS remain trusted boundaries.

[DERIVED bound] One shared normal attempt, 900 seconds overall and 180 seconds
per actor. No second setup, baseline rerun, seed search, tuning, extraction or
unrelated routing tests. Freeze the exact selected transport policy and source
pins before launch. Preserve all predecessor, companion and stopped-task bytes.
